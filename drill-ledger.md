# دفتر حساب تمرین ترجمهٔ متن

این فایل حافظهٔ بین‌جلسه‌ای پروژه است. قبل از تولید هر متن خوانده و بعد از آن به‌روز می‌شود.
فایل تمرین دورهٔ جاری: `texts/01/drill.html`

---

**دورهٔ جاری: ۱** — پوشهٔ `texts/01/`

## وضعیت متن‌ها — دورهٔ ۱

| # | موضوع | وضعیت | تعداد واحد |
|---|---|---|---|
| ۱ | فناوری و جامعه | تولید شد | ۲۴ |
| ۲ | آموزش و دانشگاه | تولید شد | ۲۴ |
| ۳ | کار و اقتصاد | تولید نشده | — |
| ۴ | شهر و محیط زیست | تولید نشده | — |
| ۵ | سلامت و سبک زندگی | تولید نشده | — |
| ۶ | سفر و مهاجرت | تولید نشده | — |
| ۷ | رسانه و اطلاعات | تولید نشده | — |
| ۸ | تاریخ و میراث | تولید نشده | — |
| ۹ | بحث و استدلال | تولید نشده | — |
| ۱۰ | جمع‌بندی فشرده | تولید نشده | — |

---

## شمارش پوشش

عدد هر نکته = تعداد واحدهایی که تا اینجا آن را تمرین کرده‌اند.

### لایهٔ ۱ — هدف: ۲۰+ بار

| نکته | شمارش |
|---|---|
| حال کامل / گذشتهٔ ساده | ۱۸ |
| حال ساده / استمراری | ۹ |
| حرف تعریف (the / a / صفر) | ۱۹ |
| شمارش‌پذیری و مطابقت | ۱۴ |
| بند موصولی | ۱۴ |
| مجهول | ۱۱ |
| at / on / in | ۹ |
| gerund / infinitive | ۱۳ |

### لایهٔ ۲ — هدف: ۶ تا ۱۰ بار

| نکته | شمارش |
|---|---|
| شرطی و نردبان فاصله | ۳ |
| modal و hedging | ۴ |
| مقایسه | ۵ |
| ارجاع و ضمیر | ۵ |
| although / despite / however | ۴ |
| بند اسمی | ۲ |
| فاعل صوری there / it | ۳ |
| جای قید | ۱۰ |

### لایهٔ ۳ — هدف: ۲ تا ۴ بار

| نکته | شمارش |
|---|---|
| سببی | ۰ |
| نقل قول و backshift | ۰ |
| وارونگی | ۰ |
| جانشینی و حذف | ۱ |
| وجه التزامی | ۰ |
| do تأکیدی | ۰ |
| اعداد و درصد | ۵ |
| صفت مرکب | ۲ |
| مالکیت و آپاستروف | ۲ |
| -ever / no matter | ۰ |
| افعال عبارتی | ۲ |
| نقطه‌گذاری و comma splice | ۲ |

---

## یادداشت‌های جلسه

بعد از هر متن، اگر چیزی پیش آمد که جلسهٔ بعد باید بداند، همین‌جا بنویس — مثلاً موضوعی که سخت‌تر از انتظار بود، نکته‌ای که باید زودتر برگردد، یا تصمیمی که ترتیب متن‌ها را عوض کرد.

- **متن ۱ (۲۴ واحد):** فایل `texts/01/drill.html` از قالب ساخته شد (`STORE_KEY = "ielts-d1"`). مسیر پوشه در سرتیتر این دفتر از `Grammar/01/` به `texts/01/` اصلاح شد. شمارش‌ها هر واحدی را حساب کرده‌اند که آن نکته در بخش «متغیر» یا «تله»اش واقعاً توضیح داده شده، نه فقط در `alt` یا `rule`.
- **افعال عبارتی در متن ۱ فقط در `alt` آمدند** (`break out`، `come about`، `go back`) و شمرده نشدند؛ متن ۲ دو واحد اضافه کرد (`go on to`، `point out`). متن ۴ ستونش است.
- **تله‌هایی که در متن ۱ خوب جواب دادند و ارزش برگشتن دارند:** `until` + فعل منفی فارسی (واحد ۲۲)، «از … خواسته شد» ← `were asked to` (واحد ۱۰)، «عادت کرده‌اند که … کنند» ← `used to hold` که معنا را وارونه می‌کند (واحد ۱۶).
- **شیوهٔ املایی بریتانیایی** انتخاب شد (`at weekends`، `centre`، `£`، `per cent`). در متن‌های بعدی ثابت نگه دار.
- **متن ۲ (۲۴ واحد):** ستون‌ها پوشش داده شدند: نهاد به‌عنوان کارکرد (واحدهای ۱، ۲، ۱۵)، منبع ۱ و ۳ «قابل تشخیص بودن» (۴، ۹، ۱۹)، و تقابل defining/non-defining (۶ در برابر ۱۰، ۱۶ در برابر ۱۷، ۲۰). بند موصولی از ۴ به ۱۴ رسید.
- **مجهول واقعی کم است.** تقریباً همهٔ واحدهای مجهول در دو متن از جنس «این را مجهول نکن» بوده‌اند (`happen`، `begin`، `rise`، `graduate`، `narrow`). مجهولِ لازم با زمان‌های مختلف و `by` هنوز فقط در متن ۱ (واحدهای ۵، ۹، ۱۰، ۱۸) آمده. متن ۸ ستونش است، ولی متن ۳ هم دست‌کم دو مجهول واقعی داشته باشد (مثلاً Task 1: `was recorded`، `are expected to`).
- **اعداد و درصد از هدف لایهٔ ۳ جلو زده (۵)،** و متن ۳ ستونش است. در متن ۳ آن را تمرین کن ولی بعد از آن کنارش بگذار.
- **وارونگی فقط در `alt` واحد ۲۲ متن ۲ آمد** (`Were university education free`) و شمرده نشد. بند اسمی (۲) و فاعل صوری (۳) هم عقب‌اند؛ متن ۳ جای طبیعی `It is worth noting that` و `what` + بند است.
