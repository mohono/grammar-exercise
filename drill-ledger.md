# دفتر حساب تمرین ترجمهٔ متن

این فایل حافظهٔ بین‌جلسه‌ای پروژه است. قبل از تولید هر متن خوانده و بعد از آن به‌روز می‌شود.
فایل تمرین دورهٔ جاری: `texts/01/drill.html`

---

**دورهٔ جاری: ۱** — پوشهٔ `texts/01/`

## وضعیت متن‌ها — دورهٔ ۱

| # | موضوع | وضعیت | تعداد واحد |
|---|---|---|---|
| ۱ | فناوری و جامعه | تولید شد | ۲۴ |
| ۲ | آموزش و دانشگاه | تولید نشده | — |
| ۳ | کار و اقتصاد | تولید نشده | — |
| ۴ | شهر و محیط زیست | تولید نشده | — |
| ۵ | سلامت و سبک زندگی | تولید نشده | — |
| ۶ | سفر و مهاجرت | تولید نشده | — |
| ۷ | رسانه و اطلاعات | تولید نشده | — |
| ۸ | تاریخ و میراث | تولید نشده | — |
| ۹ | بحث و استدلال | تولید نشده | — |
| ۱۰ | جمع‌بندی فشرده | تولید نشده | — |

---

## شمارش پوشش

عدد هر نکته = تعداد واحدهایی که تا اینجا آن را تمرین کرده‌اند.

### لایهٔ ۱ — هدف: ۲۰+ بار

| نکته | شمارش |
|---|---|
| حال کامل / گذشتهٔ ساده | ۱۱ |
| حال ساده / استمراری | ۶ |
| حرف تعریف (the / a / صفر) | ۹ |
| شمارش‌پذیری و مطابقت | ۷ |
| بند موصولی | ۴ |
| مجهول | ۶ |
| at / on / in | ۵ |
| gerund / infinitive | ۷ |

### لایهٔ ۲ — هدف: ۶ تا ۱۰ بار

| نکته | شمارش |
|---|---|
| شرطی و نردبان فاصله | ۱ |
| modal و hedging | ۳ |
| مقایسه | ۲ |
| ارجاع و ضمیر | ۳ |
| although / despite / however | ۱ |
| بند اسمی | ۱ |
| فاعل صوری there / it | ۲ |
| جای قید | ۶ |

### لایهٔ ۳ — هدف: ۲ تا ۴ بار

| نکته | شمارش |
|---|---|
| سببی | ۰ |
| نقل قول و backshift | ۰ |
| وارونگی | ۰ |
| جانشینی و حذف | ۰ |
| وجه التزامی | ۰ |
| do تأکیدی | ۰ |
| اعداد و درصد | ۳ |
| صفت مرکب | ۰ |
| مالکیت و آپاستروف | ۱ |
| -ever / no matter | ۰ |
| افعال عبارتی | ۰ |
| نقطه‌گذاری و comma splice | ۰ |

---

## یادداشت‌های جلسه

بعد از هر متن، اگر چیزی پیش آمد که جلسهٔ بعد باید بداند، همین‌جا بنویس — مثلاً موضوعی که سخت‌تر از انتظار بود، نکته‌ای که باید زودتر برگردد، یا تصمیمی که ترتیب متن‌ها را عوض کرد.

- **متن ۱ (۲۴ واحد):** فایل `texts/01/drill.html` از قالب ساخته شد (`STORE_KEY = "ielts-d1"`). مسیر پوشه در سرتیتر این دفتر از `Grammar/01/` به `texts/01/` اصلاح شد. شمارش‌ها هر واحدی را حساب کرده‌اند که آن نکته در بخش «متغیر» یا «تله»اش واقعاً توضیح داده شده، نه فقط در `alt` یا `rule`.
- **بند موصولی عقب است (۴):** در متن ۲ که ستونش defining/non-defining است، جبران می‌شود؛ ولی non-defining با کاما هنوز حتی یک بار هم نیامده. به موصولیِ کاهش‌یافته هم فقط یک بار (واحد ۹) پرداخته شد.
- **افعال عبارتی فقط در `alt` آمدند** (`break out`، `come about`، `go back`) و شمرده نشدند. متن ۴ ستونش است، ولی بهتر است متن ۲ یا ۳ هم یکی دو واحد داشته باشد.
- **تله‌هایی که خوب جواب دادند و ارزش برگشتن دارند:** `until` + فعل منفی فارسی (واحد ۲۲)، «از … خواسته شد» ← `were asked to` (واحد ۱۰)، «عادت کرده‌اند که … کنند» ← `used to hold` که معنا را وارونه می‌کند (واحد ۱۶).
- **از قلم نیفتد:** شیوهٔ املایی بریتانیایی انتخاب شد (`learned` هم پذیرفته است ولی `at weekends` و `centre` آمده‌اند). در متن‌های بعدی همین را ثابت نگه دار.
