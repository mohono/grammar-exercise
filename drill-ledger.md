# دفتر حساب تمرین ترجمهٔ متن

این فایل حافظهٔ بین‌جلسه‌ای پروژه است. قبل از تولید هر متن خوانده و بعد از آن به‌روز می‌شود.
فایل تمرین دورهٔ جاری: `texts/04/drill.html`

---

## وضعیت متن‌ها — دورهٔ ۴ (جاری) — `texts/04/drill.html` (`STORE_KEY = "ielts-d4"`)

نقشهٔ دورهٔ ۴ در ۲۰۲۶-۰۹-۱۹ تأیید شد. اصل نقشه: **هر ردیف عقب‌مانده یک بار ستون یک متن می‌شود، نه مهمان لایهٔ ۳.** دورهٔ ۳ نشان داد ردیفی که فقط لایهٔ ۳ است، دو واحد می‌گیرد و می‌ایستد. چهار ردیف صفرماندهٔ آخر دورهٔ ۳ (`do` تأکیدی، التزامی، backshift، صفت مرکب) و سه ردیف سبک دورهٔ ۳ (اسم‌سازی، ثبت، وزن جمله) هر کدام ستون یک متن‌اند. ستون متن ۵ یک **خانوادهٔ تأکید** است نه یک ساختار، چون `do` تأکیدی تنهایی یک متن را پر نمی‌کند.

| # | موضوع | ستون | لایهٔ ۳ | وضعیت | تعداد واحد |
|---|---|---|---|---|---|
| ۱ | خواب نوجوانان و ساعت شروع مدرسه | نقل قول، backshift و گزارشِ پژوهش | صفت مرکب، مالکیت | تولید شد | ۲۵ |
| ۲ | پول نقد و پرداخت دیجیتال | اسم‌سازی: فارسیِ فعل‌محور ← انگلیسیِ اسم‌محور | اعداد و درصد، جانشینی | تولید شد | ۲۵ |
| ۳ | کار از راه دور و شهرهای کوچک | ثبت (register) و فاصلهٔ ایمیل تا مقاله | سببی، افعال عبارتی | تولید شد | ۲۵ |
| ۴ | پلاستیک یک‌بارمصرف و بسته‌بندی | صفت مرکب و گروه اسمیِ سنگینِ چپ‌چین | `-ever`، نقطه‌گذاری | تولید شد | ۲۵ |
| ۵ | باغ‌وحش‌ها و حفاظت از گونه‌ها | چطور انگلیسی تأکید می‌سازد: `do`، cleft، پیش‌آوری | وارونگی، `do` تأکیدی | تولید شد | ۲۵ |
| ۶ | هنر و موسیقی در مدرسه | وجه التزامی و زبان توصیهٔ سیاستی | نقل قول و backshift، سببی | تولید شد | ۲۵ |
| ۷ | ورزش حرفه‌ای و پول پخش تلویزیونی | «حرف اضافه + اینکه» و بند اسمی | مالکیت، اعداد | تولید شد | ۲۵ |
| ۸ | کشاورزی شهری و مزارع عمودی | جفت‌های معنایی نزدیک + ضمیر بازگشتی | جانشینی، صفت مرکب | تولید شد | ۲۵ |
| ۹ | Task 2: خودروی خودران و مسئولیت | وزن جمله، extraposition، فاعل صوری، hedging | backshift، التزامی | تولید شد | ۲۵ |
| ۱۰ | جمع‌بندی: جنگل، کاغذ و بازیافت | مرور هفت ردیف عقب‌ماندهٔ بالا | وارونگی، سببی، `-ever`، `do` تأکیدی | در انتظار | — |

## وضعیت متن‌ها — دورهٔ ۳ (تمام شد) — `texts/03/drill.html` (`STORE_KEY = "ielts-d3"`)

نقشهٔ دورهٔ ۳ در ۲۰۲۶-۰۹-۱۸ تأیید شد. **اصل نقشه با دو دورهٔ قبل فرق دارد.** تا پایان دورهٔ ۲ هیچ ردیفی زیر هدف نماند — کل لایهٔ ۳ هم رد شد — پس نقشه دیگر بر اساس «چه چیزی کم آمده» چیده نشده. ستون هر متن یک **خانوادهٔ تله** است: همان تله‌هایی که در یادداشت‌های دو دورهٔ قبل بارها «ارزش برگشتن» خوردند و هیچ‌وقت ردیف مستقل نداشتند. ردیف‌های تازه‌شان در بخش «شمارش پوشش» باز شده.

| # | موضوع | ستون (خانوادهٔ تله) | لایهٔ ۳ | وضعیت | تعداد واحد |
|---|---|---|---|---|---|
| ۱ | کار داوطلبانه | فعل مرکب فارسی در برابر فعل تک‌کلمه‌ای انگلیسی | سببی، مالکیت | تولید شد | ۲۵ |
| ۲ | انرژی تجدیدپذیر و شبکهٔ برق | جفت‌های معنایی نزدیک (`raise/rise`، `affect/effect`، `economic/economical`) | اعداد، صفت مرکب | تولید شد | ۲۵ |
| ۳ | حمل‌ونقل شهری و دوچرخه | یک صورت فارسی، چند جواب («تا»، «که»، «هم»، «شدن»، «ی») | `-ever`، جانشینی | تولید شد | ۲۵ |
| ۴ | گوشی و عادت‌های روزمره | جای ذره و مفعول در افعال عبارتی + `to`ی حرف اضافه‌ای | نقل قول/backshift | تولید شد | ۲۵ |
| ۵ | هفتهٔ کاری چهارروزه | اسم‌سازی: فارسیِ فعل‌محور در برابر انگلیسیِ اسم‌محور | سببی، التزامی | تولید شد | ۲۵ |
| ۶ | سیل و آمادگی شهرها | وزن و ترتیب جمله، extraposition، فاعل صوری | وارونگی | تولید شد | ۲۵ |
| ۷ | امتحانات و ارزیابی | حذف و جانشینی در برابر تکرار فارسی | `do` تأکیدی، مالکیت | تولید شد | ۲۵ |
| ۸ | زبان نوشتاری در عصر پیام‌رسان | ثبت (register) گفتاری در برابر نوشتاری | `-ever`، نقطه‌گذاری | تولید شد | ۲۵ |
| ۹ | Task 2: ممنوعیت تبلیغ غذای ناسالم برای کودکان | نردبان الزام و hedging در استدلال | التزامی، `do` تأکیدی | تولید شد | ۲۵ |
| ۱۰ | جمع‌بندی: کتابخانه‌های عمومی و آیندهٔ کتاب | مرور هر نُه خانواده | وارونگی، سببی، `-ever` | تولید شد | ۲۵ |

## وضعیت متن‌ها — دورهٔ ۲ (تمام شد) — `texts/02/drill.html`

نقشهٔ دورهٔ ۲ در ۲۰۲۶-۰۹-۱۷ تأیید شد. اصل نقشه: نکته‌های لایهٔ ۳ که در دورهٔ ۱ به متن‌های آخر افتادند، از متن ۱ پخش می‌شوند (دو نکتهٔ لایهٔ ۳ در هر متن). نکته‌هایی که خیلی از هدف جلوترند (حرف تعریف، مجهول، gerund، اعداد، مقایسه) ستون هیچ متنی نیستند.

| # | موضوع | ستون | لایهٔ ۳ | وضعیت | تعداد واحد |
|---|---|---|---|---|---|
| ۱ | کم‌آبی و کشاورزی | حال ساده / استمراری، روند، افعال حالت | `do` تأکیدی، صفت مرکب | تولید شد | ۲۵ |
| ۲ | گردشگری انبوه | `at / on / in` زمان و مکان، حرکت در برابر سکون | سببی، `-ever` | تولید شد | ۲۴ |
| ۳ | پیری جمعیت و بازنشستگی | زبان روند و نمودار (Task 1)، فاعل صوری | وارونگی، التزامی | تولید شد | ۲۵ |
| ۴ | مرگ زبان‌ها | ماضی بعید، منفی دوگانه، تلهٔ `until` | backshift، وارونگی | تولید شد | ۲۴ |
| ۵ | هوش مصنوعی و بازار کار | modal آینده و hedging، شرطی mixed | التزامی، `do` تأکیدی | تولید شد | ۲۴ |
| ۶ | بحران مسکن | موصولی با حرف اضافه (`in which`، `whose`)، `at / on / in` | سببی، مالکیت | تولید شد | ۲۴ |
| ۷ | ورزش کودکان و رقابت | «حرف اضافه + اینکه»، بند اسمی، الگوهای فعل | `-ever`، comma splice | تولید شد | ۲۵ |
| ۸ | اکتشاف فضا و بودجهٔ علم | زمان‌های آینده، افعال عبارتی، حروف اضافهٔ دیگر | وارونگی، جانشینی | تولید شد | ۲۴ |
| ۹ | Task 2: ورود رایگان به موزه‌ها | رابط‌های تقابل، ارجاع، `this` + اسم | `do` تأکیدی، التزامی | تولید شد | ۲۵ |
| ۱۰ | جمع‌بندی (پسماند غذا) | مرور ترکیبی عقب‌مانده‌ها | وارونگی، `-ever`، `do` تأکیدی | تولید شد | ۲۵ |

## وضعیت متن‌ها — دورهٔ ۱ (تمام شد) — `texts/01/drill.html`

| # | موضوع | وضعیت | تعداد واحد |
|---|---|---|---|
| ۱ | فناوری و جامعه | تولید شد | ۲۴ |
| ۲ | آموزش و دانشگاه | تولید شد | ۲۴ |
| ۳ | کار و اقتصاد | تولید شد | ۲۳ |
| ۴ | شهر و محیط زیست | تولید شد | ۲۳ |
| ۵ | سلامت و سبک زندگی | تولید شد | ۲۴ |
| ۶ | سفر و مهاجرت | تولید شد | ۲۴ |
| ۷ | رسانه و اطلاعات | تولید شد | ۲۴ |
| ۸ | تاریخ و میراث | تولید شد | ۲۴ |
| ۹ | بحث و استدلال | تولید شد | ۲۴ |
| ۱۰ | جمع‌بندی فشرده (زنبورها و غذای ما) | تولید شد | ۲۵ |

---

## شمارش پوشش

عدد هر نکته = تعداد واحدهایی که تا اینجا آن را تمرین کرده‌اند. شمارش‌ها بین دوره‌ها روی هم جمع می‌شوند.

### لایهٔ ۱ — هدف: ۲۰+ بار

| نکته | شمارش |
|---|---|
| حال کامل / گذشتهٔ ساده | ۱۶۶ |
| حال ساده / استمراری | ۱۰۶ |
| حرف تعریف (the / a / صفر) | ۲۷۹ |
| شمارش‌پذیری و مطابقت | ۲۷۰ |
| بند موصولی | ۱۹۷ |
| مجهول | ۲۲۲ |
| at / on / in | ۱۴۷ |
| gerund / infinitive | ۲۱۵ |

### لایهٔ ۲ — هدف: ۶ تا ۱۰ بار

| نکته | شمارش |
|---|---|
| شرطی و نردبان فاصله | ۵۲ |
| modal و hedging | ۹۶ |
| مقایسه | ۹۸ |
| ارجاع و ضمیر | ۱۳۷ |
| although / despite / however | ۶۴ |
| بند اسمی | ۱۴۸ |
| فاعل صوری there / it | ۶۸ |
| جای قید | ۱۸۸ |

### لایهٔ ۳ — هدف: ۲ تا ۴ بار

| نکته | شمارش |
|---|---|
| سببی | ۲۵ |
| نقل قول و backshift | ۶۰ |
| وارونگی | ۳۰ |
| جانشینی و حذف | ۹۸ |
| وجه التزامی | ۳۳ |
| do تأکیدی | ۲۴ |
| اعداد و درصد | ۶۲ |
| صفت مرکب | ۵۰ |
| مالکیت و آپاستروف | ۷۲ |
| -ever / no matter | ۲۹ |
| افعال عبارتی | ۱۲۶ |
| نقطه‌گذاری و comma splice | ۱۲۵ |

### ردیف‌های تازه — از دورهٔ ۲، هدف: ۶ تا ۱۰ بار در هر دوره

این سه الگو در دورهٔ ۱ بارها آمدند ولی ردیف نداشتند و شمرده نشدند (ماضی بعید حدود ۲۰ واحد، منفی دوگانه حدود ۱۰). شمارش از دورهٔ ۲ شروع می‌شود.

| نکته | شمارش |
|---|---|
| ماضی بعید (ساده و استمراری) | ۶۵ |
| منفی دوگانهٔ فارسی | ۱۰۱ |
| «حرف اضافه + اینکه» | ۴۱ |
| شکل‌های آینده و بند زمان | ۴۷ |

### ردیف‌های تازه — از دورهٔ ۳، هدف: ۶ تا ۱۰ بار در هر دوره

اینها خانواده‌های تله‌اند، نه نکته‌های گرامری مستقل. هر کدام ستون یکی از متن‌های دورهٔ ۳ است و در دو دورهٔ قبل بارها بی‌ردیف از کنارشان رد شدیم.

| نکته | شمارش |
|---|---|
| فعل مرکب فارسی و حرف اضافهٔ اضافی | ۸۱ |
| ضمیر بازگشتی فارسی (موصولی و بند اسمی) | ۵۵ |
| یک صورت فارسی، چند جواب انگلیسی | ۷۳ |
| جفت‌های معنایی نزدیک | ۶۸ |
| جای ذره و `to`ی حرف اضافه‌ای | ۴۱ |
| اسم‌سازی در برابر فعل | ۴۰ |
| وزن و ترتیب جمله | ۵۳ |
| ثبت (register) | ۴۴ |

### ردیف تازه — از دورهٔ ۴، هدف: ۶ تا ۱۰ بار در هر دوره

ستون متن ۵ یک خانوادهٔ ساختاری است که دو عضوش (`do` تأکیدی و وارونگی) ردیف داشتند و دو عضو دیگرش نداشتند. این ردیف برای همان دو عضو باز شد: جملهٔ شکافته در هر سه شکلش و پیش‌آوریِ بدون وارونگی. شمارش از دورهٔ ۴ شروع می‌شود.

| نکته | شمارش |
|---|---|
| جملهٔ شکافته و پیش‌آوری | ۱۳ |

---

## یادداشت‌های جلسه

بعد از هر متن، اگر چیزی پیش آمد که جلسهٔ بعد باید بداند، همین‌جا بنویس — مثلاً موضوعی که سخت‌تر از انتظار بود، نکته‌ای که باید زودتر برگردد، یا تصمیمی که ترتیب متن‌ها را عوض کرد.

- **متن ۱ (۲۴ واحد):** فایل `texts/01/drill.html` از قالب ساخته شد (`STORE_KEY = "ielts-d1"`). مسیر پوشه در سرتیتر این دفتر از `Grammar/01/` به `texts/01/` اصلاح شد. شمارش‌ها هر واحدی را حساب کرده‌اند که آن نکته در بخش «متغیر» یا «تله»اش واقعاً توضیح داده شده، نه فقط در `alt` یا `rule`.
- **افعال عبارتی در متن ۱ فقط در `alt` آمدند** (`break out`، `come about`، `go back`) و شمرده نشدند؛ متن ۲ دو واحد اضافه کرد (`go on to`، `point out`). متن ۳ هم فقط در `alt` داشت (`open up`، `call for`، `come under`). **متن ۴ ستونش است؛ دست‌کم ۴ واحد لازم است.**
- **تله‌هایی که خوب جواب دادند و ارزش برگشتن دارند:** `until` + فعل منفی فارسی (متن ۱، واحد ۲۲)، «از … خواسته شد» ← `were asked to` (متن ۱، واحد ۱۰)، «عادت کرده‌اند که … کنند» ← `used to hold` (متن ۱، واحد ۱۶)، حذف مقایسه‌ای فارسی ← `that of / those in` (متن ۳، واحدهای ۸ و ۱۱)، «تا» چهارکاره (متن ۳، واحدهای ۱۶ و ۲۱).
- **شیوهٔ املایی بریتانیایی** انتخاب شد (`at weekends`، `centre`، `£`، `per cent`، گیومهٔ تکی). در متن‌های بعدی ثابت نگه دار.
- **متن ۲ (۲۴ واحد):** ستون‌ها پوشش داده شدند: نهاد به‌عنوان کارکرد (واحدهای ۱، ۲، ۱۵)، منبع ۱ و ۳ «قابل تشخیص بودن» (۴، ۹، ۱۹)، و تقابل defining/non-defining (۶ در برابر ۱۰، ۱۶ در برابر ۱۷، ۲۰).
- **متن ۳ (۲۳ واحد):** موضوع اشتغال زنان و شکاف دستمزد (با آمار واقعی بریتانیا: اشتغال زنان ~۵۳٪ در ۱۹۷۱ و ~۷۲٪ امروز، شکاف تمام‌وقت ۲۰۲۳ ~۷.۷٪). یادداشت جلسهٔ قبل اجرا شد: **۶ مجهول واقعی** (واحدهای ۹، ۱۳، ۱۸، ۲۰، ۲۱، ۲۳) و بند اسمی `what` (واحد ۱۴)، cleft `it is … that` (واحد ۱۲)، التزامی (۲۰) و `whichever` (۲۳) هم اضافه شدند.
- **اعداد و درصد (۷) و مقایسه (۱۲) از هدف گذشته‌اند.** در متن‌های ۴ تا ۹ عمداً سراغشان نرو مگر در `alt`؛ متن ۱۰ فقط یک واحد مرور.
- **عقب‌افتاده‌های لایهٔ ۱:** `at / on / in` فقط ۱۰ (متن ۴ ستونش است — دست‌کم ۶ واحد)؛ حال ساده / استمراری ۱۳ (متن ۵ جای طبیعی‌اش است: عادت‌ها، وضعیت موقت، state verbs).
- **لایهٔ ۳ هنوز صفر:** سببی، نقل قول و backshift، وارونگی، `do` تأکیدی. نقل قول ستون متن ۶ و سببی ستون متن ۸ است؛ وارونگی و `do` تأکیدی را از همین حالا در متن‌های ۵ یا ۶ یک بار بیاور تا همه‌اش به متن ۱۰ نیفتد. `although / despite / however` (۴) و شرطی (۳) هم برای متن ۶ و ۹.
- **متن ۴ (۲۳ واحد):** شهر بی‌ماشین — «تقاضای القایی»، پونته‌ودرا (۱۹۹۹) و پاریس. ستون‌ها: `at / on / in` در ۶ واحد (۱، ۲، ۶، ۷، ۲۰، ۲۱)، جای قید در ۶ واحد (قید تکرار، زنجیرهٔ چندفعلی، `every day` آخر جمله، جدا نکردن فعل و مفعول)، افعال عبارتی ۴ واحد (`fill up`، `give way to`، `close down`، `move back into`). حروف اضافهٔ غیر از at/on/in هم پررنگ بودند و در ردیف at/on/in **شمرده نشدند**: «با» ← `by` / `with` (۳، ۶)، `ban from` / `close to` (۸، ۱۸)، `through` / `past` / `across` (۱۲)، `within walking distance of` (۱۶).
- **ماضی بعید (ساده و استمراری) در واحدهای ۱۲، ۱۳، ۱۴ آمد** ولی ردیفی در جدول ندارد و شمرده نشد؛ متن ۸ ستونش است و از صفر شروع نمی‌کند.
- **backshift اولین بار آمد** (واحد ۱۰، `were convinced … would`) — متن ۶ دست‌کم ۲ واحد دیگر. وارونگی فقط در توضیح و قاعدهٔ واحد ۲ (`Rarely do they…`) آمد، شمرده نشد. `do` تأکیدی و سببی هنوز صفر.
- **لایهٔ ۱ همه به ۱۶+ رسیده‌اند.** `at / on / in` و حال ساده / استمراری (۱۶) عقب‌تر از بقیه‌اند؛ متن ۵ برای حال ساده / استمراری جای طبیعی است. شرطی (۳) هنوز کمترین نکتهٔ لایهٔ ۲ است و باید در متن ۶ سنگین بیاید.
- **متن ۵ (۲۴ واحد):** کم‌خوابی — عادت شبانه با گوشی، تصادف اتوبوس، شروع دیرتر مدرسه‌ها، توصیه‌های فردی. ستون‌ها: نردبان الزام (۱۱ `have to` قانونی، ۱۴ `should not` در برابر `mustn't`)، نردبان قطعیت و modal گذشته (۶ `more likely to`، ۱۰ `must have` در برابر `had to` / `should have`، ۱۹ `may have made`)، hedging (۵ `suggests`، ۱۸ `not necessarily`، ۲۳ `may sound … but`)، الگوهای فعل (۳ و ۴ `get used to -ing` با توازی، ۱۳ `remember -ing`، ۱۵ `force O to`، ۲۰ `recommend that` + `stop -ing`، ۲۲ `prevent O from -ing` و `make O V1`).
- **لایهٔ ۳ که بالاخره آمد:** وارونگی `Not only does` (واحد ۹)، `do` تأکیدی (۲۳)، backshift `can ← could` (۱۳)، التزامی بعد از `recommend that` (۲۰). **سببی هنوز صفر است** — متن ۸ ستونش است، دست‌کم ۳ واحد.
- **modal و hedging (۱۷) از هدف گذشت.** در متن‌های ۶ تا ۹ فقط وقتی خودش بیاید؛ عمداً سراغش نرو. gerund/infinitive (۲۸) و حرف تعریف (۳۷) هم راحت‌اند.
- **شرطی (۴) هنوز عقب‌ترین نکتهٔ لایهٔ ۲ است** و `although / despite / however` (۵) هم در این متن اضافه نشد. متن ۶ دست‌کم ۴ واحد شرطی (هر سه پلهٔ نردبان + mixed) و متن ۹ ستون ربط‌دهنده‌ها. مجهول در این متن هیچ واحدی نگرفت (۲۴) — متن ۶ یا ۸ جبران کند.
- **ماضی بعید** دوباره در واحد ۱۲ آمد (`had not stopped … since leaving`) و شمرده نشد. **تلهٔ فعل سبک** (`make changes` نه `give changes`، واحد ۱۹) خوب جواب داد و ردیفی ندارد؛ ارزش برگشتن دارد.
- **متن ۶ (۲۴ واحد):** مهاجرت دیروز و امروز — نامه و تماس تصویری، مصاحبه‌های یک تحقیق بریتانیایی (زن سوری، مرد لهستانی)، شوک برگشت به وطن (مرد ایتالیایی)، سیاست کلاس زبان. ستون‌ها: **نردبان فاصله در ۷ واحد** — پلهٔ ۲ هر دو ستون (۱۱، ۱۲)، mixed نوع ۲ (۱۳)، `as if` + ماضی بعید (۱۹)، پلهٔ ۱ هر دو ستون (۲۱، ۲۲)، پلهٔ ۰ با `as long as` (۲۳). **backshift در ۶ واحد** (۸، ۹، ۱۰، ۱۱، ۱۳، ۱۸) با `say`/`tell` (۹) و `that` دوم در نقل قول دوبخشی (۱۰). `despite` + اسم (۶).
- **`used to` / `would` / `be used to` / `get used to` در ۸ واحد** (۱، ۲، ۳، ۴، ۵، ۹، ۱۵، ۱۶) — ردیفی در جدول ندارد؛ فقط `be/get used to + -ing` در ردیف gerund شمرده شد. تقابل‌های مهم: ماضی استمراری فارسی ← گذشتهٔ ساده (۱)، `would` ممنوع با فعل حالتی (۴)، حالت در برابر فرآیند (۱۵ در برابر ۱۶).
- **تله‌های ارزش برگشتن:** «اگر … بود» یک شکل برای پلهٔ ۱ و ۲ (۱۱ در برابر ۲۱، عمداً در یک متن)، «برگشته بود» در بند اصلی شرطی ← `would have` نه `had` (۱۳)، «تا وقتی» ← `as long as` نه `until` (۲۳)، «همان مشکلی که» ← `the same` نه `a` (۲۴)، «بیست سال زندگی در» ← `after living … for` (۱۷).
- **وارونگی شرطی** (`Had his English been…`، `Were these classes…`) فقط در `alt` واحدهای ۱۱ و ۲۱ آمد و شمرده نشد؛ وارونگی (۱) برای متن ۱۰ می‌ماند. **سببی هنوز صفر** — متن ۸ دست‌کم ۳ واحد. **شرطی (۱۱) و backshift (۸) از هدف گذشتند**؛ در متن‌های ۷ تا ۹ عمداً سراغشان نرو. `although / despite / however` (۶) هنوز فقط `despite` را دیده؛ متن ۹ باید `although` و `however` و comma splice را سنگین بیاورد.

- **متن ۷ (۲۴ واحد):** رسانه و اطلاعات — تعطیلی روزنامهٔ محلی، جابه‌جایی خبر به شبکه‌های اجتماعی، عکس جعلی سیل، سازمان‌های راستی‌آزمایی. ستون‌ها: **ارجاع و ضمیر در ۱۱ واحد** (۴، ۵، ۶، ۸، ۱۱، ۱۲، ۱۳، ۱۹، ۲۰، ۲۲، ۲۴) و **مالکیت در ۵ واحد** (۳ `the paper's editor`، ۴ `its reporters`، ۱۰ `companies'` جمع، ۱۵ `of` برای بی‌جان، ۲۰ `their`).
- **`this` + اسم جمع‌بندی‌کننده در ۵ واحد** (۶ `this closure`، ۱۲ `this claim`، ۱۹ `that incident`، ۲۲ `this approach`، ۲۴ `this cycle`) — در همه‌شان فارسی فقط «این/آن» خالی دارد و اسم را خودش باید بسازد. **ابهام مرجع** در واحد ۱۳ (تکرار اسم به‌جای `they`) و تلهٔ «می‌گویند» ← `It is said` در واحد ۱۱.
- **لایهٔ ۳ که این متن اضافه کرد:** `-ever`/`no matter` دو واحد (۱۴ `However quickly` بدون کاما، ۲۳ `No matter how many times`) و صفت مرکب یک واحد (۱۹ `fact-checking`). **سببی، وارونگی، `do` تأکیدی و التزامی هنوز دست‌نخورده‌اند** — سببی ستون متن ۸ است، سه‌تای دیگر تماماً به متن ۱۰ می‌افتند مگر متن ۹ یکی‌شان را بردارد.
- **ماضی بعید در ۴ واحد آمد** (۳، ۱۶، ۱۷، ۱۸) و باز ردیفی ندارد و شمرده نشد؛ متن ۸ ستونش است و عملاً از ۱۰+ شروع می‌کند. تلهٔ `ago` ← `earlier` داخل قاب گذشته (واحد ۱۶) تازه بود و ارزش برگشتن دارد.
- **«تا وقتی» عمداً دو بار و با دو جواب متفاوت آمد:** واحد ۱۸ ← `by the time` و واحد ۲۴ ← `unless`. همین شیوه (یک صورت فارسی، دو جواب، در یک متن) در متن ۶ هم برای «اگر … بود» جواب داده بود.
- **تله‌های ارزش برگشتن:** فعل سبک «زحمت کشیدن» ← `take the trouble to` (واحد ۱۷)، بند پرسشی درون جمله بدون وارونگی (۲۰ و ۲۲)، `little` در برابر `a little` (۱۲)، `a few` در برابر `few` (۶)، `for themselves` در برابر `by themselves` (۲۴).
- **حرف تعریف (۵۰) و gerund/infinitive (۳۸) خیلی از هدف جلوترند** و خودشان می‌آیند؛ سراغشان نرو. `at / on / in` (۲۶) بالاخره به بقیهٔ لایهٔ ۱ رسید. `although / despite / however` (۶) هنوز عقب‌ترین نکتهٔ لایهٔ ۲ است و `however`ِ ربطی و comma splice **ستون متن ۹**اند — دست‌کم ۵ واحد.

- **متن ۸ (۲۴ واحد):** تاریخچهٔ یک کارخانهٔ نخ‌ریسی — ساخت در دههٔ ۱۸۴۰، تعطیلی ۱۹۷۸، بیست سال رهاشدگی، بازسازی به آپارتمان و موزه، و بحث میراث. متن با `had been producing` (واحد ۲) شروع و با `will have been standing` (واحد ۲۴) تمام می‌شود؛ قاعدهٔ واحد آخر «نردبان کامل استمراری» را در سه نقطهٔ مرجع جمع می‌کند.
- **سببی بالاخره آمد — ۴ واحد:** سببیِ ناخوشایند با نهادِ موضوعِ فارسی (۸ `had his windows broken`)، دو `had` پشت‌سرهم (۹)، «داد ... بررسی کنند» ← `have` نه `give` (۱۲)، سببی در حال استمراری و `have` کنشی در برابر مالکیت (۱۷). به هدف رسید؛ در متن ۱۰ فقط اگر خودش بیاید.
- **مجهول در ۱۳ واحد و تقریباً همهٔ زمان‌ها:** گذشتهٔ ساده (۱، ۴)، ماضی بعید (۷، ۱۱، ۱۳، ۱۶)، حال استمراری (۱۵)، حال کامل (۱۶، ۱۸)، gerund مجهول (۱۸ `without being recorded`)، غیرشخصی هر دو الگو (۱۹، ۲۰)، modal (۲۰)، حرف‌اضافه‌ای (۲۲ `paid for`)، حال سادهٔ مجهول در بند -ever (۲۴). تلهٔ مرکزی سوم‌شخص جمعِ بی‌فاعل فارسی بود («ساختند»، «نفرستاده بودند») و تلهٔ برعکسش — مجهولِ اضافی از «شدن» با فعل ناگذر (۱۹ `open`، ۲۱ `fall down`).
- **ماضی بعید (ساده و استمراری) در ۸ واحد** (۲، ۳، ۴، ۷، ۹، ۱۱، ۱۳، ۱۶) — همچنان ردیفی ندارد و شمرده نشد. آیندهٔ کامل استمراری (۲۴) هم اولین بار آمد.
- **لایهٔ ۳ که این متن اضافه کرد:** وارونگی `Rarely has` (۲۲)، `do` تأکیدی در ردِ ادعای قبلی (۲۱)، `whatever` بدون `will` (۲۴)، کمکیِ مشترک برای دو V3 (۱۱، شمرده در جانشینی و حذف). **وجه التزامی (۲) تنها نکتهٔ لایهٔ ۳ است که از این دوره چیزی نگرفت** و صفت مرکب (۳) و اعداد (۷) هم دست نخوردند — متن ۱۰ باید التزامی را دست‌کم یک بار دیگر بیاورد. backshift در واحد ۱۳ فقط در توضیح آمد و شمرده نشد.
- **منفیِ دوگانهٔ فارسی** در سه واحد تمرین شد (۶ `nothing was done`، ۷ `no one had been sent` + ممنوعیت `anyone` به‌عنوان نهاد منفی، ۱۸) و ردیفی ندارد؛ تلهٔ خیلی پرتکراری است و ارزش برگشتن دارد. **«تا وقتی» باز دو جواب گرفت:** ۲ ← `by the time` در گذشته و ۲۴ ← `by the time` در آینده.
- **تله‌های ارزش برگشتن:** «دادن» سببی ← `give` (۱۲، ۱۷)، `damage` / `damages` (۹)، `ironwork` / `ironworks` (۱۱)، «را باید ... کرد» ← modal + `be` + V3 (۲۰)، `what matters is` + گزارهٔ جمع (۲۳)، `as` نقش در برابر `like` شباهت (۱۵).
- **`although / despite / however` (۷) هنوز عقب‌ترین نکتهٔ لایهٔ ۲ است.** واحد ۱۳ فقط `although` در برابر `despite` و `although … but` را گرفت؛ `however`ِ ربطی، `the fact that` و comma splice کاملاً برای متن ۹ مانده‌اند — دست‌کم ۵ واحد. مجهول (۴۷) و حرف تعریف (۵۵) خیلی از هدف جلوترند.

- **متن ۹ (۲۴ واحد):** هفتهٔ کاری چهارروزه به سبک Task 2 — معرفی دو دیدگاه، آزمایش ۲۰۲۲ بریتانیا (حدود ۶۱ شرکت، شش ماه، بیشترشان ادامه دادند)، محدودیت‌ها (بیمارستان، حجم کار)، نتیجه‌گیری. ستون‌ها: **رابط‌های تقابل در ۸ واحد** — `whereas` (۳)، `although` در برابر `despite` + جمله (۷)، `although … but` دونشانه‌ای (۱۰)، `despite` + گروه اسمی با بند موصولی پنهان (۱۶)، `even though` در برابر `even if` (۲۱)؛ **comma splice در ۴ واحد** با چهار رابط متفاوت: `however` (۴)، `as a result` (۱۳)، `otherwise` (۲۰، با ویرگولِ عمدی آخر فارسیِ واحد ۱۹) و ویرگول بین نهاد و فعل (۸).
- **الگوی «حرف اضافهٔ فارسی + اینکه» ستون پنهان متن شد** و در شش واحد با شش جواب آمد: `after` بدون `that` (۶)، `although` (۷)، `the fact that` (۸)، `because` نه `because of` (۱۲)، `instead of -ing` (۱۸)، و برعکسش `now that` که `that` جزئش است (۲۳). ردیف جداگانه ندارد؛ در `although/despite`، بند اسمی و gerund شمرده شد. ارزش برگشتن دارد.
- **بند اسمی در ۴ واحد:** `the fact that` نهاد (۸)، `whether` نهاد + سؤال درون‌نشانده بعد از `depend on` (۱۷)، سؤال درون‌نشانده بعد از `-ing` (۱۸)، `whether` بعد از `ask` (۲۳). **صفت مرکب** سه بار (۱ `four-day`، ۵ `six-month`، ۱۱ `office-based`) و به هدف رسید. التزامی یک واحد (۱۹). `get everything done` (۱۴) در سببی شمرده شد.
- **منفی دوگانهٔ فارسی باز سه واحد گرفت** (۴ `neither … tells`، ۹ `achieve nothing`، ۱۵ `unless` + فعل مثبت) و هنوز ردیف ندارد. توازی `not because … but because` (۲۲) و `no longer whether … but whether` (۲۴) هم تازه بودند.
- **برای متن ۱۰:** **وارونگی (۲) و `do` تأکیدی (۲)** تنها نکته‌های لایهٔ ۳اند که در کف هدف مانده‌اند — `do` تأکیدی در این متن فقط در `alt` واحد ۲۱ آمد و شمرده نشد. هر دو دست‌کم ۲ واحد. التزامی (۳) یک واحد دیگر. شرطی و modal در این متن هیچ واحدی نگرفتند ولی از هدف گذشته‌اند؛ متن ۱۰ فقط یک واحد مرور هر کدام. جانشینی و حذف (۷) در `alt` واحد ۲۴ (`afford not to`) آمد و شمرده نشد.

- **متن ۱۰ (۲۵ واحد) — دورهٔ ۱ تمام شد.** زنبورها و گرده‌افشانی: وابستگی محصولات (بیش از سه‌چهارم، IPBES)، کاهش کلنی‌ها و گزارش‌های ۲۰۰۶ آمریکا، زیستگاه و سموم (محدودیت ۲۰۱۳ و ممنوعیت ۲۰۱۸ اتحادیهٔ اروپا)، گرده‌افشانی دستی در سیچوان، و اینکه زنبور عسل راه‌حل کافی نیست. عنوان در فایل «متن ۱۰ — زنبورها و غذای ما» است.
- **لایهٔ ۳ عقب‌مانده‌ها به هدف رسیدند:** وارونگی ۲ واحد (۷ `Not until … did anyone`، ۲۳ `only by … can we`)، `do` تأکیدی ۲ واحد (۱۸ `This does help`، ۲۵ `do cost money`)، التزامی ۲ واحد (۱۱ `proposed that … be restricted` بدون backshift، ۲۴ `It is essential that any farmer … receive` در کنار `who sets` در همان نهاد). جانشینی و حذف ۶ واحد با شش ابزار: `so have` (۵)، کمیت‌نمای بی‌اسم `several` (۸)، `the most significant of these` (۹)، `the same` (۱۶)، `ones` بعد از صفت (۱۹)، `do so` (۲۴).
- **`until` + فعل منفی فارسی** این بار دوواحدی آمد: منفی از بند `until` حذف (۶) و منفی دوگانه بعد از وارونگی ← `anyone` (۷). منفی دوگانه در ۲۲ (`None of this means`) و ۲۵ (`doing nothing`) هم تکرار شد — در کل دوره حدود ده واحد گرفته و هنوز ردیف ندارد.
- **در این متن صفر ماندند:** `although / despite / however` (۱۵)، فاعل صوری (۱۸)، سببی (۵)، `-ever` (۴). همه به هدف رسیده‌اند.
- **برای نقشهٔ دورهٔ ۲** (جلسهٔ بعد باید قبل از متن اول تأیید بگیرد): از روی شمارش‌ها، کمترین فاصله تا هدف را لایهٔ ۳ دارد که تازه به کف رسیده (وارونگی ۴، `do` تأکیدی ۴، `-ever` ۴، سببی ۵، التزامی ۵) — دورهٔ ۲ نباید بگذارد همه‌شان به متن آخر بیفتند. لایهٔ ۲ همه از ۱۵ گذشته‌اند. لایهٔ ۱ همه از ۳۲ گذشته‌اند؛ `at / on / in` (۳۳) و حال ساده / استمراری (۳۲) عقب‌ترند. سه الگوی بی‌ردیف که در کل دوره بارها برگشتند و ارزش ردیف رسمی دارند: **ماضی بعید**، **منفی دوگانهٔ فارسی**، و **«حرف اضافه + اینکه»**. تلهٔ «می‌توانست» فرضی ← `could` نه `was able to` (۱۵) و مطابقت دوطبقهٔ `one of the … that are … is` (۱۷) هم تازه بودند.

### دورهٔ ۲

- **متن ۱ (۲۵ واحد):** کم‌آبی و کشاورزی — سهم ۷۰ درصدی کشاورزی (FAO)، فرونشست زمین در ایران، دریاچهٔ ارومیه، پسته و برنج، جو به‌جای گندم، آبیاری قطره‌ای و اثر بازگشتی (صرفه‌جویی صرف زمین بیشتر می‌شود)، قیمت آب. فایل `texts/02/drill.html` از قالب ساخته شد.
- **ستون حال ساده / استمراری در ۱۰ واحد:** روند با «رو به … است» (۲)، مضارع فارسیِ بی‌نشانه برای روند (۴، ۵)، فعل حالت با نشانهٔ «این روزها» (۷)، `need` (۸)، عادت در برابر استثنا با یک فعل فارسی تکراری (۱۳)، برنامهٔ موقت (۱۶)، به‌علاوهٔ واقعیت پایدار (۱، ۳) و `as long as` + حال (۲۱).
- **لایهٔ ۳:** `do` تأکیدی یک واحد (۱۸ `do make sense` با امتیاز دادن و `but`)، صفت مرکب دو واحد (۱۶ `five-year programmes` با اسم جمع، ۱۷ `low-cost`). وارونگی فقط در `alt` واحد ۱۰ (`Nor is it`) آمد و شمرده نشد.
- **منفی دوگانه در ۳ واحد و با سه ابزار:** `little` (۲۱)، `unless` + فعل مثبت (۲۴)، `no longer … anyone` (۲۵). تلهٔ «هم» در جملهٔ منفی ← `either` (۱۰) هم تازه بود و ارزش برگشتن دارد.
- **«تا» باز سه بار:** `take … to` (۱۱)، هدف `to help` (۱۷)، بازهٔ عددی `30 to 50` (۱۵)؛ و «تا وقتی» ← `as long as` (۲۱) — سومین جواب این صورت فارسی بعد از `by the time` و `unless` در دورهٔ ۱.
- **تله‌های تازه‌ای که جواب دادند:** «ی» موصولی در برابر «ی» نکره (۱ و ۱۹)، «شدن» ناگذر ← `fall` / `rise` نه مجهول (۲۰)، `by` در برابر `to` برای مقدار تغییر (۵، ۱۵)، `Lake Urmia` بدون `the` (۶)، `few` در برابر `a few` در برابر «کمتر کسی» (۱۲)، `also` / `too` / `either` (۸، ۱۰).
- **برای متن ۲ (گردشگری انبوه):** ستون `at / on / in` است — دست‌کم ۶ واحد، با حرکت در برابر سکون. سببی و `-ever` هر کدام دست‌کم یک واحد. ماضی بعید و «حرف اضافه + اینکه» هنوز صفرند؛ اگر طبیعی جا بیفتند، یکی بیاور.

- **متن ۲ (۲۴ واحد):** گردشگری انبوه — سرازیر شدن به شهرهای جنوب اروپا، کشتی‌های کروز ونیز تا ۲۰۲۱، کاهش بیش از نصفِ جمعیت مرکز تاریخی ونیز، نانوایی‌ای که سوغاتی‌فروشی شد، اعتراض‌ها در بارسلون و آمستردام، کارزار «نیایید» آمستردام (۲۰۲۳)، ورودیهٔ ونیز (از بهار ۲۰۲۴)، لغو مجوز آپارتمان‌های گردشگری بارسلون تا ۲۰۲۸، و پخش کردن گردشگرها در طول سال.
- **ستون `at / on / in` در ۸ واحد:** `in southern Europe` در برابر کسرهٔ «of» (۱)، `in a single day` (۲)، `on land` (۴)، `at peak times` برای عبارت زمانی بی‌حرف‌اضافهٔ فارسی (۶)، `live there` بدون `in` (۸)، `in recent years` (۱۵)، `in the old town` (۱۶)، `on its busiest days` (۱۸). **حرکت در برابر سکون** در ۵ واحد جدا آمد و در ردیف شمرده نشد: `pour into` (۱)، `sail into` (۳)، `walk to` در برابر `at` (۱۱)، `go to` در برابر `on the mainland` (۱۲)، `turn into` (۱۰، ۲۴).
- **«تا» چهار بار با سه جواب:** `until` و `into` در یک واحد (۳)، `to` مکانی (۱۱)، و جفت پشت‌سرهم `by 2028` / `Until then` (۲۰ و ۲۱). «از» هم در واحد ۱۸ دو بار آمد و هیچ‌کدام `from` نبود (`since` و `charge` + دو مفعول). حروف اضافهٔ غیر از at/on/in که پررنگ بودند و در ردیف شمرده نشدند: `by more than half` (۷)، `face` بی‌`with` (۱۴)، `protest against` در برابر `to` (۱۵)، `full of` (۱۶)، `throughout` / `across` در برابر `during` (۲۳)، `visit` بی‌حرف‌اضافه در کنار `live in` در یک بند موصولی (۲۴).
- **لایهٔ ۳:** سببی یک واحد (۱۲، `have a key cut or their shoes repaired` — یک `have`، دو مفعول)، `-ever` یک واحد (۱۶ `wherever`، بی‌`to` و بی‌`will`)، صفت مرکب دو (۵ `one-day`، ۱۰ `long-time`). «برای اینکه» ← `to` در واحد ۱۲ اولین شمارش ردیف «حرف اضافه + اینکه» است.
- **منفی دوگانه در ۴ واحد** با چهار ابزار: `very little` (۵)، `too … to` + `anyone` (۱۹)، `none` + فعل مثبت (۲۲)، `no longer` (۲۴). ماضی بعید باز صفر ماند — جای طبیعی نداشت. **متن ۴ (مرگ زبان‌ها) ستونش است و باید دست‌کم ۶ واحد بیاورد.**
- **تله‌های تازه‌ای که ارزش برگشتن دارند:** ضمیر بازگشتی فارسی در بند موصولی در برابر ضمیر لازم در بند اصلی در یک جمله (۹)، `ask + O + not to` با بند موصولی طولانی وسطش (۱۷)، `discourage from` در برابر `encourage to` (۱۹)، `the number of … is` در برابر `a number of … are` (۲)، `historic` / `historical` (۷)، و `close` ناگذر در برابر مجهول (۱۳).
- **برای متن ۳ (پیری جمعیت):** ستون زبان نمودار و فاعل صوری است. `by` / `to` / `at` در این متن فقط یک بار (۷) آمد و متن ۳ باید سنگین بیاوردش. وارونگی و التزامی هنوز در این دوره صفرند و لایهٔ ۳ متن ۳اند. modal در این متن هیچ واحدی نگرفت (از هدف جلوتر است).

- **متن ۳ (۲۵ واحد):** پیری جمعیت و بازنشستگی — پیش‌بینی سازمان جهانی بهداشت (یک در شش تا ۲۰۳۰، بیش از دو برابر شدن تا ۲۰۵۰)، ژاپن (باروری زیر ۱.۵ از اوایل دههٔ ۹۰، سهم ۶۵+ از حدود ۷٪ در ۱۹۷۰ به حدود ۲۹٪ در ۲۰۲۳، اوج جمعیت ۱۲۸ میلیون در ۲۰۰۸، پیش‌بینی ۸۷ میلیون در ۲۰۷۰، نسبت کارگر به سالمند از ۹ به ۲)، فشار بر مستمری‌ها، افزایش سن مستمری بریتانیا از ۶۶ به ۶۷ (۲۰۲۶ تا ۲۰۲۸)، کارگر ساختمانی، و سهم پنهان سالمندان در اقتصاد.
- **زبان Task 1 در ۱۰ واحد:** `by` / `to` / `at` جدا و کنار هم — `peak at` در برابر `a peak of` (۹)، `increase by … percentage points` در برابر درصد (۸)، `fall to … by 2070` با دو معنای `by` در یک جمله (۱۱)، `fallen to` در برابر `by` (۱۳)، `from … to` کنار `between … and` (۱۷)، `reach` بی‌`to` و بند نتیجهٔ `reaching` (۷)، `account for` بدون استمراری (۶)، `twice the figure for` (۳)، `the proportion of … in` (۱)، `the figure for`. در ردیف‌ها بین at/on/in، اعداد و مقایسه پخش شد.
- **فاعل صوری در ۴ واحد:** `there were` برای وجود با نسبت `for every` (۱۲)، `it is difficult to` با «برای کسی که …» جلوآمده (۱۹)، `it would be a mistake to` + `see X as` (۲۱)، مجهول شخصی `is projected to` در برابر `It is projected that` (۱۱).
- **لایهٔ ۳:** وارونگی `only if … will` (۱۸)، التزامی مجهول `proposed that … be set` (۲۰)، `do` تأکیدی در امتیاز دادن (۲۴)، جانشینی `one that` (۲۵). منفی دوگانه یک واحد (۱۵ «سه راه بیشتر ندارند» ← `only`). **صفت مرکب، سببی، `-ever` و مالکیت در این متن صفر ماندند.** ماضی بعید و «حرف اضافه + اینکه» باز صفر — متن ۴ ستون ماضی بعید است و باید دست‌کم ۶ واحد بیاورد.
- **«تا» باز سه جواب گرفت:** `by 2030` مهلت (۲)، `until that age` ادامه (۱۸)، `to 87 million` مقصد (۱۱) — و ۲ و ۱۸ عمداً در یک متن در برابر هم‌اند.
- **تله‌های تازه‌ای که ارزش برگشتن دارند:** فعل سبک «فشار وارد کردن» ← `put pressure on` (۱۴)، «با + مصدر» ← `by -ing` (۱۶)، التزامی فارسی در بند موصولی شرطی ← حال ساده (۲۵)، «دلیلش» مفرد با دو دلیل (۴)، «امروز» کنار حال کامل (۱۳)، `in their sixties` در برابر `in the sixties` (۲۲)، «این کارها» ← `this work` (۲۳)، `percentage points` در برابر `per cent` (۸).
- **برای متن ۴ (مرگ زبان‌ها):** ستون ماضی بعید، منفی دوگانه و تلهٔ `until`؛ لایهٔ ۳ backshift و وارونگی. `although / despite / however` (۱۶) در دورهٔ ۲ هنوز صفر است — ستون متن ۹ است ولی یکی در متن ۴ بی‌ضرر است.

- **متن ۴ (۲۴ واحد):** مرگ زبان‌ها — حدود ۷۰۰۰ زبان و ۴۰ درصد در خطر، اوبیخی (توفیق اسنچ، مرگ ۱۹۹۲ در ترکیه، کوچ اجداد پس از تصرف قفقاز به دست روسیه، پدربزرگ و مادربزرگی که بزرگش کردند، کتیبهٔ سنگ قبر)، مانکس (آخرین گویندهٔ بومی ۱۹۷۴، ضبط‌ها، فهرست «منقرض» یونسکو در ۲۰۰۹ و تغییرش، مدرسهٔ تمام‌مانکس)، و انتخاب خانواده‌ها. عمداً بی‌عدد و بی‌نام برای مصاحبه‌های والدین (واحد ۲۱) — ادعای کلی است.
- **ماضی بعید بالاخره ردیف گرفت — ۸ واحد:** برگشت به عقب با `earlier` نه `ago` (۶)، `by the time` + `was born` (۷)، دو ماضی بعید در بند اصلی و موصولی (۸)، ماضی بعید استمراری با «دهه‌ها بود که … داشت» (۹)، کتیبه با backshift (۱۱)، `since childhood` در موصولی (۱۳)، بند پرسشی بعد از وارونگی (۱۴)، `Before that` (۱۵). **دو واحد ضدِ ماضی بعید** عمداً کنارشان آمدند و در ردیف حال کامل/گذشته شمرده شدند: ترتیب طبیعی بعد از سه واحد ماضی بعید (۱۰) و ماضی بعید فارسی با تاریخ صریح ← گذشتهٔ ساده (۱۷). این جفت ارزش برگشتن دارد.
- **منفی دوگانه و `until` در ۴ واحد پایانی:** `never` + فعل مثبت (۲۰)، بند `until` بدون نفی (۲۲)، `no language can` + `although` بدون `but` (۲۳)، و `Not until … has it been saved` که هر دو قاعده را با وارونگی جمع می‌کند (۲۴). **وارونگی** دو واحد (۱۴ `Only after`، ۲۴ `Not until`)، **backshift** سه واحد (۱۱ صورت نقلی فارسی «بوده / می‌توانسته»، ۱۴، ۲۱ با `want + O + to`).
- **در این متن صفر ماندند:** شرطی، مقایسه، سببی، التزامی، `do` تأکیدی، صفت مرکب، مالکیت، `-ever`. سببی و `-ever` ستون لایهٔ ۳ متن‌های ۶ و ۷اند؛ التزامی و `do` تأکیدی متن ۵. **مالکیت در دورهٔ ۲ هنوز صفر است** — متن ۶ ستونش است.
- **تله‌های تازه‌ای که ارزش برگشتن دارند:** «از کودکی» ← `since` نه `from` (۱۳)، `grow up` در برابر `bring up` (۸)، «سهم داشتن» ← `play a part in` (۱۶)، «با» سازوکار ← `through` (۱۹)، `a few` / `a few of the` در دو واحد متقابل (۱۵ و ۲۱)، جزیره ← `on` (۱۴، ۱۸)، `speak a language` بدون `in` (۱، ۵، ۱۳)، `every few months` (۳)، `stop being used` برای «دیگر … نمی‌…» (۴).
- **برای متن ۵ (هوش مصنوعی و بازار کار):** ستون modal آینده، hedging و شرطی mixed؛ لایهٔ ۳ التزامی و `do` تأکیدی. `although / despite / however` (۱۸) در دورهٔ ۲ دو واحد گرفت؛ ستون متن ۹ است.

- **متن ۵ (۲۴ واحد):** هوش مصنوعی و بازار کار — داغ شدن دوبارهٔ بحث با چت‌بات‌ها، برآورد ۲۰۲۴ صندوق بین‌المللی پول (نزدیک ۴۰ درصد مشاغل «در معرض»، نه «در خطر حذف»)، خودپردازها و تحویلدارهای آمریکا (دههٔ ۱۹۷۰، رشد در دهه‌های بعد، کاهش از اوایل دههٔ ۲۰۱۰ با بانکداری موبایلی)، کارمند دفتری در برابر لوله‌کش و پرستار، برنامه‌نویس و «دو برابر کد»، بازآموزی. عددی جز ۴۰ درصد نیامد؛ روند تحویلدارها عمداً بی‌عدد است.
- **modal و hedging در ۷ واحد، هر کدام با ابزاری متفاوت:** `not necessarily` و دامنهٔ نفی (۵)، `be likely to` + مصدر مجهول (۶)، `would` آیندهٔ در گذشته (۹)، `may not have been` در برابر `can't have been` (۱۳)، `could` در برابر `can` برای پیش‌بینی (۱۴)، `tend to` برای «معمولاً»ی گرایشی (۱۵)، نهاد شخصی `are unlikely to be replaced` به‌جای `It is unlikely that` (۱۶).
- **شرطی در ۴ واحد:** mixed نوع دوم — ویژگی دائمی، نتیجهٔ گذشته (۳)، mixed نوع اول با `today` به‌عنوان تنها سرنخ (۲۰)، پلهٔ ۰ (۱۹)، و `would` شرطیِ پنهان برای پیامد فرضی (۲۳). هر دو mixed در یک متن‌اند و به هم ارجاع می‌دهند.
- **لایهٔ ۳:** التزامی دو واحد (۲۱ `propose that any worker whose job is at risk have` — التزامی در بند اصلی، فعل عادی در موصولی؛ ۲۲ التزامی مجهول `be offered` کنار `before people lose`)، `do` تأکیدی دو واحد (۱۷ «درست است که»، ۲۳ «البته» با نهاد جمع). backshift دو (۴ اختیاری برای محتوای هنوز صادق، ۹ اجباری). «حرف اضافه + اینکه» دو واحد: `the question of whose` (۲) و `the fear that` (۷).
- **جفت عمدی:** `twice as much code` (۱۸) و `half as many programmers as it has now` (۱۹) پشت‌سرهم — much/many در الگوی چندبرابری. ارزش برگشتن دارد.
- **در این متن صفر ماندند:** ماضی بعید، منفی دوگانه، سببی، وارونگی (فقط در `alt` واحدهای ۳ و ۲۰)، صفت مرکب، مالکیت، `-ever`. **مالکیت در دورهٔ ۲ هنوز صفر است** و سببی ستون لایهٔ ۳ متن ۶ است — متن ۶ هر دو را باید بیاورد.
- **تله‌های تازه‌ای که ارزش برگشتن دارند:** «نه‌تنها … نشد، بلکه» برعکس‌کننده ← `far from + -ing` در برابر `not only … but also` (۱۰)، `make + O + cheaper to run` بدون مفعول تکراری (۱۱، و `easy to use` در ۳)، `hit` / `affect` بی‌حرف‌اضافه و `harder` در برابر `more` (۱۴)، «پشت کامپیوتر» ← `at` (۱۵)، `any time soon` در جملهٔ شبه‌منفی (۱۶)، `appear in` در برابر حرکت (۸)، `less on … than on` (۲۴)، `Whether` در برابر `That` / `If` در جایگاه نهاد (۲۴).
- **برای متن ۶ (بحران مسکن):** ستون موصولی با حرف اضافه (`in which`، `whose`) و `at / on / in`؛ لایهٔ ۳ سببی و مالکیت. `whose` در واحد ۲۱ همین متن آمد ولی ستون متن ۶ است. ماضی بعید (۸) و منفی دوگانه (۱۲) در این متن نیامدند؛ یکی از هر کدام در متن ۶ اگر طبیعی جا بیفتد.

- **متن ۶ (۲۴ واحد):** بحران مسکن — جوان‌های شاغل بی‌خانه، سن خرید اولین خانه، زمین کمیاب لندن و روند مجوز، آپارتمان‌های خالیِ سرمایه‌گذارها، تخلیهٔ بی‌دلیل در انگلستان «تا همین اواخر» (قانون حقوق مستأجران ۲۰۲۵، اجرا از بهار ۲۰۲۶)، روایت «یکی از دوستانم» (مادر تنها، رنگ‌آمیزی به خرج خودش، اخراج شش ماه بعد برای اجارهٔ دو برابر)، وین (بیش از نیمی از ساکنان در خانه‌های ساخته یا یارانه‌ای شهر، سابقه از دههٔ ۱۹۲۰، بیش از ۶۰ هزار خانه تا اواسط دههٔ ۱۹۳۰). تنها عدد متن همین ۶۰ هزار است.
- **موصولی با حرف اضافه در ۹ واحد، با حرف اضافه‌های مختلف:** `the way in which` در برابر `with which` (۴)، `the age at which` با تست «اسم را به بند برگردان» (۵)، `land on which` (۷)، `whose` برای شیء (۱۰) و برای شهر (۲۴)، `to which` در برابر فعل جایگزین `that owned` (۱۵)، `the example to which … point` (۱۸)، `many of which` در برابر comma splice (۲۲). **جفت عمدی ۸ و ۹:** در ۸ ضمیر «در آن» باید برود، در ۹ «در آنها» باید بماند (`who never live in them`) — تست «جای خالی کجاست» ارزش برگشتن دارد.
- **`at / on / in` در ۸ واحد:** `at the age of thirty` در برابر `in their thirties` (۳)، `at which` برای سن (۵)، `in the city centre` در برابر کسره (۹)، `crammed into` — فعل حرکتی با «در» سکونی فارسی (۱۱)، `at her own expense` (۱۴)، `at twice the rent` در برابر `for` (۱۶)، `At the time` (۱۷)، `in private hands` (۲۴).
- **لایهٔ ۳:** سببی یک واحد (۱۴، `had had the flat she was renting repainted` — ماضی بعید سببی با بند موصولی وسط مفعول)، **مالکیت بالاخره ۶ واحد:** `a home of their own` (۱)، `its` در برابر `it's` (۱۰)، `tenants'` جمع (۱۲)، `a friend of mine` (۱۴)، `the city's` (۱۹)، `the 1920s` بی‌آپاستروف (۲۰). جانشینی `doing so` (۱۷)، comma splice (۲۲)، backshift `had to` (۱۵)، صفت مرکب `five-person` فقط در کنار `families of five` (۱۱).
- **ماضی بعید دو واحد** (۱۴ سببی، ۲۱ `by the mid-1930s had built` با جفت `by` / `until`). **منفی دوگانه چهار واحد:** `no longer` (۱)، `until` + مثبت (۶)، `never` (۹)، `no law prevented` (۱۷). **«حرف اضافه + اینکه»** یک واحد: «بی‌آنکه» ← `without giving` (۱۳).
- **تله‌های تازه‌ای که ارزش برگشتن دارند:** `spend on` / `go on` (۲)، جمع توزیعی `their childhood bedrooms` (۳)، `can` برای «گاهی چنین می‌شود» در برابر `may` (۸)، `wait for + O + to` (۱۰)، `protect` بی‌«از» (۱۲)، `let … to` در برابر `give` (۱۶)، `prevent + O + from` (۱۷)، `date back to` حال ساده (۲۰)، `competition from` در برابر `with` (۲۳)، `find + O + adj + to` بی‌مفعول تکراری (۲۴).
- **در این متن صفر ماندند:** شرطی، مقایسه (جز `twice the rent` در حاشیه)، وارونگی، التزامی، `do` تأکیدی، `-ever`، `although / despite / however` (هنوز ۲ در دورهٔ ۲)، بند اسمی.
- **برای متن ۷ (ورزش کودکان و رقابت):** ستون «حرف اضافه + اینکه» (۶ — دست‌کم ۴ واحد لازم است تا به ۱۰ برسد)، بند اسمی و الگوهای فعل؛ لایهٔ ۳ `-ever` (در دورهٔ ۲ فقط یک واحد) و comma splice. شرطی در متن‌های ۴ و ۶ نیامد؛ یکی در متن ۷ اگر طبیعی جا بیفتد.

- **متن ۷ (۲۵ واحد):** ورزش کودکان و رقابت — اختلاف بر سر رقابتی بودن، کنار کشیدن بچه‌ها پیش از سیزده‌سالگی، نروژ (قواعد «حقوق کودکان در ورزش» از ۱۹۸۷، ممنوعیت انتشار نتیجه و رتبه زیر یازده سال، صدر جدول مدال‌های المپیک زمستانی)، روایت «یکی از همسایه‌هایم» (هشت پسر ثابت، نیمکت، درخواست از مربی و جواب «پدر و مادرها نتیجه می‌خواهند»)، اثر سن نسبی (متولدهای اوایل سال)، و جمع‌بندی. تنها عدد متن سال ۱۹۸۷ و سن‌هاست؛ عمداً بی‌درصد، چون ردیف اعداد از هدف جلوتر است.
- **«حرف اضافه + اینکه» در ۷ واحد و با هفت راه‌حل متفاوت:** `agree on how…` (۲ — بعد از حرف اضافه بند wh- مجاز است)، `the question of whether` (۳)، افتادن حرف اضافه با بند that در `insist that` (۹)، `despite the fact that` (۱۳)، `without + gerund مجهول` (۱۶)، `there is little point in -ing` (۲۳)، و `judged on how many…` (۲۴). واحدهای ۳ و ۱۳ عمداً یک قاعده را با دو راه‌حل نشان می‌دهند و به هم ارجاع می‌دهند. **ردیف از ۶ به ۱۳ رسید و به هدف خورد.**
- **بند اسمی ۹ واحد** (۲، ۳، ۴، ۵، ۹، ۱۸، ۲۰، ۲۲، ۲۴) و **الگوهای فعل ۸ واحد** (۱ `want + O + to`، ۸ `prevent from -ing`، ۹ مصدرهای متوازن، ۱۴ `stop -ing` در برابر `stop to do`، ۱۵ `let + V1`، ۱۶ `spend + -ing`، ۱۷ `ask + O + to`، ۲۵ `mean -ing` در برابر `mean to do`). جفت عمدی ۱۴ و ۲۵: هر دو فعل دوگانه‌اند و هر دو با همان قاعدهٔ مولد (`-ing` عقب، `to` جلو) توضیح داده شده‌اند.
- **لایهٔ ۳:** `-ever` سه واحد و هر سه شکل متفاوت — `Whatever the sport` بی‌فعل (۱۰)، `However hard` نامعین با تلهٔ کاما (۱۹)، `No matter how talented` با فعل کامل (۲۵)؛ **ردیف از ۵ به ۸ رسید.** comma splice یک واحد اختصاصی (۱۱ با `in fact` و نقطه‌ویرگول) و یک واحد جانبی (۱۹ کامای بعد از `however`). التزامی دو واحد ولی هر دو **ضدِ التزامی**: `suggest` در معنای «حاکی است» (۴) و `insist` در معنای «تأکید می‌کند» (۹) — تقابلی که تا حالا نیامده بود و ارزش برگشتن دارد.
- **شرطی بالاخره برگشت** (۲۴، پلهٔ ۲ با دو سؤال درون‌نشانده در بند if). backshift دو واحد (۱۵، ۱۸)، ماضی بعید سه واحد (۱۵، ۱۶، ۱۷) در روایت همسایه.
- **جفت‌های عمدی دیگر:** `a few minutes` (۱۷) و `little point` (۲۳) — همان تقابل با شمردنی و ناشمار، و فاصله‌شان شش واحد است تا تکرار حس شود نه تقلید. `sport` سه بار با سه رفتار: `a sport` شمردنی (۱)، `organized sport` ناشمار (۴)، `giving up sport` ناشمار (۲۵).
- **تله‌های تازه‌ای که ارزش برگشتن دارند:** `mistake X for Y` در برابر `confuse X with` (۲۱)، `ask` بی‌«از» (۱۷)، `drop out of` با `of` اجباری (۴)، `take something seriously` با مفعول وسط (۶)، `than any other + اسم مفرد` (۱۲)، فعل مفرد بعد از بند `what` با متمم جمع (۱۸)، `none of this` مفرد در برابر `these` (۲۲)، `hard` در برابر `hardly` (۱۹)، عبارت اندازه قبل از صفت تفضیلی (۲۰).
- **در این متن صفر ماندند:** سببی، وارونگی (فقط در `alt` واحدهای ۱۰ و ۲۴)، `do` تأکیدی، صفت مرکب، حال ساده در برابر استمراری، modal و hedging. **سببی در دورهٔ ۲ فقط یک واحد دارد (متن ۶) و `do` تأکیدی دو واحد؛ ستون لایهٔ ۳ متن ۹ است `do` تأکیدی و التزامی — پس سببی و صفت مرکب باید در متن ۸ یا ۱۰ بیایند.**
- **برای متن ۸ (اکتشاف فضا و بودجهٔ علم):** ستون زمان‌های آینده، افعال عبارتی و حروف اضافهٔ دیگر؛ لایهٔ ۳ وارونگی و جانشینی. سببی را هم همین‌جا بیاور (در دورهٔ ۲ فقط ۱ واحد). حال ساده در برابر استمراری در متن‌های ۴ تا ۷ اصلاً نیامده و ردیفش (۵۲) از بقیهٔ لایهٔ ۱ عقب مانده — برنامهٔ پرتاب و مأموریت‌های در جریان جای طبیعی‌اش است.

- **متن ۸ (۲۴ واحد):** اکتشاف فضا و بودجهٔ علم — بحث تکراری بر سر هزینه، برنامهٔ برگشت به ماه و ایستگاه مداری، دفاع «سرریزهای فناوری»، واگذاری ساخت موشک به پیمانکارها، و روایت یک شرکت خصوصی که بودجهٔ دولتی‌اش قطع شد (طراحی تقریباً تمام، بررسی مستقل، رفتن مهندس‌ها، لغو پرتاب‌ها، رفتن مشتری‌ها). هیچ عدد و درصدی در متن نیست — فقط «دو سال پیش» و «پارسال» — چون ردیف اعداد از هدف جلوتر است.
- **زمان‌های آینده در ۸ واحد و با شش شکل مختلف:** `be due to` در برابر `will` و `going to` (۳)، آیندهٔ کامل مجهول با `by then` (۴)، `will` در بند اصلی و حال ساده در بند `until` (۵)، شرطی نوع اول (۹)، `will not be` با فاعل gerund (۱۹)، `will` وارونه بعد از `Only when` (۲۲)، آیندهٔ استمراری (۲۳). **قاعدهٔ «`will` ممنوع در بند زمان» عمداً چهار بار برگشت** (۱، ۴، ۵، ۲۲) و متن با همان شروع و تمام شد.
- **ردیف تازه: «شکل‌های آینده و بند زمان» باز شد** (۸). مثل ماضی بعید و منفی دوگانه، این الگو در دورهٔ ۱ بارها آمده بود و شمرده نشده بود.
- **افعال عبارتی ۹ واحد و این بار روی جایگاه ذره متمرکز:** `point out` بی‌حرف اضافه در برابر `point out to` (۷)، `carry out` مجهول (۸)، `cut back on` سه‌جزئی جدانشدنی (۹)، `put off` شکسته با مفعول کوتاه (۱۰)، `account for` برای سهم (۱۱)، `send off it` در برابر `send it off` (۱۵)، `keep to` و `call off` (۱۷)، `turn to` در برابر `turn into` (۱۸)، `win them back` (۱۹). **تست ضمیر عمداً سه بار در سه واحد دور از هم تکرار شد** (۱۰، ۱۵، ۱۹) — همان شیوهٔ «یک قاعده، چند برخورد» که در متن ۷ جواب داده بود.
- **سببی بالاخره سه واحد گرفت و هر سه با یک کارکرد متفاوت:** سفارش دادن (۱۲، `has them built by outside contractors`)، اتفاق ناخوشایند (۱۳، `had its government funding cut`)، و سببی گذشته با عبارت gerund (۱۵، `had the design checked … before sending it off`). ردیف از ۷ به ۱۰ رسید و از هدف گذشت — در متن‌های ۹ و ۱۰ فقط اگر خودش بیاید.
- **وارونگی دو واحد** (۲۱ `Not only do these missions produce`، ۲۲ `Only when … will this spending be justified`) و **جانشینی و حذف سه واحد** (۱۶ `did so`، ۲۰ `and so is Europe`، ۲۴ `just as it always has`). واحدهای ۲۰ و ۲۴ عمداً به هم ارجاع می‌دهند: در هر دو باید کمکی جملهٔ قبل را پیدا کنی، یکی در قالب موافقت و یکی در قالب مقایسه.
- **تله‌های تازه‌ای که ارزش برگشتن دارند:** «همین کار را کردند» ← `did so` نه `did the same work` (۱۶)، «هم همین‌طور» ← `so is Europe` نه `so Europe is` (۲۰)، «همیشه گرفته است» ← `as it always has` نه `has always` (۲۴)، فاعل مبهم فارسی («قطع کردند») ← سببی به‌جای `they` بی‌مرجع (۱۳)، جملهٔ فارسیِ بی‌فعل ← فعل واقعی در انگلیسی (۱۱، `account for`)، `reach` بی‌«به» (۲۲)، `leave for` نه `leave to` (۱۶)، `wait for` (۱۴)، `almost` قبل از فعل اصلی (۱۴).
- **در این متن صفر ماندند:** التزامی، `do` تأکیدی، `-ever`، اعداد، مقایسه، modal و hedging. **`do` تأکیدی (۸) و التزامی (۱۰) ستون لایهٔ ۳ متن ۹ هستند؛ `-ever` (۸) و اعداد فقط برای متن ۱۰ می‌مانند.**
- **برای متن ۹ (Task 2: ورود رایگان به موزه‌ها):** ستون رابط‌های تقابل (`although / despite / however` در دورهٔ ۲ هنوز کم است)، ارجاع و `this` + اسم جمع‌بندی‌کننده؛ لایهٔ ۳ `do` تأکیدی و التزامی. بند اسمی (۳۶) و فاعل صوری (۲۸) هم در متن ۸ کم آمدند و در متن استدلالی طبیعی جا می‌افتند. مقایسه در دورهٔ ۲ تقریباً دست‌نخورده مانده — اگر در استدلال طبیعی جا افتاد، یکی دو واحد اشکالی ندارد.

- **متن ۹ (۲۵ واحد):** ورود رایگان به موزه‌ها — متن استدلالی از جنس Task 2 با ساختار کامل مقاله: مقدمه و اعلام موضع، پاراگراف موافق (بریتانیا ۲۰۰۱، رشد نزدیک به ۵۰ درصد، عادت برگشتن)، پاراگراف مخالف (هزینه‌های جاری، روایت کوتاه «موزهٔ شهری که در آن بزرگ شده‌ام» و حرف مدیرش، شواهد بررسی‌ها)، و نتیجه‌گیری با دو توصیهٔ التزامی. تنها عدد متن ۲۰۰۱ و ۵۰ درصد است.
- **رابط‌های تقابل در ۵ واحد و با هر سه دستهٔ گرامری:** `however` وسط جمله بین دو کاما (۲)، `however` بعد از فاعل (۱۰)، `Although` با تلهٔ «اگرچه … ولی …» — ستون متن (۱۵)، `Despite + -ing` با سه راه‌حل هم‌ارز در بخش قاعده (۱۷)، و `however` در کنار تلهٔ `But … however` (۲۱). **واحد ۱۲ (`instead of + -ing`) عمداً به همین خانواده وصل شده و در بخش «تله» به واحد ۱۵ ارجاع می‌دهد** — همان شیوهٔ «یک قاعده، چند برخورد». ردیف از ۲۳ به ۲۸ رسید.
- **`this` + اسم جمع‌بندی‌کننده سه بار و هر بار با اسم متفاوت:** `this policy` (۲)، `this argument` (۸)، `this finding` (۲۱). در هر سه، `this` تنها به‌عنوان خطا در `misses` آمده. ارجاع و ضمیر در ۸ واحد؛ **واحد ۴ عمداً یک ارجاع دورِ کمی پرریسک است** (`its` به «این سیاست» دو جمله قبل، در حالی که `a collection` نزدیک‌تر است) و بخش «متغیر» همان‌جا قاعدهٔ فاصله را هشدار می‌دهد.
- **لایهٔ ۳ — التزامی بالاخره دو واحد اختصاصی گرفت:** `guarantee` بی `-s` در کنار `that scraps` با `-s` در همان جمله (۲۳ — این جفتِ درون‌جمله‌ای بهترین جای قفل کردن قاعده است)، و التزامی مجهول `be paid` با گروه اسمی بلند بین `that` و فعل (۲۴). هر دو واحد سه گزینهٔ «ساده / should / مصدر» را نشان می‌دهند. ردیف از ۱۰ به ۱۲. **`do` تأکیدی یک واحد** (۱۴، همراه با آپاستروف جمع `critics'`) — ردیف ۹ شد و هنوز یکی کم دارد.
- **جفت عمدی ۱ و ۹:** `a number of governments have` در برابر `the number of visits had risen` — فاصله‌شان هشت واحد است و واحد اول صریحاً وعدهٔ برگشتش را می‌دهد. **تلهٔ ضمیر تکراری سه بار برگشت** (۷ `attract` بی مفعول، ۱۹ `designed for` بی `them`، ۲۵ `they replaced` بی `them`) و واحد ۲۵ در بخش «تله» به این تکرار عمدی اشاره می‌کند.
- **دو ساختار تازه که تا حالا در هیچ متنی نیامده بودند:** جملهٔ شکافته (۱۳، `It is … that …` با wh-cleft در `alt`) و `as if / as though` + یک پله عقب (۲۱، که به نردبان فاصلهٔ شرطی‌ها وصل شده). هر دو ارزش برگشتن دارند، مخصوصاً دومی که خطایش بی‌سروصداست: جمله درست می‌ماند و فقط موضع نویسنده عوض می‌شود.
- **تله‌های تازهٔ دیگر:** `worth` با دو رفتار در یک متن (۵ `worth defending`، ۱۳ `worth the money`)، `paid for by` با دو حرف اضافهٔ پشت‌سرهم (۳)، `the way how` (۱۱)، `such a` در برابر `no such` (۱۰ و ۲۵ — جفت عمدی)، `staff have` (۱۶)، `had had to` (۱۸)، `evidences` (۱۹)، `carried out` در برابر `carrying out` (۲۰)، `on the cheap` (۲۲)، `per` و `each` با اسم مفرد (۲۴)، `outside of` (۱۰)، `to home` (۱۲).
- **در این متن صفر ماندند:** سببی، وارونگی، `-ever`، صفت مرکب (فقط در `alt` واحد ۳). **همهٔ اینها باید در متن ۱۰ بیایند** — `-ever` روی ۸ مانده و `وارونگی` روی ۹، یعنی هر دو هنوز زیر هدف‌اند؛ سببی (۱۰) و صفت مرکب (۱۴) به هدف خورده‌اند و فقط اگر طبیعی جا افتادند.
- **برای متن ۱۰ (جمع‌بندی — پسماند غذا):** آخرین متن دورهٔ ۲. ستون: باقی‌ماندهٔ لایهٔ ۳ — **وارونگی و `-ever` اجباری‌اند**، به‌علاوهٔ یک واحد `do` تأکیدی تا ردیفش به ۱۰ برسد. موضوع پسماند غذا اعداد و درصد را طبیعی می‌آورد و مقایسه هم (در دورهٔ ۲ فقط ۳ واحد) جای طبیعی دارد. شرطی هم در دورهٔ ۲ کم مانده (۲۹) و در استدلال «اگر این کار را بکنیم» خودش می‌آید.

- **متن ۱۰ (۲۵ واحد):** پسماند غذا — متن پایانی دورهٔ ۲ و عمداً جمع‌بندی‌محور. چهار پاراگراف: ابعاد مسئله با عدد (یک‌سوم جهانی، ۴٫۷ میلیون تن بریتانیا، چندبرابری در برابر مغازه‌ها و رستوران‌ها، مقایسه با انتشار گازها)، علت‌ها در خانه (دو تاریخ روی بسته‌بندی، عادت‌های خرید، دو کالا به قیمت یکی)، یک روایت میدانی (زنجیرهٔ فروشگاهی شمال انگلستان، خیریهٔ نان، حرف مدیرش)، و نتیجه‌گیری استدلالی.
- **هر سه نکتهٔ اجباری آمدند.** **وارونگی ۳ واحد و هر بار با آغازگر متفاوت:** `Not only does` (۴)، `Rarely do` (۲۰)، `Not until … will` (۲۴). واحد ۲۴ عمداً روی تلهٔ «وارونگی روی کدام بند می‌افتد» تمرکز دارد، که در متن ۸ (`Only when … will`) لمس نشده بود. ردیف از ۹ به ۱۲. **`-ever` ۳ واحد:** `However carefully` بی‌کاما (۱۳)، `no matter how much` در نقل قول (۱۹)، `Whatever governments decide` (۲۳) — ردیف از ۸ به ۱۱، یعنی هر دو بالاخره از هدف گذشتند. **`do` تأکیدی یک واحد** (۲۱، `do raise awareness` در الگوی امتیاز-و-رد) و ردیف به ۱۰ رسید.
- **جفت عمدی ۱۱ و ۱۵:** `do not help either` در برابر `have done so too` — یک صورت فارسی («هم»)، دو جواب، با چهار واحد فاصله، و واحد ۱۵ در بخش «متغیر» صریحاً به این جفت اشاره می‌کند. همان شیوه‌ای که در متن ۶ («اگر … بود») و متن ۷ («تا وقتی») جواب داده بود.
- **تست ضمیر در فعل عبارتی چهار بار برگشت:** `throw it away` (۲)، `hand it over` (۱۶)، `set them up` (۲۰)، `giving it away` (۲۲). واحد ۲۰ در بخش «متغیر» به این تکرار اشاره می‌کند. در متن ۸ هم سه بار آمده بود؛ این قاعده حالا ۷ برخورد در دورهٔ ۲ دارد و می‌شود در دورهٔ ۳ رهایش کرد.
- **دو مفعولی‌ها عمداً جفت شدند:** `cost each household` (۴) و `saved the charity` (۱۸)، و واحد ۱۸ به واحد ۴ ارجاع می‌دهد. `a year` و `a month` توزیعی هم در همان دو واحد آمدند.
- **قاعدهٔ «`will` ممنوع در بند زمان» دو بار پشت سر هم در پایان متن** (۲۴ و ۲۵) — واحد ۲۵ صریحاً می‌گوید عمدی است. همان الگوی متن ۸ که با همین قاعده شروع و تمام شده بود.
- **منفی دوگانهٔ فارسی پنج واحد** (۱، ۱۱، ۱۴، ۱۹، ۲۴) و این پرتکرارترین ردیف این متن است؛ ردیف از ۲۶ به ۳۱. واحد ۲۴ سه تلهٔ برگشتی را روی هم دارد (منفی دوگانه، `will` در بند زمان، حرف اضافهٔ اضافی) و بخش «تله»اش صریحاً می‌گوید اینها خطای دانشی نیستند، خطای اجرایی‌اند.
- **تله‌های تازه‌ای که ارزش برگشتن دارند:** `get round to cooking` — `to`ی حرف اضافه‌ای (۱۴)، `alone` در برابر `Only in` (۲)، `three times the amount` در برابر `three times more than` (۳)، `survive the departure` بی حرف اضافه (۲۰)، `go on -ing` در برابر `go on to do` (۲۵)، `ought to have been eaten` در برابر `must have been eaten` (۲۵)، `is that` بدون «این» (۸)، `the other` در برابر `another` (۹)، `few` بی `a` با معنای منفی (۱۰)، `raise` در برابر `rise` (۲۱)، `on their own` در برابر `by themselves` (۲۱ — سومین برخورد این خانواده بعد از متن ۷).
- **سببی و فاعل صوری در این متن صفر ماندند.** سببی (۱۰) به هدف خورده بود، ولی **فاعل صوری `there`/`it` روی ۳۲ ایستاده و در چهار متن آخر دورهٔ ۲ تقریباً هیچ نگرفت** — برای دورهٔ ۳ یادداشت شود.
- **برای دورهٔ ۳ (نقشه هنوز چیده نشده):** عقب‌ترین‌های لایهٔ ۳ حالا سببی (۱۰)، `do` تأکیدی (۱۰)، `-ever` (۱۱)، وارونگی (۱۲) و التزامی (۱۳) هستند — یعنی کل لایهٔ ۳ بالاخره از هدف گذشته و دیگر هیچ ردیفی صفر یا نزدیک صفر نیست. عقب‌ترین لایهٔ ۲ فاعل صوری (۳۲) است. بقیهٔ ردیف‌ها همه بالای هدف‌اند، پس نقشهٔ دورهٔ ۳ نباید بر اساس «چه چیزی کم آمده» چیده شود؛ باید بر اساس **جنس تله** چیده شود — همان تله‌هایی که در یادداشت‌های بالا «ارزش برگشتن» خورده‌اند و هنوز ردیف مستقلی ندارند (فعل سبک، جای ذره، `to`ی حرف اضافه‌ای، جفت‌های معنایی مثل `raise/rise`، و صورت‌های فارسیِ چندجوابه).

### دورهٔ ۳

- **متن ۱ (۲۵ واحد):** کار داوطلبانه — چهار پاراگراف: تصویر کلی و دو نوع داوطلبی با آمار Community Life Survey (سهم داوطلبی رسمیِ دست‌کم ماهی یک بار از حدود ۲۷٪ در ۲۰۱۳ به حدود ۱۶٪ در ۲۰۲۱ و بی‌بازگشت ماندنش)، هزینهٔ پنهان برنامه‌های داوطلبی برای سازمان‌ها (استعلام سابقه، آموزش، نظارت)، یک روایت میدانی (کتابخانهٔ محله‌ای که شورای شهر در ۲۰۱۶ بست و اهالی ظرف کمتر از یک سال بازش کردند)، و نتیجه‌گیری استدلالی. فایل `texts/03/drill.html` از قالب ساخته شد.
- **ستون (فعل مرکب فارسی) در ۸ واحد و با هر سه جهتش:** فارسی حرف اضافه دارد و انگلیسی ندارد — `help charities` (۱)، `supervise people` (۱۱)، `replace paid staff` (۲۲)؛ انگلیسی حرف اضافه می‌خواهد و فارسی حرف اضافهٔ دیگری دارد — `help somebody with` در برابر «کمک در» (۴)، `depend on` آویزان (۲۳)؛ و قالب‌های چندتکه‌ای که فارسی هیچ ردی ازشان نمی‌دهد — `think of X as Y` (۲)، `blame A on B` در برابر `blame B for A` (۷)، `treat X as Y` + `make a mistake` (۲۴). **واحد ۱ در بخش «متغیر» صریحاً تست عملی را می‌دهد** («فعل فارسی دو تکه + حرف اضافه ← به فعل انگلیسی شک کن») و واحدهای بعدی بارها به آن برمی‌گردند.
- **دام مضاعف جزوه (فعل حرف اضافه نمی‌خواهد ولی اسمِ هم‌ریشه‌اش می‌خواهد)** در `misses` واحد ۱ آمد: `help charities` در برابر `give help to charities`. ارزش دارد در متن ۵ (اسم‌سازی) ستون بشود، چون دقیقاً همان‌جا زنده می‌شود.
- **سببی سه واحد و هر سه با شکل متفاوت** — بی‌عامل `must have their background checked` (۱۰)، با عامل `have new volunteers trained by outside professionals` (۱۳)، و نسخهٔ معلوم `get experienced members to do it` (۱۴). واحد ۱۴ عمداً روی تنها استثنای خانواده تمرکز دارد: `get` به `to` نیاز دارد، `have` و `make` و `let` نه. ردیف از ۱۰ به ۱۳.
- **مالکیت و آپاستروف چهار واحد و هر بار با یک تصمیم متفاوت:** `a doctor's appointment` (۴)، جمعِ `volunteers'` (۱۸)، مفردِ `the group's` (۱۹)، و `its` بی‌آپاستروف (۲۰). **۱۸ و ۱۹ عمداً پشت سر هم‌اند** چون تنها فرقشان جای آپاستروف است و فارسی این تفاوت را اصلاً علامت نمی‌زند. ردیف از ۲۸ به ۳۲.
- **جفت عمدی ۹ و ۱۲:** «هم» در جملهٔ منفی ← `either`، «هم» در جملهٔ مثبت ← `as well`، با سه واحد فاصله و ارجاع صریح در هر دو. همان شیوه‌ای که در متن‌های ۶ و ۷ دورهٔ ۲ جواب داده بود. **جفت عمدی ۸ و ۱۷:** `earlier` و `later` به‌عنوان قرینهٔ هم، هر دو با ماضی بعید و هر دو در برابر `ago`.
- **ضمیر بازگشتی فارسی پنج بار برگشت** (۲، ۱۶، ۲۳، ۲۴، ۲۵) و واحد ۲۳ سخت‌ترینش است: آنجا باید «به آن» را نصف کنی — ضمیر برود، حرف اضافه بماند. این خانواده در دو دورهٔ قبل هم مدام آمده بود ولی ردیف نداشت؛ حالا دارد.
- **تله‌های تازه‌ای که ارزش برگشتن دارند:** `recover` ناگذر در برابر مجهول درست کنارِ `close` گذرا در واحد ۱۶ (۶ و ۱۶ — جفت عمدی روی همان «شدن» فارسی)، `mean + -ing` در برابر `mean that` در یک متن (۳ و ۲۲)، `whether` در جایگاه فاعل (۱۵)، حذف مقایسه‌ای `than they were` (۲۱)، `stop + -ing` در برابر `stop + to` (۲۵)، `want` و `ask` که بند `that` نمی‌گیرند (۱۲ و ۲۵)، `a day a week` با دو `a` متفاوت (۱۱)، و `they` مفردِ بی‌جنسیت به‌عنوان معادل «او»ی فارسی (۱۰ و ۲۰).
- **تصمیم روایی که تکرارش کن:** هر جا شخصیت داستان جنسیتش معلوم نبود، یا جمع شد (واحد ۱۱: «دو مدیر») یا `they` مفرد گرفت (۱۰، ۲۰). فارسی جنسیت ندارد و اگر `he` یا `she` بگذاری، اطلاعاتی را جعل کرده‌ای که در متن فارسی نبوده — یعنی واحد را از «تله» به «اطلاعاتِ غایب» می‌بری.
- **در این متن صفر ماندند:** شرطی، modal و hedging، `although / despite / however`، **فاعل صوری** (فقط در `alt` واحد ۱۵)، وارونگی، التزامی، `do` تأکیدی، `-ever`. فاعل صوری از پایان دورهٔ ۲ روی ۳۲ ایستاده و ستون متن ۶ است؛ اگر آنجا هم کم بیاید، متن ۱۰ باید جبرانش کند.
- **برای متن ۲ (انرژی تجدیدپذیر و شبکهٔ برق):** ستون جفت‌های معنایی نزدیک است — دست‌کم ۶ واحد، و `raise/rise` حتماً یکی از آنها، چون در دورهٔ ۲ (متن ۱ واحد ۲، متن ۱۰ واحد ۲۱) دو بار فقط در حاشیه لمس شد. موضوع اعداد و درصد را طبیعی می‌آورد. لایهٔ ۳: اعداد و صفت مرکب. **دو خانوادهٔ صفرماندهٔ متن ۱ — اسم‌سازی و وزن جمله — ستون متن‌های ۵ و ۶ هستند؛ در متن ۲ سراغشان نرو.**
- **متن ۲ (۲۵ واحد):** انرژی تجدیدپذیر و شبکهٔ برق — چهار پاراگراف: افت هزینهٔ خورشیدی و سهم ۱۳ درصدی باد و خورشید در ۲۰۲۳ (بیش از سه برابر ده سال قبل)، مسئلهٔ تطبیق لحظه‌ای عرضه و تقاضا و هزینهٔ ذخیره‌سازی، روایت میدانی استرالیای جنوبی (طوفان سپتامبر ۲۰۱۶، باتری صد مگاواتی، صرفه‌جویی حدود ۱۵۰ میلیون دلار در دو سال اول)، و نتیجه‌گیری با شرطی مختلط و hedging.
- **ستون (جفت‌های معنایی نزدیک) در ۷ واحد و با سه جنس متفاوت:** جفت ناگذر/گذرا — `fall` در ۱ و `raise` در ۲، عمداً پشت سر هم و با **«را»ی فارسی به‌عنوان تست عملی** («مفعول نشان‌دار داری؟ پس فعل گذرا لازم است»)، و `waste` در ۸ که همان تست را در قالب مجهول امتحان می‌کند؛ جفت هم‌ریشه با معنای متفاوت — `economical` در برابر `economic` در ۱۲؛ و جفت اسم/فعل — `effect on` در ۱۹ در برابر `affect` بی‌حرف اضافه در ۲۱، با دو واحد فاصله و ارجاع صریح در هر دو. واحد ۱۴ (`matter` در برابر `have importance`) حلقهٔ وصل ستون این متن با ستون متن ۱ است.
- **`raise/rise` بالاخره ردیف مستقل گرفت.** در دورهٔ ۲ دو بار فقط در حاشیه لمس شده بود. حالا در واحد ۲ هستهٔ بحث است و در ۱ زمینه‌اش چیده شده.
- **منفی دوگانهٔ فارسی چهار بار و هر بار با نشانهٔ متفاوت:** `neither … is` (۵)، `nowhere to go` (۸)، `no country has` (۲۲)، `will never happen` (۲۳). واحد ۲۲ در بخش «متغیر» صریحاً می‌گوید این سومین بار است — تکرار عمدی و اعلام‌شده، چون این خطا قاعدهٔ نحوی زبان مادری است نه فراموشی واژگانی.
- **صفت مرکب سه بار و یک‌بارش روی استثنا:** `newly built` بدون خط تیره (۹، چون اول قید `-ly` است، در تقابل صریح با `gas-fired` و `large-scale`)، `100-megawatt` با واحد مفرد (۱۸)، و `fossil fuel subsidies` به‌عنوان اسم در نقش صفت (۲۴). ردیف از ۱۸ به ۲۱.
- **جفت عمدی ۶ و ۱۴:** همان کلمهٔ `electricity`، یک‌بار بی‌حرف تعریف (مادهٔ کلی) و یک‌بار با `the` (قابل تشخیص از بافت متن). هر دو واحد صریحاً به هم ارجاع می‌دهند. همان شیوهٔ جفت‌سازی که در دورهٔ ۲ و متن ۱ جواب داده بود.
- **اعداد چهار واحد:** `per cent` دوکلمه‌ای و بی‌جمع (۳)، چندبرابری `three times the figure for` در برابر `times more than`ِ مبهم (۴)، واحد مفرد در صفت مرکب (۱۸)، و `150 million` بی‌`s` کنار ترتیبیِ `the first two years` (۲۰). ردیف از ۳۰ به ۳۴.
- **تله‌های تازه‌ای که ارزش برگشتن دارند:** `leave + مفعول + وضعیت` در برابر `make` که فقط صفت یا اسم می‌گیرد (۱۵، و در ۲۳ دوباره از سمت `make` به آن اشاره شد)، ساخت میانه `does not store easily` در `alt` واحد ۶، `what` در برابر `something that` (۱۷)، فعل‌های دومفعولی `save somebody something` (۲۰)، `may well` روی نردبان قطعیت در برابر `maybe` (۲۵)، و آپاستروف در عبارت زمان `ten years' time` (۲۵).
- **در این متن صفر ماندند:** سببی، وارونگی (فقط در `alt` واحد ۲۴)، التزامی، `do` تأکیدی، `-ever`، **فاعل صوری** (این بار حتی در `alt` هم نیامد). فاعل صوری از پایان دورهٔ ۲ روی ۳۲ ایستاده و دو متن پشت سر هم از کنارش رد شده‌ایم؛ ستون متن ۶ است و اگر آنجا هم کم بیاید، متن ۱۰ باید جبرانش کند.
- **برای متن ۳ (حمل‌ونقل شهری و دوچرخه):** ستون «یک صورت فارسی، چند جواب» است — «تا»، «که»، «هم»، «شدن»، «ی». دو تایش در این متن ناخواسته شروع شده و باید آنجا سیستماتیک شود: «شدن» در واحدهای ۶ و ۸ (مجهول در برابر ناگذر)، و «هم» در واحد ۲۱ (`also` در وسط در برابر `too` در انتها). لایهٔ ۳: `-ever` و جانشینی. **سببی و التزامی و `do` تأکیدی دو متن است که صفر مانده‌اند؛ در متن ۳ دست‌کم یکی‌شان را جا بده حتی اگر ستون نیست.**

- **متن ۳ (۲۵ واحد):** حمل‌ونقل شهری و دوچرخه — چهار پاراگراف: تصویر کلی بریتانیا (سفرهای کوتاه ولی با ماشین، خیابانِ طراحی‌شده برای خودرو، امنیت ذهنی)، چرخش هلند در دههٔ ۱۹۷۰ و تقابل رنگ در برابر جداسازی فیزیکی، یک روایت میدانی (محله‌ای در شمال شرقی لندن، بستن خیابان‌های فرعی، اعتراض مغازه‌دارها، دو برابر شدن سفرهای دوچرخه در سه سال، دزدی دوچرخه)، و نتیجه‌گیری با امتیاز-و-رد. هیچ عدد و درصدی جز «سه کیلومتر» و دهه‌ها در متن نیست، چون ردیف اعداد (۳۴) خیلی از هدف جلوتر است.
- **ستون در ۱۸ واحد پخش شد و هر پنج صورت سیستماتیک شدند.** «شدن» چهار جواب: مجهول واقعی (۳، ۱۰)، فعل معلومِ تعریفی `count as` (۱۱)، مجهولِ `be made` در برابر `become` (۱۶)، و ناگذرِ `double` (۱۹). «که» چهار جواب: `so that` هدف (۴)، `where` موصولی با ضمیر بازگشتی (۵)، `so … that` نتیجه (۷)، `that` بند اسمی (۸ و ۱۸ و ۲۲). «تا» چهار جواب: `until` (۶)، `by` (۹)، `to`ی بازهٔ مکانی (۱۴)، `to`ی هدف (۱۵). «هم» سه جواب و هر سه در یک پاراگراف: `so did` (۱۷)، `either` (۱۸)، `also` (۲۰). «ی» دو جواب: نکره ← `a` (۱) در برابر موصولی ← `the` + بند (۲).
- **جفت‌های عمدی و ارجاع‌های صریح:** ۱ و ۲ («ی»، پشت سر هم)، ۶ و ۹ («تا»، `until` در برابر `by`)، ۱۷ و ۱۸ و ۲۰ («هم»، سه واحد پیاپی)، ۱۳ و ۲۵ (`makes a difference` در برابر `will make no difference` — واحد آخر صریحاً به ۱۳ برمی‌گردد). ردیف ستون از ۷ به ۲۵ رسید.
- **لایهٔ ۳:** `-ever` سه واحد و هر بار با شکل متفاوت — `wherever` (۱۲)، `however short the distance` با **حذف فعل** (۱۴)، `whichever` (۲۴)؛ ردیف از ۱۱ به ۱۴. جانشینی و حذف پنج واحد با پنج ابزار — حذف `is` بعد از `however` (۱۴)، `others` (۱۶)، `so did` (۱۷)، `one` (۲۱)، `far more` (۲۳)؛ ردیف از ۳۴ به ۳۹. **سببی و `do` تأکیدی که دو متن صفر مانده بودند، هر کدام یک واحد گرفتند:** `had their bikes stolen` (۲۱، ناخوشایند، با فاعل مبهم فارسی) و `do take up space` (۲۳، امتیاز-و-رد).
- **قاعدهٔ «`will` ممنوع در بند زمان و بند تعمیم» عمداً سه بار آمد** (۶ `until`، ۲۴ `whichever`، ۲۵ `until`) و متن با همان قاعده شروع و تمام شد — همان الگوی متن ۸ و ۱۰ دورهٔ ۲. ردیف «شکل‌های آینده و بند زمان» از ۱۵ به ۱۹.
- **منفی دوگانهٔ فارسی پنج واحد** (۶ `no campaign will`، ۱۴ `there is no`، ۱۸ `no one was listening`، ۲۱ `never bought`، ۲۵ `will make no difference`) و ردیف از ۳۸ به ۴۳. در هر پنج، منفی روی یک کلمه سوار شده و فعل مثبت مانده.
- **فاعل صوری بالاخره یک واحد گرفت** (۱۴، `there is no continuous route`) و از ۳۲ به ۳۳ رسید. سه متن پشت سر هم تقریباً از کنارش رد شده بودیم. **ستون متن ۶ است و باید آنجا دست‌کم ۵ واحد بگیرد.**
- **تله‌های تازه‌ای که ارزش برگشتن دارند:** `a means of transport` مفردِ `s`دار (۱)، `count as` در برابر `consider` بی `as` (۱۱)، حذف فعل در `however short the distance` (۱۴)، `others` بی‌اسم در برابر `other` (۱۶)، ترتیب وارونهٔ `so did taxi drivers` (۱۷)، `that` دوم در نقل قول دوبخشی (۱۸ — دومین برخورد بعد از متن ۶ دورهٔ ۲)، `later` در برابر `ago` داخل قاب گذشته (۱۹ — دومین برخورد بعد از متن ۷ دورهٔ ۱)، `none of this` مفرد در برابر `these` (۲۲ — سومین برخورد)، و سؤال درون‌نشانده بدون وارونگی با حرف اضافهٔ آویزان `who the street belongs to` (۲۴).
- **در این متن صفر ماندند:** شرطی، وجه التزامی، اعداد و درصد، اسم‌سازی، وزن جمله، ثبت، و «حرف اضافه + اینکه». **التزامی (۱۳) تنها نکتهٔ لایهٔ ۳ است که در این دوره هنوز هیچ نگرفته** — متن ۵ (اسم‌سازی) و متن ۹ (Task 2) ستون‌هایش را دارند، ولی اگر تا متن ۷ نیامد، جایی برایش باز کن.
- **برای متن ۴ (گوشی و عادت‌های روزمره):** ستون جای ذره و مفعول در افعال عبارتی به‌علاوهٔ `to`ی حرف اضافه‌ای است — دست‌کم ۶ واحد، و تست ضمیر (`switch it off` در برابر `switch off it`) حتماً چند بار. ردیف «جای ذره» با ۲ واحد از این متن شروع شده (۸ `give them back`، ۲۳ `take up space`)، پس از صفر شروع نمی‌کند. لایهٔ ۳: نقل قول و backshift. **حال ساده / استمراری (۶۸) عقب‌ترین ردیف لایهٔ ۱ است و موضوع عادت‌های روزمره جای طبیعی‌اش است — دست‌کم ۵ واحد.** شرطی هم در این دوره هنوز صفر است؛ «اگر گوشی را کنار بگذاری» خودش می‌آید.

- **متن ۴ (۲۵ واحد):** گوشی و عادت‌های روزمره — چهار پاراگراف: تصویر روزمره (برداشتن و کنار گذاشتن گوشی، خودکار شدن عادت، پایین آمدن سن اولین گوشی، طراحی اپلیکیشن‌ها)، مکانیسم توجه و خواب (هزینهٔ قطع تمرکز، عادت به در دسترس بودن، توصیهٔ کنار گذاشتن گوشی پیش از خواب)، یک روایت میدانی (دبیرستانی در شمال ولز، تحویل گرفتن گوشی‌ها، حرف مدیر و معلم‌ها در دو مقطع ترم)، و نتیجه‌گیری استدلالی. هیچ عدد و درصدی جز سن ده و سیزده و «یک ساعت» در متن نیست، چون ردیف اعداد (۳۴) خیلی جلوتر از هدف است.
- **ستون در ۹ واحد و با هر دو نیمه‌اش.** جای ذره پنج بار با ضمیر (۲ `put them down`، ۱۲ `switching them off`، ۱۷ `pick them up`، ۲۲ `puts it off`) و سه بار با مفعول اسمی (۱ `pick up our phones`، ۱۳ `putting the phone away`، ۱۶ `hand their phones in`، ۲۵ `put their phones away`). **جفت عمدی ۱۶ و ۱۷:** همان فعل، یکی با اسم و ذرهٔ آخر و یکی با ضمیر و ذرهٔ وسط، پشت سر هم — و واحد ۱۷ در بخش «تله» صریحاً می‌گوید خطر اصلی تعمیمِ الگوی واحد قبلی است، نه بی‌اطلاعی از قاعده. `to`ی حرف اضافه‌ای یک واحد اختصاصی گرفت (۱۱ `get used to being` با تست اسم) و در ۱۰ (`get back to`) زمینه‌اش چیده شد. ردیف از ۲ به ۱۱.
- **انتخاب بین دو ترتیبِ درست هم آموزش داده شد، نه فقط درست در برابر غلط.** واحدهای ۱ و ۱۶ و ۲۵ روی قاعدهٔ «کهنه جلو، تازه و سنگین آخر» می‌چرخند. همین اصل در واحد ۱۰ به شکل extraposition برگشت و ردیف تازهٔ «وزن و ترتیب جمله» را از صفر به ۲ رساند — **ستون متن ۶ است و آنجا باید دست‌کم ۵ واحد بگیرد.**
- **حال ساده در برابر استمراری پنج واحد و به شیوهٔ «یک صورت فارسی، چند جواب»:** واحدهای ۱ و ۴ و ۵ عمداً یک صورت فعلی فارسی دارند و سه جواب متفاوت می‌گیرند — عادت (حال ساده)، لحظه (استمراری با `at this very moment`)، و روند (استمراری بدون قید لحظه). واحد ۵ در بخش «متغیر» صریحاً به این سه‌تایی اشاره می‌کند. ردیف از ۶۸ به ۷۳، و عقب‌ترین ردیف لایهٔ ۱ دیگر نیست.
- **نقل قول و backshift چهار واحد پیاپی و هر بار با یک پلهٔ متفاوت:** گذشتهٔ ساده ← ماضی بعید (۱۸)، `cannot` ← `could not` به‌علاوهٔ دو پله در یک جمله (۱۹)، حال کامل ← ماضی بعید با فعل گزارشیِ استمراری (۲۰)، و **سؤال نقل‌شده با برداشتن ترتیب سؤالی** (۲۱). واحد ۲۱ در بخش «تله» نکتهٔ تازه‌ای را باز می‌کند: این خطا از فارسی نمی‌آید، از اتوماتیک شدنِ ناقصِ خود انگلیسی می‌آید. ردیف از ۲۷ به ۳۱.
- **التزامی بالاخره در این دوره یک واحد اختصاصی گرفت** (۲۴، `suggest that each family set its own rule`، با `its` بی‌آپاستروف در همان جمله) و **عمداً قرینهٔ واحد ۱۳** است که همان معنا را با `recommend + -ing` می‌سازد و التزامی را فقط در `alt` دارد. قاعدهٔ انتخاب بین این دو در بخش «متغیر» واحد ۲۴ آمده: اگر فاعل بند مهم است، برو سراغ `that`. ردیف ۱۳ به ۱۴.
- **جفت عمدی ۱۴ و ۲۳:** `do so` در برابر `do the same` — یکی فاعل را نگه می‌دارد و دیگری فاعل تازه می‌گیرد؛ نُه واحد فاصله و ارجاع صریح در واحد ۲۳. همان شیوهٔ جفت‌سازی که در متن‌های ۱ و ۳ این دوره جواب داده بود.
- **واحد ۲۵ عمداً سه تلهٔ برگشتی را روی هم دارد** (منفی دوگانه، `will` در بند زمان، جای ذره) و بخش «تله»اش می‌گوید هدف سنجش رفتار در بار شناختی بالاست، نه آموزش قاعدهٔ تازه. همان کاری که واحد ۲۴ متن ۱۰ دورهٔ ۲ کرد. **شرطی هم بعد از سه متن صفر، در واحد ۲۳ یک واحد گرفت** (نوع اول، با تلهٔ التزامی فارسی ← حال سادهٔ انگلیسی).
- **تصمیم روایی تکرار شد:** مدیر مدرسه و معلم‌ها جنسیت ندارند و `they` مفرد گرفتند (۱۹، ۲۱)، و بخش «متغیر» واحد ۱۹ دلیلش را می‌گوید — گذاشتن `he` یا `she` اطلاعاتی جعل می‌کند که در فارسی نبوده.
- **تله‌های تازه‌ای که ارزش برگشتن دارند:** `contact` بی `with` (۱۹ — همان خانوادهٔ ستون متن ۱)، `spend time on` (۸)، `much noisier` در برابر `very noisier` (۲۰)، `altogether` در برابر `all together` (۱۵)، `at the end of` در برابر `in the end` (۱۷)، `from scratch` بی `the` (۹)، `it takes X to do` (۱۰)، `few` بی `a` (۱۴ — دومین برخورد بعد از متن ۳)، `do this work` به‌جای `do so` (۱۴)، `its` در برابر `it's` (۲۴)، `an hour` (۱۳)، `peoples` (۴)، و `recommend/suggest` که مصدر نمی‌گیرند (۱۳ و ۲۴).
- **در این متن صفر ماندند:** سببی، وارونگی (فقط در `alt` واحد ۲۵)، `do` تأکیدی، `-ever`، صفت مرکب، اعداد، و جفت‌های معنایی نزدیک. `do` تأکیدی (۱۱) و سببی (۱۴) عقب‌ترین‌های لایهٔ ۳ در کل پروژه‌اند و **ستون لایهٔ ۳ متن‌های ۵ و ۷ هستند**.
- **برای متن ۵ (هفتهٔ کاری چهارروزه):** ستون اسم‌سازی است — فارسیِ فعل‌محور در برابر انگلیسیِ اسم‌محور — و ردیفش هنوز صفر است، پس دست‌کم ۶ واحد لازم دارد. **دام مضاعف جزوه (فعل حرف اضافه نمی‌خواهد ولی اسمِ هم‌ریشه‌اش می‌خواهد) دقیقاً همان‌جا زنده می‌شود؛** در متن ۱ فقط در `misses` واحد ۱ آمد (`help charities` در برابر `give help to charities`) و در واحد ۲۲ همین متن هم از سمت `ban … from` در برابر `a ban on` لمس شد. لایهٔ ۳: سببی و التزامی. موضوع هفتهٔ کاری، مقایسه و درصد را طبیعی می‌آورد ولی هر دو خیلی جلوتر از هدف‌اند، پس زورکی نیاور.

- **متن ۵ (۲۵ واحد):** هفتهٔ کاری چهارروزه — چهار پاراگراف: تصویر کلی (کاهش تاریخی ساعت کار و اینکه چیزِ تازه، خواستِ چهار روز بدون کم شدن حقوق است)، مکانیسم (تمایز میان کاهش ساعت و کاهش حجم کار، بازبینی وقت، جلسه‌ها، تأثیر بر خروجی)، یک روایت میدانی (آژانس طراحی در گلاسکو، آزمایش شش‌ماهه، اصرار اتحادیه، تیم پشتیبانی که نتوانست)، و نتیجه‌گیری (کارهای مبتنی بر حضور، ضرورت کم شدن خودِ کار). **موضوع با متن ۹ دورهٔ ۱ هم‌پوشانی دارد ولی زاویه‌اش کاملاً فرق می‌کند:** آنجا Task 2 و ربط‌دهنده‌های تقابل بود، اینجا روایت و اسم‌سازی؛ هیچ آمار مشترکی هم نیامد.
- **ستون (اسم‌سازی) در ۱۱ واحد و با هر سه سازوکارش.** فشارِ ساختاری که اسم را اجباری می‌کند — `the idea of` (۱)، `lead to` (۴)، `began with` (۱۱)، `expect` + مفعول (۱۹)؛ اسم‌سازی برای مقایسه‌پذیر کردن دو رویداد — `a reduction in hours … a reduction in workload` (۸) و جفت `the presence of / the completion of` (۲۳)؛ و **جهت معکوس** در واحد ۲۱ که زنجیرهٔ اضافهٔ فارسی را به فعل برمی‌گرداند و قاعدهٔ تعادل جزوه («یکی دو اسم‌سازی در هر پاراگراف» و «بیش از دو `of` پشت سر هم یعنی زیاده‌روی») را صریح می‌گوید. ردیف از ۰ به ۱۱.
- **زنجیرهٔ سه‌تاییِ اسم‌سازی از واحد ۴ به بعد عمداً تکرار شد و در واحد ۱۹ به‌عنوان «بسته» جمع‌بندی شد:** فعل ← اسم، قید ← صفت، و حرف اضافهٔ تازه برای اسم (`a sharp fall in`، `a clear improvement in`، `a careful review of`). واحدهای ۴، ۱۱، ۱۷، ۱۹ همه همین سه تصمیم را با هم می‌خواهند.
- **دام مضاعف جزوه بالاخره ستون شد.** واحد ۱۴ (`the effect of X on Y`) و واحد ۱۵ (`affect` بی حرف اضافه) عمداً پشت سر هم‌اند و در هر دو، فارسی همان «روی» را دارد — یعنی یک صورت فارسی، دو جواب. بخش «تله» واحد ۱۵ توضیح می‌دهد چرا این خطا مقاوم است: در نیمی از موارد تجربه تأییدش می‌کند، و بازخوردِ نامنظم از بازخورد منفی هم بدتر است.
- **سببی سه واحد و هر سه با نقش متفاوت:** بی‌عامل و از دید سفارش‌دهنده (۱۲ `had their meeting rules rewritten`)، معلوم با `get + to` (۱۳، با تأکید دوباره بر تنها استثنای خانواده)، و از دید کسی که کار سرش آمده (۱۷ `had their hours cut`). واحد ۱۲ در قاعده‌اش تقابل سببی و ماضی بعید را می‌گذارد — تنها تفاوتشان جای مفعول است. ردیف از ۱۴ به ۱۷ و به هدف رسید.
- **التزامی دو واحد و هر دو شکل تازه:** مجهولِ منفی بعد از فعل (۱۸ `insisted that pay not be reduced`، با توضیح صریح اینکه backshift روی التزامی اجرا نمی‌شود چون التزامی اصلاً زمان ندارد) و مجهول بعد از صفت ضرورت (۲۴ `It is essential that the workload itself be reduced`). ردیف از ۱۴ به ۱۶. در متن ۴ فقط نسخهٔ معلومش آمده بود.
- **`do` تأکیدی در واحد ۲۵ برگشت** و ردیف از ۱۱ به ۱۲ رسید — هنوز عقب‌ترین نکتهٔ لایهٔ ۳ است و **ستون لایهٔ ۳ متن ۷ است؛ دست‌کم ۲ واحد دیگر لازم دارد.** واحد ۲۵ عمداً سه تلهٔ برگشتی را روی هم دارد (do تأکیدی، جای ذره با فعل دومفعولی، و منفیِ فارسی بعد از «تا وقتی» که باید حذف شود) — همان کاری که واحد ۲۵ متن ۴ کرد.
- **وارونگی از سمت «نکردنش» آموزش داده شد** (۲۰): `only` روی نهاد وارونگی نمی‌خواهد، ولی `only` روی عبارت قیدی می‌خواهد. ردیف از ۱۴ به ۱۵.
- **منفی دوگانهٔ فارسی هفت واحد** (۶ `neither … had much`، ۹ `any`، ۱۰ `nothing else changes`، ۱۹ `nothing of the kind`، ۲۱ `never changed`، ۲۲ `none of this proves`، ۲۵ `until` بی منفی) و ردیف از ۴۶ به ۵۳. بیشترین تعداد در یک متن تا امروز.
- **تله‌های تازه‌ای که ارزش برگشتن دارند:** `the same as` در برابر «با … یکی نیست» (۸)، حذف `be` در توازی `not merely moved` (۲۴)، ضمیر تأکیدی `itself` بعد از اسم نه بعد از فعل (۲۴)، `halve` در برابر `half` (۱۳)، `run a trial` در برابر `make a trial` (۱۶)، `by a fifth` در برابر `to a fifth` (۱۷)، `output` ناشمار (۱۹)، `staff` جمعِ بی‌`s` (۲۳)، و دو نقطه در برابر comma splice (۲۱).
- **تصمیم روایی باز تکرار شد:** صاحب آژانس و کارکنان جنسیت ندارند و `they`ی مفرد گرفتند (۱۷)، با همان استدلال متن‌های ۱ و ۴.
- **در این متن صفر ماندند:** `although / despite / however` (۳۵)، `-ever` (۱۴)، و «یک صورت فارسی، چند جواب» فقط از راه جفت ۱۴/۱۵ شمرده شد. `-ever` (۱۴) و `do` تأکیدی (۱۲) و وارونگی (۱۵) عقب‌ترین‌های لایهٔ ۳ در کل پروژه‌اند.
- **برای متن ۶ (سیل و آمادگی شهرها):** ستون وزن و ترتیب جمله است — extraposition، فاعل صوری، اطلاعات کهنه اول و سنگین آخر. ردیفش با ۳ واحد از این متن به ۵ رسیده، پس از صفر شروع نمی‌کند. **فاعل صوری (۳۵) در چهار متن این دوره فقط ۲ واحد گرفته و ستون همین متن است — دست‌کم ۵ واحد لازم دارد، وگرنه باید متن ۱۰ جبرانش کند.** لایهٔ ۳: وارونگی. موضوع سیل، مجهول و ماضی بعید و بند زمان را طبیعی می‌آورد. `although / despite / however` (۳۵) در سه متن پیاپی صفر مانده؛ اگر در متن ۶ طبیعی جا افتاد یکی دو واحد بیاور، وگرنه ستون متن ۹ است.
- **متن ۶ (۲۵ واحد):** سیل و آمادگی شهرها — چهار پاراگراف: تصویر کلی (رودخانه‌های صاف‌شدهٔ شهرهای قدیمی، گران‌تر شدن سیل، زمینِ ساخته‌شده به‌جای زمینِ جاذب، سرعت آب در برابر مقدار باران، فاضلاب‌های قرن نوزدهمی)، سازوکار (ظرفیت چقدر کافی است، آمارِ اقلیمی که دیگر وجود ندارد، دو راهِ «بیرون نگه داشتن» و «جا باز کردن»، دیواری که مسئله را به شهر بعدی می‌فرستد، چرخش سیاست به سمت جا باز کردن، اهمیت زمانِ هشدار)، یک روایت میدانی (شهر کوچکی در شمال انگلستان، دو سیل پیش از تصمیم شورا، بالا بردن دیوار، آبی که از تپهٔ پشت شهر آمد، بی‌هشدار ماندن اهالی، بیمه‌ناپذیر شدن خانه‌ها، دو سال تا بازگشایی خیابان اصلی)، و نتیجه‌گیری استدلالی. هیچ درصدی در متن نیست و تنها عددها «یک متر»، «دو سال»، «بیست سال»، «قرن نوزدهم» و «یک‌سوم»اند، چون ردیف اعداد (۳۵) خیلی جلوتر از هدف است.
- **ستون (وزن و ترتیب جمله) در ۹ واحد و با همهٔ سازوکارهایش.** extraposition در جایگاه فاعل (۸ `It is difficult for engineers to…`) و در جایگاه مفعول (۲۰ `found it impossible to…`)؛ کهنه اول و تازه آخر (۱)؛ پله‌گذاری با `not only … but also` (۲)؛ کنار هم نشاندن عنصرهای مقایسه‌شونده به‌جای انداختنشان به ته جمله (۵ `rather than` وسط دو ویرگول)؛ فشرده کردن یک بند کامل فارسی در یک صفت (۶ `unnoticed`)؛ جدا نکردن فعل و مفعول (۱۱)؛ فرستادن بار سنگین به آخر با `what`-cleft (۱۴)؛ و جلو آوردن عمدی قید زمان برای زمینه‌سازی (۱۶ `for three winters it held`). ردیف از ۵ به ۱۴.
- **فاعل صوری بالاخره جبران شد: ۸ واحد.** `there is` معرفی‌کننده (۱)، `it`-cleft (۵)، `It is + صفت + for + مصدر` (۸)، `there are` با مطابقت جمع (۱۰)، `it`ِ مفعولی بعد از `find` (۲۰)، `It took … for … to` (۲۱)، `It is widely accepted that` (۲۲)، و `There remains` با مطابقت مفرد (۲۳). ردیف از ۳۵ به ۴۳ — یادداشت پایان دورهٔ ۲ که می‌گفت این ردیف در چهار متن آخر تقریباً هیچ نگرفت، اجرا شد.
- **وارونگی سه واحد و هر سه با آغازگر متفاوت:** `only` + عبارت قیدی (۱۲)، `Not until` با `did` قرضی و فاعلِ `anyone` (۱۷)، و `Not only` + modal با مجهول (۲۴). واحد ۱۲ عمداً به تقابل متن ۵ ارجاع می‌دهد (`only` روی نهاد در برابر `only` روی قید). ردیف از ۱۵ به ۱۸ و بالاخره از هدف رد شد.
- **واحد ۱۷ سه تلهٔ برگشتی را یک‌جا دارد** — وارونگی، منفیِ فارسی بعد از «تا وقتی» که باید حذف شود، و backshift — و بخش «تله»اش توضیح می‌دهد چرا منفیِ دوم خطرناک‌ترینشان است: چون خطا معنایی است و با بازخواندن جملهٔ انگلیسی پیدا نمی‌شود.
- **`although / despite / however` بعد از سه متن سکوت برگشت: ۳ واحد.** `however` وسط جمله با دو ویرگول (۱۴ و ۲۳) و `despite` + گروه اسمی (۲۰). واحد ۱۴ جدول سه‌دسته‌ای جزوه را در قاعده‌اش می‌گذارد. ردیف از ۳۵ به ۳۸.
- **جفت‌های معنایی نزدیک چهار واحد:** `lay/lie` (۷)، `raise/rise` در قالب مجهول (۱۶)، `say/tell` (۱۹)، و `insure/ensure` (۲۰). ردیف از ۱۴ به ۱۸.
- **منفی دوگانه هفت واحد** (۹ `no longer`، ۱۷ `not until` + `anyone`، ۱۸ `had nowhere to go`، ۱۹ `no warning at all`، ۲۱ `never did`، ۲۲ `no city can be made`، ۲۵ `until` بی منفی) و ردیف از ۵۳ به ۶۰. قاعدهٔ واحد ۱۸ صریح می‌گوید انگلیسی منفی را روی اسم می‌گذارد نه روی فعل.
- **تله‌های تازه‌ای که ارزش برگشتن دارند:** `whose` برای بی‌جان (۱)، `The reason is that` در برابر `is because` (۳)، `reach` بی حرف اضافه (۴)، `damage` در برابر `damages` (۵)، خانوادهٔ صفتِ `un-` برای بند «بی‌آنکه…» (۶)، `most of` + تعیین‌کننده (۷)، `by` در برابر `to` برای اندازهٔ تغییر (۱۶)، `another two years` (۲۱)، و مکمل اسم (`temptation to` در برابر `possibility of`) در واحد ۲۳.
- **واحد ۲۵ چهار تلهٔ برگشتی را روی هم گذاشت** — بند زمان بی `will`، منفیِ فارسی بعد از «تا وقتی»، `however much` (تنها واحد `-ever` این متن، ردیف از ۱۴ به ۱۵)، و مجهول از بی‌عاملِ فارسی — همان کاری که واحد ۲۵ متن‌های ۴ و ۵ کرد.
- **در این متن صفر ماندند:** سببی (۱۷)، التزامی (۱۶)، `do` تأکیدی (۱۲)، اعداد و درصد (۳۵، عمدی)، صفت مرکب (۲۴)، و شرطی (۳۴).
- **برای متن ۷ (امتحانات و ارزیابی):** ستون حذف و جانشینی در برابر تکرار فارسی است — ردیفش با ۲ واحد از این متن به ۴۶ رسیده. لایهٔ ۳: `do` تأکیدی و مالکیت. **`do` تأکیدی روی ۱۲ ایستاده و عقب‌ترین نکتهٔ لایهٔ ۳ در کل پروژه است؛ ستون لایهٔ ۳ همین متن است و دست‌کم ۲ واحد لازم دارد.** بعد از آن `-ever` (۱۵) و التزامی (۱۶) عقب‌ترین‌ها هستند؛ `-ever` ستون متن ۸ است و التزامی ستون متن ۹، پس هر دو جا دارند. موضوع امتحان، حذف با فعل کمکی و `one/ones` و `do so` را طبیعی می‌آورد و سببی (`have students assessed`) هم در آن جا می‌افتد.
- **متن ۷ (۲۵ واحد):** امتحانات و ارزیابی — چهار پاراگراف: تصویر کلی (تکیهٔ همهٔ نظام‌ها به امتحان رسمی، یک معیار واحد در برابر ارزیابی کلاسی، ساعت‌های تصحیح بعد از روز کاری، وابستگی نمرهٔ یک کلاس به حوصلهٔ ممتحن، فاصلهٔ نمرهٔ دو ممتحن روی یک برگه، امتیاز دادن به منتقدان، و ارجاع مدیرها به عدالت نه دقت)، سازوکار (کاری که از ممتحن خواسته می‌شود، چند دقیقه در برابر چند هفته، دقتِ چندگزینه‌ای در برابر انشا، تصحیح دوباره به دست دو ممتحن مستقل، ممتحن سوم، شواهدی که خلاف باور ممتحن‌ها را می‌گوید، و معیارهای متفاوتِ مدرسه‌ها)، یک روایت میدانی (دبیرستانی در شمال انگلستان که امتحان‌های پایان ترم را برداشت، دو استعفای سال قبلش، حرف مدیر، بی‌اثر ماندن تصمیم برای هر دو گروه دانش‌آموز، حرف یکی از همکارها، شکایت والدین، و برگشتن امتحان‌ها تا سال بعد)، و نتیجه‌گیری استدلالی. هیچ عددی در متن نیست جز «دو»، «سه» و «چند سال»، چون ردیف اعداد (۳۵) سه متن است که عمداً دست‌نخورده مانده.
- **ستون (حذف و جانشینی) در ۱۵ واحد و با تمام سازوکارهایش.** حذف با کمکیِ قرضی + وارونگی (۱ `so do poorer ones`)، کمکیِ تنها در منفی (۲ `whereas classroom assessment does not`)، `do so` (۳ و ۲۵)، حذف بعد از `than` (۵ `wider than most parents assume`)، حذف فعل در نیمهٔ دوم `not X but Y` (۶ و ۷ `rather than accuracy`)، `do`ی اجباری بعد از `to` (۸)، `that of` (۱۰)، دو حذفِ جفت‌شده با دو کمکی متفاوت در یک جمله (۱۳ `when they do not, it does not`)، `otherwise` (۱۴)، `the same` (۱۵)، `others` بی‌اسم (۱۷)، `nor did` + `ones` (۱۹)، `though not` (۲۲)، و تکرار حرف اضافه در حذفِ موازی (۲۴ `not from a single day`). ردیف از ۴۶ به ۶۱.
- **واحد ۱ تست عملی ستون را صریح می‌دهد** («هر جا فارسی فعلی را دوباره گفت، به‌جای ترجمه‌اش یک کمکی بگذار و بقیه را بینداز») و **واحد ۲ قاعدهٔ انتخاب کمکی را** («جملهٔ اول را در ذهنت سؤالی کن؛ هر کمکی‌ای که آنجا لازم شد، همان کمکیِ حذف است»). واحدهای ۸ و ۱۳ عمداً به هر دو برمی‌گردند.
- **دو نکتهٔ باریک که جفت شده‌اند:** واحد ۲ می‌گوید حذف بعد از کمکی **می‌ایستد** (`does not`, و نه `does not do this`)، و واحد ۸ می‌گوید بعد از `to` **نمی‌ایستد** و `do` اجباری است (`what examiners are asked to do`). این تقابل بهترین چیزی بود که از ستون بیرون آمد و ارزش دارد در متن ۱۰ برگردد.
- **`do` تأکیدی سه واحد و هر سه با یک کار متفاوت:** امتیاز دادن به منتقد (۶ `exams do measure something`)، قالب کاملِ امتیاز-و-رد در یک جمله (۱۱ `do take less time, but…`)، و امتیاز دادن به حرف خود گوینده داخل نقل قول گذشته (۱۸ `did reduce`). **محدودیت اصلی خانواده در واحد ۱۱ صریح آمده: `do` با `be` جمع نمی‌شود**، و همان‌جا توصیه شده جمله را با فعل اصلی بسازد تا این ابزار در دسترس بماند. ردیف از ۱۲ به ۱۵ و بالاخره از هدف رد شد — عقب‌ترین نکتهٔ لایهٔ ۳ حل شد.
- **مالکیت هشت واحد و هر بار با یک تصمیم متفاوت:** `'s` در برابر `of` بر اساس **جانداری و طول** در یک جمله (۴ `a class's results` و `the mood of the examiner who…`)، جمعِ `examiners'` (۵)، `its` بی‌آپاستروف (۱۵)، جمعِ `teachers'` (۱۸)، `one of + 's + جمع` (۲۰)، سازمان (۲۱ `the school's governing body`)، `each student's` (۲۴)، و برگشت به `of` برای شیء (۲۵ `the format of the exam`). واحد ۴ و ۲۵ عمداً دو سر متن‌اند تا همان قاعده دو بار در دو جهت دیده شود. ردیف از ۴۲ به ۵۰.
- **واحد ۲۰ در بخش «متغیر» تفاوت دوگانهٔ `insist` را می‌گوید** — «با تأکید گفتن» فعل معمولی می‌گیرد، «مطالبه کردن» التزامی — و صریح به واحد ۲۴ ارجاع می‌دهد که همان خانواده با `propose` برمی‌گردد. اگر جلسه‌ای روی التزامی کار کرد، این جفت را نگه دار.
- **تلهٔ پیش‌فرضِ `be` خطرناک‌ترین نیمهٔ ستون بود** و در واحد ۱۳ ستون خودش را گرفت: چون `be` تنها فعلی است که فارسی‌زبان دیده که ته جمله می‌ایستد (`yes, it is`)، هر حذفی به `when they aren't, it isn't` می‌لغزد. این تله در واحدهای ۱ و ۲ هم در `misses` آمد.
- **ضمیر بازگشتی پنج واحد** (۹ حرف اضافهٔ آویزان، ۱۱ داخل `what`، ۲۱ بعد از `whatever`، ۲۲ داخل موصولی بی `that`، و ۲۳ که **عکسش** را نشان می‌دهد: `the weight we have given it` که `it` در آن مفعول دوم است و باید بماند). واحد ۲۳ عمداً بعد از چهار واحد «حذفش کن» آمده تا قاعده مکانیکی نشود. ردیف از ۱۲ به ۱۷.
- **منفی دوگانه پنج واحد** (۲، ۱۳، ۱۹ `nor did` بی `didn't`، ۲۰ `no one had objected`، ۲۵ `until` + `no change`) و ردیف از ۶۰ به ۶۵. قاعدهٔ واحد ۲۰ صریح می‌گوید «در هر بند یک سهمیهٔ منفی داری».
- **تله‌های تازه‌ای که ارزش برگشتن دارند:** `than what` از «از چیزی که» (۵)، `right on a point` (۶)، `spend + ing` در برابر `spend + on` در یک متن (۳ و ۹)، `a piece of work` (۹)، `in a test` در برابر `for an essay` (۱۰)، جای `only` که معنا را عوض می‌کند (۱۱)، `make` که در قالب سببیِ `V3` نمی‌نشیند (۱۲)، نردبان `suggest / indicate / show / prove` (۱۴)، خانوادهٔ `scrap / lift / abolish / remove` برای «برداشتن» (۱۶)، `others` در برابر `other` (۱۷)، `something else` و جای `else` (۱۸)، `too` در برابر `either` (۱۹)، `at a meeting` در برابر `in` (۲۰)، دو شکل `whatever` (ضمیری و تعیین‌کننده‌ای) در ۲۱، `by` در برابر `until` (۲۲)، و انعکاسیِ تأکیدی در برابر انعکاسیِ مفعولی (۲۳).
- **واحد ۲۵ چهار تلهٔ برگشتی را روی هم گذاشت** — `be to` در بند شرط بی `will`، سؤال درون‌نشانده (سومین بار در همین متن بعد از ۷ و ۸)، منفیِ فارسی بعد از «تا وقتی»، و منفی روی اسم به‌جای فعل — به‌علاوهٔ بستن ستون با `do so`. همان کاری که واحد ۲۵ متن‌های ۴ و ۵ و ۶ کرد.
- **در این متن صفر ماندند:** اعداد و درصد (۳۵، عمدی و سومین متن پیاپی)، و هیچ ردیف دیگری صفر نماند. کم‌ترین‌ها: شرطی (۳۵ با ۱ واحد)، فاعل صوری (۴۴ با ۱ واحد)، سببی (۱۸ با ۱ واحد)، وارونگی (۱۹ با ۱ واحد)، التزامی (۱۷ با ۱ واحد)، `-ever` (۱۶ با ۱ واحد).
- **برای متن ۸ (زبان نوشتاری در عصر پیام‌رسان):** ستون ثبت (register) است — ردیفش با ۴ واحد از این متن از ۳ به ۷ رسیده، پس از صفر شروع نمی‌کند ولی هنوز سبک‌ترین ردیف دورهٔ ۳ است و **دست‌کم ۶ واحد لازم دارد**. چیزهایی که در متن ۷ به ثبت خورد و می‌شود ازشان ادامه داد: `do so` در برابر `do it` (۳)، `do` تأکیدی در برابر `really` (۶)، `of course` که به خواننده برمی‌خورد (۱۱)، و `though` در برابر `although` جلوی عبارت (۲۲). لایهٔ ۳: `-ever` (۱۶) و نقطه‌گذاری (۶۱ — خیلی جلوتر از هدف، پس فقط جاهایی که طبیعی است). موضوع پیام‌رسان، اختصار و علامت حذف و جملهٔ ناقص و ضمیر افتاده را طبیعی می‌آورد، و **حذف فاعل** — «قاعدهٔ وارونهٔ» جزوه که نیمهٔ دیگر ستون متن ۷ است و در این متن اصلاً نیامد — دقیقاً همان‌جا زنده می‌شود. شرطی (۳۵) و hedging (۴۹) در دو متن پیاپی هر کدام یکی دو واحد گرفته‌اند؛ ستون متن ۹اند، پس اگر در متن ۸ جا نیفتادند مشکلی نیست.
- **متن ۸ (۲۵ واحد):** زبان نوشتاری در عصر پیام‌رسان — گلهٔ معلم‌ها، اینکه شکایت سر املا نیست سر ثبت است، ثبت به‌عنوان یک بستهٔ انتخاب، آنچه پیام‌رسان پاداش می‌دهد (سرعت، به بهای حذف)، شواهدی که آسیب به نمرهٔ امتحان را تأیید نمی‌کنند، و نتیجه‌گیری که مسئله جابه‌جا شدن است نه دانستن. متن یک مقالهٔ استدلالی است که **داخلش پیام نقل می‌شود** — همین تقابل درون‌متنی جای طبیعی ستون را ساخت.
- **ستون (ثبت) ۱۰ واحد و ردیف از ۷ به ۱۷ رسید** و بالاخره از هدف گذشت: نقطه‌ویرگول در برابر ویرگول (۲)، `simply` در برابر `just` (۳)، `ones` به‌جای تکرار اسم (۵)، فهرست با ویرگول نه با «و»های پشت سر هم (۶)، `such as` در برابر `like` (۸)، `However` در برابر شروع با `But` (۱۰)، مجهول در برابر `they`ی عام (۱۲)، `do` تأکیدی در برابر `really` (۱۳)، `Of course` که به خواننده برمی‌خورد و `Admittedly` به‌عنوان بدیل (۱۸)، و `though` در برابر `although` (۲۲). همهٔ چهار موردی که یادداشت متن ۷ سفارش کرده بود آمدند.
- **حذف فاعل — «قاعدهٔ وارونه» — در دو واحد و از دو جهت:** واحد ۲ نشان می‌دهد انگلیسیِ نوشتاری فاعل را نمی‌اندازد (`; is about register` غلط است)، و واحد ۸ نشان می‌دهد پیام‌رسان دقیقاً همان را می‌اندازد (`on my way, be there in ten`). این جفت عمدی است: یک قاعده، یک بار به‌عنوان ممنوعیت و یک بار به‌عنوان گونهٔ دیگر.
- **`-ever` چهار واحد و هر بار با کلمهٔ متفاوت:** `No matter how many` (۹)، `However quickly` بی‌کاما (۱۵)، `Whatever platform` به‌عنوان تعیین‌کننده (۱۷)، `Whichever way` در بند تعمیمِ بی‌`will` (۲۵). ردیف از ۱۶ به ۲۰ و از هدف گذشت. **واحد ۱۵ عمداً پنج واحد بعد از ۱۰ آمد** تا دو `however` — تقابلیِ کامادار و امتیازدهندهٔ بی‌کاما — در یک متن و نزدیک هم دیده شوند؛ هر دو واحد صریح به این تقابل اشاره می‌کنند.
- **نقطه‌گذاری پنج واحد:** نقطه‌ویرگول (۲ و ۱۷)، جفت ویرگول دور معترضه و ممنوعیت ویرگول بین فاعل و فعل (۵)، خط تیرهٔ بلند برای فکر بعدی (۹)، و `however`ی محاصره‌شده با دو ویرگول (۱۰). comma splice در `misses` هفت واحد مختلف برگشت.
- **`do` تأکیدی بالاخره از ۱۵ به ۱۶ رفت** (واحد ۱۳، `do know` در برابر `really know`). هنوز عقب‌ترین ردیف لایهٔ ۳ است و متن ۹ ستونش است.
- **منفی دوگانه چهار واحد** (۸ `nobody is`، ۱۶ `without ever`، ۱۸ `never … at all`، ۲۴ `nor that`) و ردیف از ۶۵ به ۶۹. هر بار از یک زاویهٔ متفاوت: ضمیر نامعین، حرف اضافهٔ منفی، قید تشدیدیِ غیرمنفی، و حرف ربط منفی.
- **تله‌های تازه‌ای که ارزش برگشتن دارند:** `students’` در برابر `student’s` در واحد اول متن (۱)، `lack` متعدی در برابر `a lack of` (۴)، `each`ی شناور که مطابقت را عوض نمی‌کند (۶)، `by + ing` در برابر `with + اسم` (۷)، `both` که جای اشتباه می‌نشیند (۸)، `go unnoticed` که ساختش ممکن نیست و باید حفظ شود (۱۱)، `little` در برابر `a little` که ادعا را برعکس می‌کند (۱۴ و ۲۲)، `evidence that` که هیچ‌وقت `which` نمی‌شود (۱۴)، `the same` که همیشه `the` دارد (۱۷)، **`read like` با شکل معلوم و معنای مجهول** (۱۸ — تازه‌ترین تلهٔ متن و دقیقاً برعکس چیزی که تمرین‌های مجهول یاد داده‌اند)، `very` که تفضیلی را تشدید نمی‌کند (۲۰)، صفت مرکب که جمع بسته نمی‌شود (۲۱)، `anyone` بعد از `before` (۲۳)، و `none of this` مفرد در برابر `none of these` (۲۴).
- **واحد ۱۷ هم یک نکتهٔ وارونگی را بدون خرج کردن یک واحد کامل جا داد:** `only` روی فاعل وارونگی نمی‌سازد، فقط `only` + قید می‌سازد. ردیف وارونگی (۱۹) دست‌نخورده ماند و برای متن ۱۰ محفوظ است.
- **در این متن صفر ماندند:** سببی (۱۸)، نقل قول و backshift (۳۶)، وارونگی (۱۹)، التزامی (۱۷)، اعداد و درصد (۳۵)، شرطی (۳۵)، فاعل صوری (۴۴)، ماضی بعید (۳۹)، «حرف اضافه + اینکه» (۲۱). **سببی، وارونگی و التزامی عقب‌ترین‌های لایهٔ ۳ باقی مانده‌اند و هیچ‌کدام ستون متن ۹ نیستند — پس عملاً همه‌شان به متن ۱۰ می‌افتند، که ستونش همین است.**
- **برای متن ۹ (Task 2: ممنوعیت تبلیغ غذای ناسالم برای کودکان):** ستون نردبان الزام و hedging در استدلال است؛ لایهٔ ۳ التزامی و `do` تأکیدی. **`do` تأکیدی (۱۶) و التزامی (۱۷) دو تا از سه ردیف عقب‌ماندهٔ پروژه‌اند و ستون لایهٔ ۳ همین متن‌اند — هر کدام دست‌کم ۲ واحد.** موضوعِ «باید/نباید ممنوع شود» هر دو را طبیعی می‌آورد: `should be banned` و `must`، و `recommend / propose / demand that + مصدر لخت` برای توصیه‌های سیاستی. شرطی (۳۵) و فاعل صوری (۴۴) هم در متن ۸ صفر ماندند و در متن استدلالی جا می‌افتند (`If advertising were banned…`، `It is often argued that…`). `although / despite / however` (۴۳) در متن ۸ فقط دو واحد گرفت و متن ۹ جای طبیعی‌اش است. **اعداد و درصد (۳۵) چهار متن پیاپی صفر مانده — اگر ادعای آماری در استدلال جا افتاد، یکی دو واحد بگذار.** سببی را نگه دار برای متن ۱۰.

- **متن ۹ (۲۵ واحد):** ممنوعیت تبلیغ غذای ناسالم برای کودکان، به سبک Task 2 — چهار پاراگراف: معرفی موضوع و دو دیدگاه (نقل نظر، آمار «یکی از هر پنج کودک» انگلستان، آنچه روشن نیست، حرف موافقان و حرف مخالفان)، دلیل موافقان (کودک شش‌ساله و شعار تبلیغاتی، شواهد چند کشور، شرطی منطقی، اصلِ وظیفهٔ دولت، خواستهٔ نهادهای پزشکی، توصیهٔ ساعت نُه، شیلی و کانادا)، طرف دیگر (امتیاز دادن به اثر تبلیغ، فرضِ ناپدید شدنش، قیمت و اندازهٔ وعده، فضای آنلاین، قواعدی که یک دهه زودتر نوشته می‌شدند، دفاع از ممنوعیتِ ناکامل، هشدار به موافقان، شواهد شیلی)، و نتیجه‌گیری. اعداد فقط «یکی از هر پنج» و «نُه شب» و «یک دهه» است.
- **ستون (نردبان الزام و hedging) در ۱۱ واحد و کل نردبان از هر دو سر آمد:** `should be banned` برای پیشنهاد سیاستی (۱)، `must protect` برای اصل و ضرورت منطقی (۱۰)، `does not have to be complete` برای «لازم نیست» (۱۹)، `must not promise` برای «نباید» (۲۰)، و `should not be blamed` برای قضاوت استدلالی (۲۴). **واحد ۱ تست عملی ستون را صریح می‌دهد** («هر بار که فارسی «باید» گفت، بپرس از کدام پله است») و **واحد ۱۰ همان تست را از جهت مخالف می‌زند** — یعنی «در Task 2 همیشه should» قاعدهٔ یک‌طرفه نیست. hedging: `Few people would deny` (۲)، `roughly` (۳)، `cannot reasonably be expected` (۵)، `suggests` نه `proves` (۸)، `seems likely to` + هشدار سه‌لایه شدن (۲۲)، `though not as clearly as` (۲۱).
- **واحد ۱۹ و ۲۰ عمداً پشت سر هم‌اند** چون `must not` و `do not have to` مثبت‌هایشان هم‌معنا و منفی‌هایشان مخالف‌اند، و فارسی این تمایز را **دارد** («نباید» / «لازم نیست») — پس تله ریشهٔ زبان مادری ندارد و فقط نگاشت می‌خواهد. همین سه واحد پیاپی (۱۹، ۲۰، ۲۴) سه پلهٔ متفاوت گرفتند. بهترین جفت متن است و ارزش برگشتن دارد.
- **التزامی بالاخره به هدف رسید — ۳ واحد و هر سه با یک لباس متفاوت:** مجهول بعد از فعل، بی backshift با فعل اصلیِ حال کامل (۱۱ `demanded that the current rules be tightened`)، منفیِ بی `do` (۱۲ `recommend that broadcasters not run`)، و محرک صفتی با فاعل مفرد و فعل معلوم (۲۳ `It is essential that any new rule cover`). واحد ۲۳ عمداً سخت‌ترین حالت است چون نبودن `-s` آنجا «غلط دیده می‌شود». ردیف از ۱۷ به ۲۰. در هر سه واحد سه گزینهٔ جزوه (التزامی / `should` / `for + to`) در `alt` آمد.
- **`do` تأکیدی ۲ واحد و با دو کارِ متفاوت:** امتیاز دادن به حرف طرف مقابل و بعد `but` (۱۴ `does influence … but`)، و دفاع از حرف خود گوینده در برابر تردیدی که در همان پاراگراف ساخته شده (۲۱ `does point in one direction`). واحد ۱۴ محدودیت خانواده را هم می‌گوید (`do` با `be` جمع نمی‌شود) و هر دو واحد تقابل با `really` / `certainly` را صریح می‌زنند — یکی «پذیرش» می‌گوید و دیگری «شدت». ردیف از ۱۶ به ۱۸.
- **شرطی ۵ واحد و سه پلهٔ نردبان فاصله:** پلهٔ صفر با تست جایگزینی `when` (۹)، پلهٔ یک با `Even if` و «فردا» در کنار فعل گذشته (۱۵)، پلهٔ دو در مجهولِ سه‌لایه و نامتقارن (۱۸ `had been written` / `would have been drafted`)، به‌علاوهٔ `provided that` + حال ساده (۲۲) و بند تعمیمیِ `Whether or not` بی `will` (۲۵). ردیف از ۳۵ به ۴۰. واحد ۱۸ در `alt` وارونگیِ `Had the rules been written` را داد ولی شمرده نشد.
- **ضمیر بازگشتی از دو جهت و در فاصلهٔ کنترل‌شده:** واحد ۸ نشان می‌دهد `it` **لازم** است (`the advertisement that interrupts it` — مرجعش اسم دیگری است)، واحد ۱۷ و ۲۴ نشان می‌دهند باید **حذف** شود (`over which national rules have far less power`، `that they had no part in creating`). هر سه واحد صریح به هم ارجاع می‌دهند. همان تکنیک واحد ۲۳ متن ۷ و ردیف از ۲۰ به ۲۳.
- **`although / despite / however` بالاخره ۴ واحد گرفت** (۷ `Although` در برابر دونشانه‌ایِ `although … but`، ۱۹ `Despite` + گروه اسمی و ممنوعیت `despite of`، ۲۰ `Even so` به‌عنوان قید ربطیِ «پذیرش سپس تقابل»، ۲۱ `though not` که `although` جایش نمی‌نشیند) و ردیف از ۴۳ به ۴۷. `Even so` و `though not` هر دو تازه بودند.
- **تله‌های تازه‌ای که ارزش برگشتن دارند:** `rest with` در برابر `rest on` و `decision about` در برابر `of` (۶)، `tell the two apart` با مفعول وسط (۹)، الگوی ممنوعِ `demand somebody to` (۱۱)، ساعت با `in the evening` و ممنوعیت `before at nine` (۱۲)، تکرار حرف اضافه در `in Chile and in parts of` (۱۳) و در `as much in … as in …` (۱۶)، `lie in` در برابر `lie with` در یک متن (۶ و ۱۶)، `far less` در برابر `very less` و `less` در برابر `fewer` با ناشمار (۱۷)، `a decade earlier` نه `ago` داخل قاب گذشته (۱۸، دومین بار بعد از متن ۷)، `alone` بعد از اسم به‌جای جای‌گیریِ دردسرسازِ `only` (۱۸)، `point in` در برابر `point to` (۲۱)، `seems likely to` در برابر `seems probably` (۲۲)، `as well as` که مطابقت را عوض نمی‌کند (۲۳)، `blame X for Y` در برابر `blame Y on X` (۲۴)، و `the harder work` که تفضیلیِ دوتایی `the` می‌خواهد (۲۵).
- **واحد ۲۵ چهار تلهٔ برگشتی را روی هم گذاشت** — بند تعمیمی بی `will`، منفیِ دوم که با `or not` حذف می‌شود، `the` روی تفضیلی، و فعل مرکب فارسی («ارزان کردن») که به `make + صفت` می‌رود، به‌علاوهٔ جفت خط تیرهٔ بلند و معترضهٔ `-ing` که باید هم‌نقش گروه اسمی باشد. همان کاری که واحد ۲۵ متن‌های ۴ تا ۷ کرد.
- **در این متن صفر ماندند:** سببی (۱۸)، وارونگی (۱۹)، `-ever` / `no matter` (۲۰). هر سه به هدف رسیده‌اند ولی سه‌تا از عقب‌ترین ردیف‌های لایهٔ ۳ باقی مانده‌اند.
- **برای متن ۱۰ (جمع‌بندی: کتابخانه‌های عمومی و آیندهٔ کتاب):** ستونش مرور هر نُه خانوادهٔ تلهٔ دوره است و لایهٔ ۳ آن وارونگی، سببی و `-ever`. **این سه تنها ردیف‌هایی هستند که از متن ۸ به این طرف هیچ واحدی نگرفته‌اند — هر کدام دست‌کم ۲ واحد، و این آخرین فرصت است.** وارونگی (۱۹) دو بار در `alt` رد شد (متن ۸ واحد ۱۷، این متن واحد ۱۸ با `Had the rules been written`) و باید بالاخره واحد کامل بگیرد. از خانواده‌های ستون دوره، سبک‌ترین‌ها **اسم‌سازی (۱۷)**، **جای ذره (۲۱)** و **ثبت (۲۱)** هستند، پس متن ۱۰ باید مرورش را روی همین سه سنگین‌تر کند تا روی خانواده‌هایی که از ۴۰ گذشته‌اند. اعداد و درصد (۳۶) پنج متن است که تقریباً دست‌نخورده مانده؛ موضوع کتابخانه (آمار امانت، بودجه، تعداد شعبه‌های تعطیل‌شده) طبیعی‌ترین جایی است که می‌شود دو سه واحد عددی گذاشت. جفت درخشان این متن — `must not` در برابر `do not have to` در دو واحد همسایه — الگوی خوبی است برای جفت‌های متن آخر.

- **متن ۱۰ (۲۵ واحد) — دورهٔ ۳ تمام شد.** کتابخانه‌های عمومی و آیندهٔ کتاب — چهار پاراگراف: ارقام تعطیلی و امانت و استدلال منتقدان (۱ تا ۴)، آنچه کتابخانه علاوه بر کتاب می‌دهد و اثر بسته شدنش (۵ تا ۸)، واکنش شوراها و پیچیدگی کتاب الکترونیکی و ترجیح کاغذ (۹ تا ۱۶)، و نتیجه‌گیری سیاستی (۱۷ تا ۲۵). آمارها عمداً تقریبی و محتاطانه‌اند («بیش از ۷۰۰»، «حدود یک‌سوم»، «حدود دوسوم»).
- **سه ردیف اجباری لایهٔ ۳ به هدف رسیدند.** وارونگی ۳ واحد و هر بار آغازگر متفاوت: `Not only does` (۱۶)، `Only by … can` (۱۹)، `Had` در شرطی پلهٔ سوم (۲۲). سببی ۲ واحد با دو الگو: `have + شخص + V1` (۹) و `have + چیز + V3` (۱۵) که واحد ۱۵ صریح به ۹ ارجاع می‌دهد. `-ever` ۳ واحد: `Whatever` + اسم (۴)، `However` + صفت بی‌ویرگول (۲۰)، و واحد ۲۵. اعداد و درصد ۳ واحد (۱، ۲، ۱۴)، بعد از پنج متن.
- **جفت‌های واحد ۳ و ۲۵ عمداً متقابل‌اند:** در ۳ ضمیر باید بیفتد (`an expense the country can no longer afford`) و در ۲۵ باید بماند (`somewhere to read it`)، چون اسم جلویی مفعول نیست، مکان است. واحد ۲۵ هر دو را صریح می‌گوید و به‌عنوان تلهٔ «قاعده را کورکورانه پیاده کردن» معرفی می‌کند. در همان واحد `will` در بند `-ever` و `how` تنها هم برگشتند. جفت ۱ و ۲ هم متقابل است: `between … and` برای پنجرهٔ بسته و `since` برای بازهٔ باز.
- **ردیف‌های سبک دورهٔ ۳ کمتر از انتظار جلو رفتند.** اسم‌سازی (۱۷ ← ۱۹) فقط دو واحد گرفت (۸ و ۲۲ که `cost of closure` در آن فرعی است)؛ در `alt`ِ ۲ و ۱۱ هم آمد ولی شمرده نشد. ثبت (۲۱ ← ۲۴) و وزن و ترتیب (۲۲ ← ۲۴) هر کدام ۲ تا ۳ واحد. این سه هنوز عقب‌ترین ردیف‌های دورهٔ ۳ هستند و بهترین کاندیدای ستون در دورهٔ ۴.
- **در این متن صفر ماندند:** `do` تأکیدی (۱۸)، وجه التزامی (۲۰)، نقل قول و backshift (۳۷)، صفت مرکب (۲۷). **این چهار عقب‌ترین ردیف‌های کل پروژه‌اند و در دورهٔ ۴ باید ستون یا لایهٔ ۳ باشند.** `do` تأکیدی از متن ۹ (۱۸) به این طرف ۰ گرفته و التزامی هم؛ بازگشتشان بدون ستون بودن ممکن نیست.
- **تله‌های تازه که ارزش برگشتن دارند:** «اقتصادی» فارسی که هم `economic` است هم `economical` (۲۱)، «تمام شدن» که برای موجودی `run out of` می‌شود نه `finish` (۱۳)، «بدهند … بیاورند» که لفظ‌به‌لفظ `give … to bring` می‌شود (۱۵)، `affect` بی `on` (۷)، `probably will` با قید قبل از کمکیِ تنها در حذف (۲۴)، «با اینکه» که این‌جا `by` است نه `although` (۱۹)، `close` ناگذر در برابر `closure` (۸، ۲۲)، `committed to saving` با تست ضمیر `to it` (۲۳)، و `fill them in` با ضمیر وسط و ذره‌ای که در مجهول با فعل می‌ماند (۶).
- **برای دورهٔ ۴:** طبق دستور اسکیل، نقشهٔ تازه باید قبل از تولید متن اول تأیید شود. نقشه را از روی چهار ردیف صفر (`do` تأکیدی، التزامی، backshift، صفت مرکب) و سه ردیف سبک (اسم‌سازی، ثبت، وزن جمله) بچین.

- **متن ۱ دورهٔ ۴ (۲۵ واحد) — دورهٔ ۴ شروع شد.** فایل `texts/04/drill.html` از قالب ساخته شد (`STORE_KEY = "ielts-d4"`). خواب نوجوانان و ساعت شروع مدرسه — چهار پاراگراف: پژوهش و یافته‌اش (۱ تا ۶)، توضیح زیستی (۷ تا ۱۲)، مدرسه‌هایی که ساعت را عقب انداختند و نتیجه‌اش (۱۳ تا ۲۰)، و اعتراض‌ها و نتیجه‌گیری (۲۱ تا ۲۵). آمارها عمداً تقریبی‌اند («کمتر از هفت ساعت»، «حدود یک‌پنجم»).
- **ستون در ۱۰ واحد و کل جدول backshift از هر دو سر آمد.** سؤال نقل‌شده با حذف `do` و ترتیب خبری (۱)، حال کامل ← ماضی بعید (۴)، حقیقتِ هنوز برقرار که backshiftش اختیاری است (۵)، گذشتهٔ ساده ← ماضی بعید (۱۴)، `will → would` (۱۵)، `must → would have to` (۱۶)، و حال استمراری ← گذشتهٔ استمراری (۱۲). در برابرشان دو واحد نشان می‌دهند کِی عقب **نمی‌روی**: فعل نقلِ حال کامل (۷) و حال ساده (۲۴).
- **واحدهای ۴، ۷ و ۲۴ عمداً یک زنجیرهٔ سه‌تایی‌اند و هر سه صریح به هم ارجاع می‌دهند.** حرفشان یکی است: **شکلِ فعل نقل تصمیم می‌گیرد، نه معنا یا قدمتِ حرف.** این بهترین جفت‌سازی متن است و الگویش برای متن ۹ (که backshift لایهٔ ۳ آن است) ارزش تکرار دارد.
- **واحد ۱ و ۲۴ هم جفت‌اند، از زاویهٔ دیگر:** بند پرسشیِ داخل جمله همیشه ترتیب خبری می‌گیرد، چه بعد از فعل نقل باشد (۱) و چه مفعول یک فعل معمولی (۲۴) و چه بعد از حرف اضافه (۲۵ `about who should pay`). سه بار، سه جای متفاوت.
- **صفت مرکب ۲ واحد ولی واحد ۱۷ تقابل کامل را در یک جمله داد:** `two years later` در کنار `a two-year trial` — همان کلمه، یک بار اسم مستقل و جمع، یک بار داخل صفت و مفرد. واحد ۸ (`fifteen-year-old`) هم در `alt` نشان داد که همان صفت وقتی خودش اسم شود دوباره جمع می‌بندد (`fifteen-year-olds`). ردیف از ۲۷ به ۲۹؛ متن ۴ ستونش است و باید سنگین‌تر برگردد.
- **مالکیت ۴ واحد و هر بار یک شکل متفاوت:** جمعِ منتهی به s (۶ `teenagers'`)، جمعِ با تغییر املا (۲۰ `families'`)، ممنوعیت آپاستروف برای جمع (۴ `the nineties`)، و ملکیِ بی‌آپاستروف (۲۱ `its`). تست دو مرحله‌ای جزوه («اول جمع را بساز، بعد آپاستروف») در ۶ آمد و در ۲۰ عمداً تکرار شد چون املا وسط راه عوض می‌شود.
- **واحد ۲۱ (`not every school can`) بهترین تلهٔ تازهٔ متن است** و ارزش برگشتن دارد: فارسی و انگلیسی دقیقاً یک ساختار دارند ولی متفاوت تفسیرش می‌کنند، و خطا هیچ‌وقت خودش را لو نمی‌دهد چون جملهٔ حاصل کاملاً دستوری است. دامنهٔ نفی ردیف مستقلی ندارد و زیر «منفی دوگانه» شمرده شد.
- **تله‌های تازه‌ای که ارزش برگشتن دارند:** `a night` به معنای «در هر شب» (۲)، صفت‌های `a-`دار که قبل از اسم نمی‌آیند — `asleep`، `afraid`، `alive` (۸، ۱۵، ۲۵ و هر سه به هم ارجاع می‌دهند)، `two hours later` در برابر `after two hours` (۹)، ممنوعیت `because of this reason` (۱۰)، `explain to somebody` (۷)، `stress on` (۲۰)، `decide about` با بند (۲۴)، `get home` بی حرف اضافه (۱۵)، `they`ی مفرد وقتی جنسیت معلوم نیست (۱۵)، `none of this` که همیشه مفرد است (۲۰)، و `the first three months` در برابر ترتیب فارسی (۱۴).
- **در این متن صفر ماندند:** سببی (۲۰)، وارونگی (۲۲)، وجه التزامی (۲۰)، `do` تأکیدی (۱۸)، `-ever` (۲۳)، جانشینی و حذف (۷۲)، فاعل صوری (۴۸)، اسم‌سازی (۱۹)، ثبت (۲۴). اسم‌سازی چهار بار در `alt` آمد (۴، ۲۳، ۲۴) ولی شمرده نشد — و دقیقاً ستون متن ۲ است.
- **برای متن ۲ (پول نقد و پرداخت دیجیتال):** ستونش اسم‌سازی است و ردیفش (۱۹) عقب‌ترین ردیف دورهٔ ۳ بود؛ دورهٔ ۳ نشان داد که در `alt` آوردنش کافی نیست، پس این‌بار باید در بخش «متغیر» یا «تله» توضیح داده شود، دست‌کم ۸ واحد. لایهٔ ۳ آن اعداد و درصد (۴۲) و جانشینی و حذف (۷۲) است؛ موضوع پرداخت طبیعی‌ترین جا برای درصد و روند است. **`do` تأکیدی (۱۸) و التزامی (۲۰) هنوز عقب‌ترین ردیف‌های لایهٔ ۳اند** و ستون متن‌های ۵ و ۶ هستند — تا آنجا صبر کن و در متن ۲ زورکی جا نده.

- **متن ۲ دورهٔ ۴ (۲۵ واحد):** پول نقد و پرداخت دیجیتال — چهار پاراگراف: افول نقد و ارقامش (۱ تا ۶)، چرا (راحتی، هزینهٔ نگهداری نقد، تعطیلی شعبه‌ها) (۷ تا ۱۳)، چه کسانی جا می‌مانند و تجربهٔ سوئد (۱۴ تا ۲۰)، و نتیجه‌گیری (۲۱ تا ۲۵). آمارها عمداً تقریبی‌اند («شش از هر ده»، «یکی از هر هفت»، «بیش از یک‌سوم»، «یکی از هر دوازده»).
- **ستون در ۱۳ واحد آمد و ردیف اسم‌سازی از ۱۹ به ۳۲ رفت** — یعنی دستور جلسهٔ قبل («در `alt` آوردنش کافی نیست») اجرا شد و در همهٔ این واحدها در «متغیر» یا «تله» توضیح داده شده. اسم‌سازی عمداً به شش <b>کارِ</b> متفاوت تقسیم شد تا تکرار یک درس نباشد: نهاد کردن فرآیند (۴)، جمع کردن فهرست مصدرهای فارسی زیر یک اسم (۹)، بلعیدن بندِ «چون» (۱۳)، جایگزین کردن بندِ «اینکه» در جایگاه نهاد (۱۴)، اسم کردن یک بندِ کامل با gerund مجهول (۱۶)، و اسم کردن صفت (۷ `convenience`).
- **مکانیزم مرکزی متن، در واحدهای ۱ و ۱۹ و ۱۶ سه بار و هر بار در قالبی دیگر آمد: وقتی فعل اسم شد، قید هم باید صفت شود.** واحد ۱ در قالب سادهٔ `be + adj`، واحد ۱۹ داخل `so … that` (سخت‌ترین حالت، چون `so` جمله را آشنا نشان می‌دهد و `so quickly` درست به نظر می‌رسد)، و واحد ۱۶ با صفت `-ing`دار (`a growing sense`). هر سه صریح به هم ارجاع می‌دهند. این بهترین زنجیرهٔ متن است.
- **واحد ۲۳ عمداً خلاف ستون است** و «هشدار تعادل» جزوه را اجرا می‌کند: نشان می‌دهد وقتی تقابل بین دو سؤال است، اسم‌سازی متن را بدتر می‌کند. `alt` همان واحد نسخهٔ کاملاً اسمی را می‌دهد و می‌گوید هر دو درست‌اند و فقط مخلوط کردنشان غلط است. بدون این واحد، متن می‌توانست عادتِ اسم‌سازیِ بی‌جا بسازد؛ در متن‌های بعدی که ستون سبکی دارند همین تکنیک (یک واحد ضدِ ستون) ارزش تکرار دارد.
- **جفت‌های عمدیِ متن:** واحد ۱۲ فعل `close` و واحد ۱۳ اسم `closure` — همان ریشه، دو جایگاه، دو واحد پشت سر هم و با ارجاع صریح. واحد ۱۹ و ۲۲ هر دو نشان می‌دهند اسم‌سازی حرف اضافهٔ فعل را نگه می‌دارد (`withdraw from` ← `the withdrawal of … from`، `return to` ← `a return to`). واحدهای ۱۸ و ۲۱ دو بار پشت سر هم مجهولِ برآمده از سوم‌شخص جمعِ بی‌فاعل‌اند، و در هر دو جای قید هم یکسان است.
- **اعداد و درصد ۴ واحد** (۲، ۳، ۱۲، ۱۵) با چهار شکل متفاوت نسبت: `six out of every ten`، `one in seven`، `a third of`، `one adult in twelve`. **جانشینی و حذف ۵ واحد** (۶ `neither was` با وارونگی، ۱۰ کمکیِ مشترک برای چهار V3، ۱۳ `than it once was`، ۲۴ `do so`، ۲۵ موصولیِ کوتاه‌شدهٔ `those least able`). هر دو ردیف لایهٔ ۳ این متن به هدف رسیدند.
- **تله‌های تازه که ارزش برگشتن دارند:** `in cash` / `by card` / `with a card` — سه حرف اضافه برای یک «با»ی فارسی (۲)؛ `by` در برابر `until` که هر دو «تا»اند و اولی ماضی بعید می‌آورد (۳)؛ `fall to` در برابر `fall by` (۳)؛ `a second or two` که ترتیب فارسی را برعکس می‌کند (۸)؛ `take` / `spend` / `last` که فاعل انتخابشان می‌کند (۸)؛ `close` ناگذر در برابر `have been closed`ِ مجهول‌نما از «تعطیل شده‌اند» (۱۲)؛ `since` تنها به‌جای «از آن زمان تا حالا» (۱۲)؛ مطابقت کسری که از بعدِ `of` می‌آید نه از هسته (۱۲ — تنها جایی که قاعدهٔ هسته کوتاه می‌آید)؛ `at a rate` نه `with/in a rate` (۱۱)؛ `than any other` + مفرد (۱۷)؛ `alarmed` در برابر `alarming` (۱۹)؛ و `access to` بی حرف تعریف (۲۴).
- **در این متن صفر ماندند:** سببی (۲۰)، وجه التزامی (۲۰)، `do` تأکیدی (۱۸)، نقل قول و backshift (۴۷)، صفت مرکب (۲۹)، `-ever` (۲۳)، ثبت (۲۴)، «حرف اضافه + اینکه» (۲۶)، شکل‌های آینده (۳۷). طبق دستور جلسهٔ قبل، `do` تأکیدی و التزامی عمداً دست‌نخورده ماندند چون ستون متن‌های ۵ و ۶ هستند.
- **برای متن ۳ (کار از راه دور و شهرهای کوچک):** ستونش **ثبت (register)** است و ردیفش (۲۴) حالا عقب‌ترین ردیف دورهٔ ۳ است — همان چیزی که در دورهٔ ۳ هم فقط ۲ تا ۳ واحد گرفت و به هدف نرسید. درس متن ۲ را همان‌جا تکرار کن: ردیفی که ستون است باید دست‌کم ۸ واحد بگیرد و در «متغیر» یا «تله» توضیح داده شود، نه در `alt`. موضوع «ایمیل کاری در برابر گزارش رسمی» طبیعی‌ترین جا برای فاصلهٔ ثبت است: کوتاه‌نویسی، فعل عبارتی در برابر فعل تک‌کلمه‌ای لاتین‌تبار، `get` در برابر `obtain`، `a lot of` در برابر `considerable`. لایهٔ ۳ آن سببی (۲۰) و افعال عبارتی (۱۰۳) است؛ سببی سه متن است که صفر مانده و موضوع کار از راه دور (`have the office redesigned`، `get staff to move`) جای طبیعی‌اش است — دست‌کم ۳ واحد.

- **متن ۳ دورهٔ ۴ (۲۵ واحد):** کار از راه دور و شهرهای کوچک — چهار پاراگراف: چه اتفاقی افتاد و چرا مردم از شهر بزرگ بیرون رفتند (۱ تا ۶)، شهرهای کوچک چه به دست آوردند و چه از دست دادند (۷ تا ۱۳)، عقب‌کشیدن کارفرماها و گزارش مشاوران (۱۴ تا ۲۰)، و نتیجه‌گیری سیاستی (۲۱ تا ۲۵).
- **ستون در ۱۱ واحد آمد و ردیف ثبت از ۲۴ به ۳۵ رفت.** دستور جلسهٔ قبل («ردیفی که ستون است باید در متغیر یا تله توضیح داده شود، نه در `alt`») اجرا شد. ثبت عمداً به شش <b>نشانهٔ</b> جدا شکسته شد تا یک درسِ تکراری نباشد: دستهٔ افعال عبارتی (۱)، مقدارنما (۲)، ضمیر عام (۴)، فعل ساده در برابر لاتین‌تبار (۷ `go up` ← `rise`، ۱۱ `say/need/more` ← `state/require/further`)، تعدیل ادعا (۱۸ `generally` نه `always`)، و پرهیز از پرسش بلاغی (۲۰ `The question of which side is right` نه `So who is right?`). به‌علاوه شروع با `But` و comma splice در ۱۹.
- **واحد ۱ عمداً از سمت وارونه شروع می‌کند:** تله‌اش «ندانستن» نیست، «زیادی دانستن» است — قاعدهٔ «فعل عبارتی در Writing ننویس» که کامل گرفته می‌شود و `carried out`ِ آکادمیک را قربانی می‌کند. سه دستهٔ جزوهٔ حروف اضافه در `rule` همان‌جا آمد و در ۷، ۱۸ و ۲۲ دوباره ارجاع داده شد.
- **جفت ۱۱ و ۱۲ بهترین ساختهٔ متن است و الگویش ارزش تکرار دارد:** یک گزارش رسمی و یک ایمیل داخلی، دربارهٔ یک موضوع، پشت سر هم. واحد ۱۱ سمت رسمی را می‌سازد و واحد ۱۲ نشان می‌دهد ثبتِ پایین‌تر در جای خودش <b>درست</b> است، نه ضعیف. واحد ۱۳ بلافاصله حرف را صریح می‌کند («فاصلهٔ دو متن دربارهٔ درستی چیزی نمی‌گوید، دربارهٔ مخاطب می‌گوید»). این همان تکنیک «یک واحد ضدِ ستون» است که در متن ۲ (واحد ۲۳) جواب داد — این‌بار به‌جای یک واحد، یک جفت.
- **واحد ۱۷ دامنهٔ قاعده را می‌زند، نه خود قاعده:** کوتاه‌نویسیِ `isn't` داخل نقل قول مستقیم درست است، چون قاعدهٔ ثبت روی صدای نویسنده اعمال می‌شود نه روی موادی که نقل می‌کند. `misses` اولش دقیقاً «اصلاح کردنِ نقل قول» است. نقطه‌گذاری گیومه هم در ۱۱ (نقل ناقص، نقطه بیرون) و ۱۷ (نقل کامل، نقطه داخل) عمداً متقابل آمد.
- **سببی ۴ واحد و هر چهار الگوی خانواده:** `have + چیز + V3` (۳ `had their systems redesigned`)، همان الگو در معنای ناخوشایند با `had had` دوتایی و backshift (۹ `had had his rent doubled`)، `make + شخص + V1` بدون `to` (۱۵)، و `allow + شخص + to + V1` در برابرش (۱۶). واحد ۱۵ بی‌قاعدگیِ مهم را هم می‌گوید: در مجهول، `to` به `make` برمی‌گردد (`were made to come`). ردیف از ۲۰ به ۲۴؛ به هدف رسید و در متن‌های بعدی فقط اگر خودش بیاید.
- **واحد ۱۵ و ۱۶ عمداً همسایه‌اند** چون معنایشان هم‌خانواده است (اجبار و اجازه) و الگویشان مخالف. همان کاری که `must not` / `do not have to` در متن ۹ دورهٔ ۳ کرد.
- **نقطه‌گذاری و comma splice ۱۰ واحد گرفت** — بیشترین در یک متن. `as a result` (۶)، کاما قبل از `and`ِ دو جملهٔ کامل (۸)، گیومه (۱۱، ۱۷)، دونقطه (۱۲)، نقطه‌ویرگول (۱۳، ۲۳، ۲۴)، `however`ِ میانی بین دو کاما (۱۹)، و معترضهٔ `at least in part` (۲۵). ردیف از ۸۲ به ۹۲.
- **تله‌های تازه که ارزش برگشتن دارند:** `a number of` جمع در برابر `the number of` مفرد (۱۴ — تنها جای دیگری بعد از کسرها که `of` مطابقت را عوض می‌کند)، `no longer` در برابر `any longer` و جایش بعد از <b>اولین</b> کمکی (۱۰ و ۲۴)، `rise by` در برابر `rise to` و `a rise of` (۷)، `far more` در برابر `very more` (۱۲)، `the very commute` به‌عنوان صفت تأکیدی (۲۲)، `move house` بی حرف تعریف (۲۳)، `be to + مصدر` برای «قرار است» در بند شرط (۲۱ — راهی برای آینده بدون `will` در بند شرط)، `two hours away` در برابر `with the office` (۱۶)، `somewhere cheaper` با صفت بعد از ضمیر نامعین (۵)، `made the difference` در برابر `made a difference` (۵)، و `further` در برابر `more` (۱۱).
- **افعال عبارتی ۶ واحد و ردیف از ۱۰۳ به ۱۰۹.** دستهٔ ۱ (۱ `carry out`، ۸ `set up`، ۱۸ `lead to`)، دستهٔ ۲ (۲۲ `end up with`)، و مجهولِ فعل عبارتی که ذره‌اش را نگه می‌دارد (۲۴ `be taken for granted`، ۲۵ `been handed over to`). واحدهای ۲۴ و ۲۵ صریح به هم و به `fill them in`ِ متن ۱۰ دورهٔ ۳ ارجاع می‌دهند.
- **در این متن صفر ماندند:** وارونگی (۲۲)، وجه التزامی (۲۰)، `do` تأکیدی (۱۸)، `-ever` / `no matter` (۲۳). طبق نقشهٔ دوره، `do` تأکیدی ستون متن ۵ و التزامی ستون متن ۶ است، پس عمداً دست‌نخورده ماندند. **وارونگی و `-ever` هیچ ستونی در این دوره ندارند** و باید در متن‌های ۷ تا ۱۰ به‌عنوان لایهٔ ۳ برداشته شوند، وگرنه همه‌شان به متن ۱۰ می‌افتد.
- **برای متن ۴ (پلاستیک یک‌بارمصرف و بسته‌بندی):** ستونش **صفت مرکب و گروه اسمیِ سنگینِ چپ‌چین** است و ردیف صفت مرکب (۳۰) سه دوره است که هر بار دو سه واحد می‌گیرد و می‌ایستد. درس متن‌های ۲ و ۳ را تکرار کن: دست‌کم ۸ واحد و همه در «متغیر» یا «تله». موضوع بسته‌بندی طبیعی‌ترین جا برای گروه اسمیِ چندلایه است (`single-use plastic packaging waste`, `a deposit-return scheme`, `food-grade recycled material`) و برای تلهٔ مفرد در صفت مرکب (`a five-litre container` در برابر `five litres`). لایهٔ ۳ آن `-ever` (۲۳) و نقطه‌گذاری (۹۲) است؛ `-ever` سه متن است که صفر مانده و ستونی هم نخواهد داشت — دست‌کم ۲ واحد. اسم‌سازی (۳۳) و وزن جمله (۳۰) هم از ردیف‌های سبک دورهٔ ۳ مانده‌اند و گروه اسمی سنگین جای طبیعی هر دوست.

- **متن ۴ دورهٔ ۴ (۲۵ واحد):** پلاستیک یک‌بارمصرف و بسته‌بندی — چهار پاراگراف: ابعاد مسئله و چرا بازیافت شکست می‌خورد (۱ تا ۶)، آنچه امتحان شده و مشکل هر کدام (۷ تا ۱۳)، ظرف‌های قابل استفادهٔ دوباره و جنگِ اعداد (۱۴ تا ۲۰)، و نتیجه‌گیری (۲۱ تا ۲۵).
- **ستون در ۱۲ واحد آمد و ردیف صفت مرکب از ۳۰ به ۴۲ رفت** — بزرگ‌ترین جهش این ردیف در کل پروژه، بعد از سه دوره که هر بار دو سه واحد می‌گرفت و می‌ایستاد. برخلاف دوره‌های قبل، هیچ‌کدام در `alt` نماند.
- **ستون عمداً به پنج قاعدهٔ جدا شکسته شد تا یک درس تکراری نباشد:** (۱) خط تیره وقتی دو کلمه با هم یک صفت می‌سازند و قبل از اسم می‌آیند — چهار خانواده‌اش هم جدا آمد: صفت+اسم (۱ `single-use`)، پیشوند+اسم (۵ `multi-layer`)، اسم+اسم (۷ `deposit-return`)، اسم+V3 (۱۰ `plastic-coated`). (۲) **بدون** خط تیره وقتی اسم صرفاً نقش توصیف گرفته (۴ `recycling bin`، ۱۸ `the packaging industry`، ۲۵ `the recycling rate`). (۳) مفردِ داخل صفت مرکب (۳ `a ten-year target`، ۱۵ `two-litre` و `half-litre` در کنار `ten bottles`). (۴) خط تیره بعد از اسم برداشته می‌شود (۱۷ `should be machine washable`). (۵) استثنای شیوه‌ای (۲۲ `a five per cent cut` بی خط تیره در بریتانیایی).
- **واحد ۸ (`drinks bottles`) عمداً بعد از سه واحدِ «مفرد بمان» آمد** تا نشان بدهد قاعده استثنا دارد: `drinks bottles`، `sports shoes`، `careers advice`، `arms race`، `savings account`. بخش تله همان‌جا می‌گوید خطا از «تعمیمِ بیش از حدِ قاعدهٔ تازه» می‌آید — همان تکنیک «یک واحد ضدِ ستون» که در متن ۲ (۲۳) و متن ۳ (۱۲) جواب داد، این بار داخل خودِ ستون.
- **نیمهٔ دوم ستون — کجا دست از فشرده‌سازی برداری — در ۳ واحد آمد:** واحد ۹ حدِ چپ‌چینی را می‌زند (`every market-bound non-recyclable packaging tonne` به‌عنوان `miss`) و قاعده‌اش را می‌دهد: **توصیف کوتاه و ثابت به چپ، توصیف بلند و موردی به راست**. واحد ۱۴ ترتیب صفت‌ها و کامای صفت‌های هم‌پایه، و واحد ۲۱ صفت‌هایی که بعد از اسم می‌نشینند (`the sheer quantity involved`, `the people involved`, `the money available`).
- **جفت عمدیِ ۲ و ۱۴ کامای صفت را از دو طرف می‌گیرد:** `a cheap, thin film` با کاما (هم‌پایه) در برابر `sturdy glass containers` بی کاما (انباشته)، با تست دومرحله‌ای (`and` جا می‌شود؟ جایشان عوض می‌شود؟). جفت ۱۷ و `alt` همان واحد هم یک صفت را در دو جایگاه می‌گذارد: `machine washable` بعد از `be`، `machine-washable containers` قبل از اسم.
- **`-ever` ۴ واحد و ردیف از ۲۳ به ۲۷ — سه نقش متفاوت و سه نقطه‌گذاری:** قیدی با قید و کامای آخرِ بند (۶ `However carefully residents sort…`)، **نهادی و بی کاما** (۱۲ `Whatever solution is chosen brings…`)، قیدی با اسم و کاما (۱۹ `Whatever figures the two sides produce,`)، و `No matter how` که فقط قیدی است و هرگز نهاد نمی‌شود (۲۴). واحد ۲۴ حذفِ `is` در نوشتار رسمی را هم می‌دهد (`No matter how strict the ban,`).
- **جفت ۶ و ۱۱ بهترین تقابل متن است:** `However carefully…` بی کاما در برابر `The coating, however, is…` با دو کاما — یک کلمه، دو دستور زبان. نشانهٔ تشخیص صریح آمد: بعد از `However` یا کاما می‌آید یا صفت، هرگز هر دو و هرگز هیچ‌کدام.
- **نقطه‌گذاری ۱۲ واحد و ردیف از ۹۲ به ۱۰۴** — باز بیشترین در یک متن. کامای صفت (۲، ۱۴)، کامای قید ربطی (۴، ۱۸)، دونقطه در سه نقش متفاوت (۲ شرحِ جمله، ۱۴ معرفی گروه اسمی، ۲۳ معرفی gerund)، ممنوعیت کاما بین نهاد و فعل (۱۲)، کاما قبل از `and`ِ دو جملهٔ کامل (۱۶)، و کامای بند قیدی مقدم (۱۹، ۲۴).
- **تله‌های تازه که ارزش برگشتن دارند:** `the reason is that` نه `because` (۵)، `drinks bottles` و فهرست استثناهایش (۸)، `rest on` در برابر `rest with` که عمداً از متن ۳ برگشت (۱۹)، `less` در برابر `fewer` با ناشمار (۱۵)، `sheer` که فقط قبل از اسم می‌آید و `involved` که فقط بعد از اسم (۲۱)، `a cut in` نه `a cut of` (۲۲)، `ban … outright` با قید بعد از مفعول در برابر `an outright ban` (۲۳)، `has yet to be banned` به‌عنوان نفی بدون `not` (۲۴ `alt`)، و `not so much X as Y` که قفلش `as` است نه `than` (۲۰ `alt`).
- **`until` بعد از دو دوره برگشت** (۱۳) و این بار کل خانواده را گرفت: نفی داخل حرف ربط نه روی فعل، ممنوعیت `will` در بند زمان، و تقابل با `unless` و `as long as`.
- **در این متن صفر ماندند:** سببی (۲۴)، وارونگی (۲۲)، وجه التزامی (۲۰)، `do` تأکیدی (۱۸)، نقل قول و backshift (۴۸)، ثبت (۳۵)، مالکیت (۶۱). طبق نقشهٔ دوره، `do` تأکیدی ستون متن ۵ و التزامی ستون متن ۶ است. **وارونگی (۲۲) همچنان تنها ردیف لایهٔ ۳ است که هیچ ستونی در این دوره ندارد و در دو متن پیاپی صفر گرفته** — متن ۵ (خانوادهٔ تأکید: `do`، cleft، پیش‌آوری) طبیعی‌ترین جای ممکن برایش است و باید همان‌جا دست‌کم ۲ واحد بگیرد، وگرنه به متن ۱۰ می‌افتد.
- **برای متن ۵ (باغ‌وحش‌ها و حفاظت از گونه‌ها):** ستونش **خانوادهٔ تأکید** است — `do` تأکیدی، cleft، پیش‌آوری — و طبق نقشه یک خانواده است نه یک ساختار، چون `do` تأکیدی تنهایی یک متن را پر نمی‌کند. ردیف `do` تأکیدی (۱۸) از متن ۹ دورهٔ ۳ به این طرف صفر گرفته و عقب‌ترین ردیف لایهٔ ۳ است؛ دست‌کم ۶ واحد و همه در «متغیر» یا «تله». cleft در این پروژه بارها آمده ولی همیشه فرعی بوده — این‌بار باید هر سه شکلش جدا بیاید: `it is … that`، `what … is`، و `all (that) … is`. وارونگی لایهٔ ۳ آن است و باید دست‌کم ۲ واحد کامل بگیرد (نه فقط `alt`)؛ موضوع باغ‌وحش برای `Not only does`، `Only by …` و `Rarely has` جای طبیعی دارد. حواست باشد که `do` تأکیدی با `be` جمع نمی‌شود و در متن ۹ دورهٔ ۳ همین محدودیت توضیح داده شده — تکرارش نکن، ازش بساز.

- **متن ۵ دورهٔ ۴ (۲۵ واحد):** باغ‌وحش‌ها و حفاظت از گونه‌ها — چهار پاراگراف: استدلال محکم طرفداران و ماجرای اوریکس عربی (۱ تا ۶)، اینکه این داستان چقدر استثناست و تکثیر در اسارت چرا سخت است (۷ تا ۱۳)، هزینه‌ها و فیل‌ها و جنگِ اولویت‌ها (۱۴ تا ۲۰)، و نتیجه‌گیری دربارهٔ ادعای آموزشی و شرطِ لازم (۲۱ تا ۲۵).
- **ستون در ۲۱ واحد از ۲۵ آمد و هر سه عضو خانواده سهم مستقل گرفتند.** `do` تأکیدی ۶ واحد (۱، ۶، ۱۱، ۱۴، ۲۲، ۲۴) و ردیفش از ۱۸ به ۲۴؛ وارونگی ۶ واحد (۸، ۱۲، ۱۳، ۱۶، ۲۱، ۲۵) و ردیفش از ۲۳ به ۲۹ — بزرگ‌ترین جهش وارونگی در کل پروژه، بعد از دو متن پیاپی که صفر گرفته بود؛ و شکافته و پیش‌آوری ۹ واحد (۳، ۵، ۷، ۱۰، ۱۷، ۱۸، ۲۰، ۲۱، ۲۳) که ردیف تازه‌ای برایشان باز شد. هیچ‌کدام در `alt` نماندند.
- **`do` تأکیدی عمداً به شش کار جدا شکسته شد، نه شش تکرار یک درس:** تأیید یک ادعا با فعل اصلیِ `have` (۱)، امتیاز دادن در گذشته با `did` + شکل ساده (۶)، پیش‌دستی بر اعتراض خواننده (۱۱)، محدودیتِ `be` و سه جایگزینش (۱۴)، امتیاز و رد در دو جملهٔ جدا (۲۲)، و جدا کردن یک زیرمجموعه داخل بند موصولی (۲۴). واحد ۲۴ تازه‌ترینشان است و در هیچ دوره‌ای نیامده بود.
- **دستور جلسهٔ قبل («`do` با `be` جمع نمی‌شود؛ تکرارش نکن، ازش بساز») در واحد ۱۴ اجرا شد:** به‌جای گفتنِ دوبارهٔ محدودیت، واحد سه جایگزین را می‌سازد (`indeed` بعد از `be`، `It is true that…` برای کل بند، و پیش‌آوری صفت که رد می‌شود) و در بخش تله صریح می‌گوید خطا از **تعمیم بیش از حدِ قاعدهٔ تازه** می‌آید، نه از ندانستنش — همان تکنیکی که واحد ۸ متن ۴ با `drinks bottles` اجرا کرد.
- **سه شکل شکافته جدا آمدند و هر کدام کار متفاوتی دارد:** `it … that` دو بار — یک بار روی فاعل (۳) و یک بار روی مفعول با `not X but Y` (۱۰)؛ `what … is` سه بار — فاعلِ گذشته (۵)، با حرف اضافهٔ آویزان (۱۸)، و دو `what` در یک جمله به شکل معادله (۲۳)؛ و `all (that) … is` یک بار (۱۷) که در بخش متغیر صریح می‌گوید کارش **محدود کردن** است نه تأکید کردن.
- **جفت عمدیِ ۳ و ۲۰ بهترین ساختهٔ متن است و الگویش ارزش تکرار دارد:** دو فارسیِ تقریباً هم‌شکل («… است، نه …») با دو جواب مخالف. واحد ۳ می‌شکافد چون دارد تصور رایج را اصلاح می‌کند؛ واحد ۲۰ نمی‌شکافد چون فقط خبر می‌دهد، و `miss` اولش همان شکافتهٔ بی‌جاست. واحد ۲۰ «واحد ضدِ ستون» این متن است و تله‌اش صریحاً از **خودِ درس** می‌آید، نه از فارسی — همان کاری که واحد ۲۳ متن ۲ و واحد ۱۲ متن ۳ کردند.
- **مرز وارونگی در دو واحد جدا زده شد، نه در توضیح:** واحد ۱۳ (`Hardly any of the animals … are`) نشان می‌دهد وقتی عنصر منفی **فاعل** است وارونگی نمی‌آید، و واحد ۱۶ (`Only when … did`) نشان می‌دهد وارونگی روی **بند اصلی** می‌نشیند نه روی بندی که `only when` ساخته. واحد ۲۱ هم وارونگی **کامل** (فعل جلو، فاعل آخر، بی‌کاما) را از وارونگیِ کمکی جدا می‌کند و مطابقت را از فاعلِ پس از فعل می‌گیرد. این سه با هم قاعده را از «کلمه» به «جایگاه و نقش» منتقل می‌کنند.
- **پیش‌آوری در دو واحد و با دو نقطه‌گذاری متضاد:** واحد ۷ عبارت حرف‌اضافه‌ایِ خنثی با کامای اجباری و بدون وارونگی، در برابر واحد ۲۱ که عبارتش جزء ساختار است و کاما **ممنوع**. هر دو صریح به اصل وزن جمله وصل شده‌اند و واحد ۲۱ به واحد ۷ ارجاع می‌دهد.
- **تله‌های تازه که ارزش برگشتن دارند:** `It was` که در شکافته همیشه مفرد می‌ماند هرچند بخش تأکیدی جمع باشد (۳ — فارسی «بودند» می‌گوید)؛ `do`ی جانشین در انتهای مقایسه در برابر `do`ی تأکیدی، یک شکل و دو کار، عمداً در واحد ۱۵ درست بعد از `do`های تأکیدی (۱۴ و ۱۱)؛ حرف اضافهٔ آویزان در بند `what` (۱۸)؛ `call itself X` بدون `as` در برابر `regard as` (۲۵)؛ `a few dozen` و `a few hundred` که مفرد می‌مانند (۷ و ۱۷)؛ `die younger` در برابر `die sooner` (۱۵)؛ `a great deal of` در برابر `a great many` (۱۹)؛ و `by` در برابر `until` که بعد از دو دوره دوباره آمد و این بار ماضی بعیدش را هم با خودش آورد (۴).
- **منفی دوگانه سه واحد گرفت و هر سه شکل متفاوت:** بعد از `Nor` بقیهٔ جمله مثبت می‌ماند (۸)، با `hardly` سراغ `ever` و `at all` می‌روی نه `never` (۱۳)، و نفی روی فاعل با فعل مثبت در برابر ممنوعیت `anyone` به‌عنوان فاعل منفی (۱۴).
- **در این متن صفر ماندند:** سببی (۲۴)، وجه التزامی (۲۰)، نقل قول و backshift (۴۸)، صفت مرکب (۴۲)، `-ever` / `no matter` (۲۷)، شکل‌های آینده (۴۳)، اسم‌سازی (۳۵). التزامی ستون متن ۶ است و عمداً دست‌نخورده ماند.
- **برای متن ۶ (هنر و موسیقی در مدرسه):** ستونش **وجه التزامی و زبان توصیهٔ سیاستی** است و ردیف التزامی (۲۰) حالا عقب‌ترین ردیف لایهٔ ۳ است — چهار متن پیاپی صفر گرفته. درس چهار متن قبل را تکرار کن: ردیفی که ستون است دست‌کم ۸ واحد می‌خواهد و همه در «متغیر» یا «تله»، نه در `alt`. التزامی را هم مثل ستون‌های قبل به **کارهای** جدا بشکن، وگرنه یک درس هشت‌بار تکرار می‌شود: بعد از فعل‌های پیشنهاد و دستور (`recommend/propose/insist/demand that … be`)، بعد از صفت‌های `It is essential/vital that`، شکل بریتانیاییِ `should` در برابر التزامی خالی، نبودِ backshift در التزامی، منفیِ التزامی (`that he not be`)، و مجهولش. موضوع «بودجهٔ هنر در مدرسه» طبیعی‌ترین جا برای زبان توصیه است. لایهٔ ۳ متن ۶ طبق نقشه `do` تأکیدی و سببی است، ولی هر دو تازه به هدف رسیده‌اند (۲۴)، پس فقط اگر خودشان آمدند. **نقل قول و backshift (۴۸) در چهار متن این دوره فقط یک بار آمده** و متن ۹ ستونی برایش ندارد؛ در متن ۶ که زبان گزارشِ توصیه است، دو سه واحد جا دارد.

- **متن ۶ دورهٔ ۴ (۲۵ واحد):** هنر و موسیقی در مدرسه — چهار پاراگراف: افت درس‌های هنر و گزارش کمیتهٔ مجلس (۱ تا ۶)، استدلال طرفدارها و اینکه بازبینی شواهد چقدر محتاط‌تر از انتظار بود (۷ تا ۱۳)، مطالبه‌های اتحادیه‌ها و پاسخ وزارتخانه (۱۴ تا ۲۰)، و نتیجه‌گیری سیاستی (۲۱ تا ۲۵).
- **ستون در ۹ واحد آمد و ردیف التزامی از ۲۰ به ۲۹ رفت** — بعد از چهار متن پیاپی که صفر گرفته بود. هیچ‌کدام در `alt` نماندند و همه در «متغیر» یا «تله» توضیح داده شدند.
- **التزامی به هفت کار جدا شکسته شد، نه هفت تکرار یک درس:** (۶) فعل محرک با فاعل سوم‌شخص مفرد و فعلِ بی `-s` (`every secondary school offer`)؛ (۸) فعل محرک با `have`ی ساده که شکل مفرد و جمعش فقط با یک `-s` فرق دارد؛ (۱۰) **منفیِ التزامی** بدون `do` (`that the subject not be reduced`) که تنها منفیِ بی‌`do`ی انگلیسی امروزی است؛ (۱۵) **مجهولِ** التزامی (`that music be restored`)؛ (۱۶) محرک از جنس **اسم** (`their demand that … be taught`) به‌علاوهٔ مطابقت فعل اصلی از راه دور؛ (۱۸) نسخهٔ بریتانیاییِ `should` و نردبان سه‌گزینه‌ایِ جزوه؛ (۲۲) **دامنهٔ** التزامی — دو فعل در یک بند، `whoever designs` با `-s` و `think` بی `-s`؛ (۲۵) التزامی در کنار `be to` که همان فعل `be` را یک بار صرف‌شده و یک بار خام می‌گذارد.
- **واحد ۱۳ «واحد ضدِ ستون» این متن است و از همه مهم‌تر:** همان فعل `insist`، این بار با بند خبری (`insist that nothing is being cut`). جفت عمدیِ ۸ و ۱۳ یک فعل را با دو دستور زبان مقابل هم می‌گذارد و تست تشخیص را صریح می‌دهد: اگر جلوی بند «باید» جا می‌شود التزامی است، اگر «که واقعاً» جا می‌شود خبری. بخش تله صریحاً می‌گوید خطا از **تعمیم بیش از حدِ قاعدهٔ تازه** می‌آید — همان تکنیکی که واحد ۸ متن ۴ و واحد ۲۰ متن ۵ اجرا کردند، و این بار روی خودِ فعلِ ستون.
- **نقل قول و backshift ۵ واحد و ردیف از ۴۸ به ۵۳** — بعد از اینکه در چهار متن اول این دوره فقط یک بار آمده بود. هر پنج از یک زاویه: گذشتهٔ ساده به ماضی بعید با `had had` (۳)، حال استمراری به گذشتهٔ استمراری (۴)، `will` به `would` **به‌علاوهٔ جابه‌جایی قید زمان** (۵ `the following September`)، حال به گذشته داخل گزارشِ پژوهش (۱۲)، و دو بند گزارش‌شده در یک جمله (۱۷).
- **واحد ۶ عمداً بلافاصله بعد از سه واحد backshift آمد** تا مرز را بزند: التزامی از نظام زمان بیرون است و با گذشته بودن فعل اصلی عقب نمی‌رود. همین تقابل در واحد ۲۵ دوباره و از زاویهٔ دیگر آمد.
- **تله‌های تازه که ارزش برگشتن دارند:** قید بین `have` و `been` در مجهولِ کامل و ممنوعیتش وسط فعل عبارتی (۱)، `fall by` در برابر `fall to` (۲)، `had had` که تکرار شکل در آن مجاز است (۳)، `the way how` که هیچ‌وقت ساخته نمی‌شود (۱۴)، `demand that` در برابر `demand which` (۱۶)، `have no plans to` در برابر `have no intention of` که یکی مصدر می‌گیرد و دیگری gerund (۱۷)، دو `by` با دو کار متفاوت در یک جمله — روش و عامل (۱۹)، صفتی که دنباله دارد و به راست اسم می‌رود (۲۰ `someone qualified to teach`)، `another five years` که مفرد نمی‌شود (۲۳)، و `whether` در نقش مسند که `if` جایش را نمی‌گیرد (۲۴).
- **جفت ۹ و ۲۴ هم بی‌سروصدا دو تلهٔ جانشینی را گرفت:** `one` در برابر `it` با اجبارِ جای ضمیر وسط فعل عبارتی (`pick one up`)، و `get to do` در برابر `have to do`.
- **سببی ۱ واحد (۱۹) و ردیف به ۲۵:** `have their instruments donated` با هر سه شکل در بخش قاعده (`have + شیء + V3`، `have somebody do`، `get somebody to do`). فارسیِ واحد عمداً «با ترتیب دادن اینکه…» گفت تا شکل انگلیسی را لو ندهد.
- **در این متن صفر ماندند:** `do` تأکیدی (۲۴)، جملهٔ شکافته و پیش‌آوری (۹ — فقط در `alt`ِ ۲۴ آمد)، «حرف اضافه + اینکه» (۲۹)، اسم‌سازی (۳۵)، ضمیر بازگشتی فارسی (۳۹).
- **برای متن ۷ (ورزش حرفه‌ای و پول پخش تلویزیونی):** ستونش **«حرف اضافه + اینکه» و بند اسمی** است و ردیفش (۲۹) حالا عقب‌مانده‌ترین ردیف دورهٔ ۲ است — در کل دورهٔ ۴ صفر گرفته. درس پنج متن قبل را تکرار کن: ستون دست‌کم ۸ واحد می‌خواهد و همه در «متغیر» یا «تله». مثل ستون‌های قبل به کارهای جدا بشکنش، وگرنه یک درس هشت‌بار تکرار می‌شود: `the fact that` به‌عنوان تنها راهِ گذاشتن بند بعد از حرف اضافه، `in that` و `except that`، بند اسمی در نقش فاعل و اصل وزن جمله، `whether` بعد از حرف اضافه (در متن ۶ پایه‌اش در واحد ۲۴ گذاشته شد — ازش بساز، تکرارش نکن)، بند اسمی بعد از صفت (`aware that`, `confident that`)، و `what`ِ بند اسمی در برابر `that`. لایهٔ ۳ طبق نقشه مالکیت و اعداد است و موضوع ورزش برای هر دو طبیعی است. **`do` تأکیدی (۲۴) و جملهٔ شکافته (۹) هر دو به هدف رسیده‌اند یا نزدیک‌اند، پس فقط اگر خودشان آمدند.**

- **متن ۷ دورهٔ ۴ (۲۵ واحد):** ورزش حرفه‌ای و پول پخش تلویزیونی — چهار پاراگراف: چطور پول پخش وارد ورزش شد و چرا شبکه‌ها حاضر بودند بپردازند (۱ تا ۶)، پول کجا رفت و چه شکافی ساخت (۷ تا ۱۳)، هزینه‌ای که هوادارها دادند به‌علاوهٔ یک روایت میدانی از باشگاهی در شمال شرقی (۱۴ تا ۲۰)، و نتیجه‌گیری سیاستی دربارهٔ تقسیم درآمد (۲۱ تا ۲۵).
- **ستون در ۱۶ واحد آمد و ردیف «حرف اضافه + اینکه» از ۲۹ به ۳۸ رفت، ردیف بند اسمی از ۱۱۵ به ۱۳۱.** هیچ‌کدام در `alt` نماندند.
- **ستون به هشت کار جدا شکسته شد، نه هشت تکرار یک درس:** (۲) بند `what` بعد از حرف اضافه، به‌عنوان تنها راه سالمِ `depended on …`؛ (۵) هستهٔ اسمیِ `for the simple reason that`؛ (۶) `whether or not` و ممنوعیت `if or not`؛ (۸) `in that` به‌عنوان یکی از دو حرف اضافه‌ای که مستقیم بند `that` می‌گیرند (جفتش `except that` در بخش قاعده)؛ (۱۳) `the fact that` با نردبان سه‌پله‌ای اولویت — اول حرف ربط، بعد اسم‌سازی، آخر `the fact that`؛ (۱۶) بند بعد از **صفت** و افتادن حرف اضافه (`aware that` در برابر `aware of that`)؛ (۲۱) `the question of whether` و ممنوعیت `if` بعد از حرف اضافه؛ (۲۲) خانوادهٔ `on the grounds that` / `on the assumption that` / `in the hope that` به‌عنوان یک قالب با اسم متغیر؛ (۲۴) بند `whether` در جایگاه فاعل.
- **واحد ۱۷ کار نهمی را گرفت که هیچ‌وقت ردیف نداشته:** تکرار `that` در بند گزارش‌شدهٔ دوم (`and that a third had…`) و اینکه بدون آن، بند دوم از نقل قول بیرون می‌افتد و حرف نویسنده خوانده می‌شود.
- **سه واحد شکافته (۱۱، ۱۹، ۲۵) عمداً پخش شدند** تا قالب `What X is that Y` سه بار از سه زاویه بیاید: با `not … but …` (۱۱)، در تقابل با سکوتِ یک نهاد (۱۹)، و به‌عنوان بستنِ نتیجه‌گیری (۲۵). ردیف از ۹ به ۱۲ و حالا به هدف رسیده.
- **واحد ۱۹ «واحد ضدِ ستون» این متن است:** همان فعل `insist` متن ۶، این بار با بند خبری و backshift (`insisted that the rise was modest`). جفت عمدیِ متن ۶ واحد ۸ و این واحد، تست «باید / که واقعاً» را یک دوره بعد دوباره می‌زند.
- **مالکیت ۷ واحد و ردیف از ۶۴ به ۷۱:** جفتِ `of` و `'s` در یک جمله (۴ `the value of the league's contract`)، آپاستروف جمع (۷ `players'`، ۱۴ `supporters'` و `broadcasters'`)، `the world's best players` (۹)، `own` که مالک‌نما می‌خواهد (۱۱)، و `its` در برابر `it's` با تست جایگزینی (۱۸).
- **اعداد ۶ واحد و ردیف از ۵۵ به ۶۱:** `roughly half of` (۳)، `twentyfold` و ممنوعیت `than` با ضریب (۴)، `per cent` در برابر `percentage` و مطابقت با اسمِ بعد از `of` (۱۰)، `several times as much as` (۱۲)، `one in five` که مفرد است (۱۷)، و `several thousand` که جمع نمی‌شود (۱۸).
- **تله‌های تازه که ارزش برگشتن دارند:** `how recently … became` بی وارونگی و بی `did` (۱ و دوباره ۲۵ با `how it is played`)، `the 1990s` بی آپاستروف (۲)، `finish bottom of` بی حرف تعریف (۱۲)، `cost somebody something` بی حرف اضافه و بی `costed` (۱۳)، `months in advance` در برابر `before`ِ بی‌لنگرگاه (۱۵)، `no way of getting home` با دو تله در یک عبارت (۱۶)، `altogether` در برابر `all together` (۱۷)، `raise/rise` و `stop doing/stop to do` در یک واحد (۱۸)، `by` در برابر `until` (۲۰)، `every` با اسم مفرد (۲۲)، و `either` که مفرد است و منفی را حمل نمی‌کند (۲۴).
- **نقطه‌گذاری ۵ واحد و ردیف به ۱۲۰:** سه بار نقطه‌ویرگول بین دو جملهٔ مستقل (۷، ۱۹، ۲۴)، کامای اجباری بند توضیحی (۹)، و دونقطه در برابر نقطه‌ویرگول در بخش قاعدهٔ واحد ۲۵ — که چهار انتخاب نقطه‌گذاری را یک‌جا جمع می‌کند.
- **در این متن صفر ماندند:** سببی (۲۵)، وارونگی (۳۰)، `-ever` (۲۹)، `do` تأکیدی (۲۴)، و `although/despite/however` (۵۹).
- **متن ۸ دورهٔ ۴ (۲۵ واحد):** کشاورزی شهری و مزارع عمودی — چهار پاراگراف: وعده و جذابیتِ کوتاه کردن مسیر غذا (۱ تا ۶)، آنچه در عمل باقی می‌ماند و مسئلهٔ نور و برق (۷ تا ۱۳)، یک روایت میدانی از مزرعه‌ای در انباری متروک نزدیک شهری در شمال انگلستان (۱۴ تا ۲۰)، و نتیجه‌گیری دربارهٔ آنچه کشاورزی شهری واقعاً می‌تواند بدهد (۲۱ تا ۲۵). هیچ عدد و درصدی جز «دوازده طبقه»، «۲۰۱۹»، «۲۰۲۲» و «سه برابر» در متن نیست.
- **ستون در ۸+۸ واحد آمد و هر دو نیمه‌اش به هدف رسید.** جفت‌های معنایی نزدیک از ۵۸ به ۶۶ و ضمیر بازگشتی از ۴۱ به ۴۹. هیچ‌کدام در `alt` نماندند.
- **نیمهٔ جفت‌ها به هشت کار جدا شکسته شد، نه هشت تکرار یک درس:** (۴) `rise/raise` به‌عنوان جفت لازم–متعدی، با تستِ «دنبال مفعول بگرد»؛ (۵) `less/fewer` به‌علاوهٔ تقویت تفضیلی با `far` نه `very`؛ (۷) `economic/economical` — عمداً از سمتِ مخالفِ دورهٔ ۳ متن ۲، که آنجا `economical` درست بود؛ (۸) `replace` که در فارسی حرف اضافه دارد و در انگلیسی ندارد، کنار `grow` که **استثنای** قاعدهٔ `rise/raise` است و هم لازم است هم متعدی؛ (۱۰) `amount/number` با نکتهٔ `the number of … has` در برابر `a number of … have`؛ (۱۲) `efficient/effective` به‌علاوهٔ پیشوند منفی `in-` نه `un-`؛ (۱۶) `lie/lay/lain` در برابر `lay/laid/laid` در برابر `lie/lied/lied` — سه فعل در یک جدول؛ (۲۴) `a substitute for` در برابر فعل `replace` که حرف اضافه نمی‌گیرد، یعنی همان ریشهٔ واحد ۸ یک بار دیگر از سمت اسم.
- **نیمهٔ ضمیر بازگشتی هر سه شکل جزوه را گرفت و هر شکل را دو بار برد:** شکل ۱ (ضمیر مفعولی) در ۶ و ۱۰، شکل ۲ (ضمیر بعد از حرف اضافه) در ۱۱، ۱۹ و ۲۱ — با ممنوعیت حرف اضافهٔ دوتایی در ۱۱ و ۲۱ — شکل ۳ (`whose`) در ۱۵، و در ۲۵ روی فعلی که فارسی‌اش حرف اضافه دارد و انگلیسی‌اش ندارد (`suit`). واحد ۲ هم بند `what` را به‌عنوان راهِ فرار از همین خانواده آورد.
- **جانشینی ۶ واحد و ردیف از ۸۸ به ۹۴:** `does` در مقایسه (۵)، `the ones that` (۹ و دوباره ۲۵)، حذف با `but not` (۱۰)، حذف با نگه داشتن `to` (۱۸)، و حذف بعد از فعل کمکی در نقل قول (۲۰).
- **صفت مرکب ۴ واحد و ردیف از ۴۵ به ۴۹:** `purpose-built` (۳)، `year-round` (۷)، `high-value` (۹) و `labour-intensive` (۲۳). قاعده هر بار یکسان گفته شد — خط تیره قبل از اسم، و هیچ جزئی جمع نمی‌شود — ولی خانوادهٔ واژگانی هر بار فرق کرد.
- **`although/despite/however` بالاخره از صفر درآمد و ۳ واحد گرفت (۵۹ ← ۶۲):** `however` در جایگاه وسطی با دو ویرگول و ممنوعیت comma splice (۷)، `despite` بدون `of` در برابر `in spite of` که `of` می‌خواهد (۱۳)، و `although` بدون `but` (۱۷). این عقب‌مانده‌ترین ردیف لایهٔ ۲ بود و حالا نیست.
- **تله‌های تازه که ارزش برگشتن دارند:** `have distance with` به‌جای `… away` (۱)، حذف مرجع بعد از `away` (۱)، `most of` که تعیین‌کننده می‌خواهد (۱ و ۹)، `spend on` نه `spend for` (۲)، `unless` با فعل مثبت (۸)، `be paid for` با حرف اضافهٔ آویزان در مجهول (۸)، وجه میانه `sold well` نه `were sold well` (۱۷)، `three times as much as` نه `more than` (۱۸)، `no such a cost` (۱۹)، `that`ِ دوم که تعیین می‌کند ادعا مال کیست (۲۰)، `the following` در برابر `next` و افتادن `in` (۲۱)، `something smaller` با صفت بعد از اسم (۲۲)، `cost/cost/cost` و ممنوعیت `costed` (۲۳)، قالب «فاعل + صفت + مصدرِ بی‌مفعول» (۲۳)، و `may yet` با جای قفل‌شدهٔ `yet` (۲۵).
- **در این متن صفر ماندند:** سببی (۲۵)، وارونگی (۳۰)، `-ever` (۲۹)، `do` تأکیدی (۲۴)، وجه التزامی (۳۰)، شرطی (۵۲) و «حرف اضافه + اینکه» (۳۸).
- **برای متن ۹ (Task 2: خودروی خودران و مسئولیت):** ستونش طبق نقشه **وزن جمله، extraposition، فاعل صوری و hedging** است و لایهٔ ۳ آن backshift و التزامی. این متن آخرین فرصت برای وجه التزامی (۳۰) و backshift (۵۷) پیش از متن جمع‌بندی است، و موضوعِ مسئولیت حقوقی برای هر دو طبیعی است: `it is essential that the manufacturer be liable`، `regulators recommended that testing be extended`، و گزارش نظر کارشناس‌ها. نردبان قطعیت و الزام هم در همین موضوع خودش می‌آید. **متن ۱۰ باید جمع‌بندی باشد** و طبق نقشه چهار ردیف صفرماندهٔ بالا را یک‌جا بگیرد: سببی، وارونگی، `-ever` و `do` تأکیدی.

- **متن ۹ دورهٔ ۴ (۲۵ واحد):** Task 2 — خودروی خودران و مسئولیت. چهار پاراگراف: طرح پرسش و استدلال طرفدارها (۱ تا ۶)، آنچه شواهد واقعاً نشان می‌دهد و شرایط آزمایش‌ها (۷ تا ۱۳)، مسئلهٔ مسئولیت حقوقی و گزارش یک کمیته (۱۴ تا ۲۰)، و نتیجه‌گیری با دو پرسش باز (۲۱ تا ۲۵). هیچ عدد و درصدی در متن نیست — ردیف اعداد (۶۱) عمداً دست‌نخورده ماند.
- **ستون در ۱۳ واحد آمد و ردیف فاعل صوری از ۵۸ به ۶۸ رفت، وزن و ترتیب جمله از ۴۵ به ۵۳.** هیچ‌کدام در `alt` نماندند.
- **ستون به ده کار جدا شکسته شد، نه ده تکرار یک درس:** (۱) `There is` برای معرفی چیز تازه، با `little` در برابر `a little`؛ (۲) extraposition یک بند **پرسشی** با `It is far less clear who…`؛ (۴) راه دومِ جملهٔ بی‌فاعل — بردن **مفعول** به جایگاه فاعل با حرف اضافهٔ آویزان (`hard to argue with`)، که جفت `It is hard to argue with their case` است؛ (۶) قالب استدلالی `It does not follow that` با فاعل gerund؛ (۷) مجهول غیرشخصی `It is often claimed that` و جفتش `are said to have been`؛ (۱۱) `There is little evidence that` با دو `that`ِ متفاوت در یک جمله — هم‌نقشِ اسم در برابر موصولی؛ (۱۳) `It would be unwise for X to V` با قاعدهٔ `for` موقعیت / `of` شخصیت؛ (۲۱) `there` با فعلی جز `be` (`There remain`) و مطابقت با فاعلِ پس از فعل؛ (۲۵) `It seems likely that` که هم جاپرکن است هم تعدیل ادعا و متن را با همان ابزاری می‌بندد که باز کرده بود.
- **واحد ۱۴ «واحد ضدِ ستون» این متن است و از همه مهم‌تر:** `The real difficulty lies elsewhere` — بعد از پنج جمله‌ای که با `There is` و `It is` شروع شده بودند، جمله‌ای که هیچ‌کدام را نمی‌گیرد. نیمهٔ دومِ قاعده را می‌گوید: `there` فقط اطلاعات **تازه و نامعین** را وارد می‌کند و با `the` شکسته می‌شود. بخش تله صریحاً می‌گوید خطا از **تعمیم بیش از حدِ قاعدهٔ تازه** می‌آید — همان تکنیکی که واحد ۸ متن ۴، واحد ۲۰ متن ۵ و واحد ۱۳ متن ۶ اجرا کردند. نشانهٔ عملی‌اش هم داده شده: به تعیین‌کنندهٔ اسمِ بعد از فعل نگاه کن.
- **وجه التزامی ۳ واحد و ردیف از ۳۰ به ۳۳، هر سه از زاویهٔ تازه:** (۱۶) معلومِ خالی با فعل `pay` و یک عبارت معترضه بین فاعل و فعل که مطابقت را می‌کشد؛ (۱۹) مجهول (`no vehicle be allowed`) با فاعل منفی و فعل مثبت؛ (۲۰) **منفیِ** التزامی (`not be owned`) به‌عنوان تنها جای انگلیسی امروزی که نفی بدون `do` ساخته می‌شود، در کنار یک گزارشِ معترضه‌ای که نشان می‌دهد `It is essential` با گذشته بودن `added` عقب نمی‌رود.
- **backshift ۳ واحد و ردیف از ۵۷ به ۶۰:** (۹) گذشتهٔ ساده به ماضی بعید؛ (۱۷) حال به گذشته و حال استمراری به گذشتهٔ استمراری؛ (۱۸) `will` به `would` به‌علاوهٔ ممنوعیت دو modal پشت سر هم (`would have to`، نه `would must`).
- **جفت عمدیِ ۱۶ و ۱۷ بهترین ساختهٔ متن است:** دو بند گزارش‌شده پشت سر هم، یکی که عقب **نمی‌رود** (التزامی) و یکی که می‌رود (خبری). بخش «متغیر» واحد ۱۷ صریح می‌گوید خطر اینجاست که درسِ واحد قبل یک پله بیش از اندازه تعمیم داده شود، و تست «باید / که واقعاً» را دوباره می‌زند. واحد ۱۹ هم همان فعل `insist` متن‌های ۶ و ۷ را برمی‌گرداند، این بار در نسخهٔ التزامی — سه متن، یک فعل، سه دستور زبان.
- **منفی دوگانه ۶ واحد و ردیف از ۹۵ به ۱۰۱** (۵، ۱۲، ۱۴، ۱۹، ۲۲، ۲۴) و بخش تلهٔ واحد ۲۴ صریح می‌گوید این چهارمین بار در همین متن است — عمدی، برای نشان دادن اینکه این خطا با یک‌بار فهمیدن تمام نمی‌شود. `any` که نمی‌تواند فاعلِ منفی باشد در ۱۴ و ۱۹ دو بار آمد.
- **ضمیر بازگشتی ۶ واحد و ردیف از ۴۹ به ۵۵** (۴، ۸، ۱۰، ۱۱، ۲۲، ۲۳) و این بار از چهار جای متفاوت: در ساختار صفت + مصدر (۴ و ۱۰)، در بند موصولی با حرف اضافهٔ جلورفته (۸)، در بند موصولی معمولی (۱۱، ۲۲)، و در بند پرسشیِ درون‌نشانده که مفعولش سر بند رفته (۲۳).
- **سؤال درون‌نشانده در ۵ واحد** (۲، ۱۲، ۱۵، ۲۲، ۲۳) و هر بار یک نکتهٔ اضافه: صفت که با `how` جلو می‌آید (۱۲)، حذفِ کل بند بعد از کلمهٔ پرسشی (۱۵)، بند پرسشی در نقش مسند (۲۲)، و `how much` + اسم که با هم جابه‌جا می‌شوند (۲۳).
- **تله‌های تازه که ارزش برگشتن دارند:** `little` در برابر `a little` که دو بار و با دو اسم ناشمار آمد (۱ `doubt`، ۱۱ `evidence`)؛ `under which` برای شرایط، به‌عنوان هم‌نشینی‌ای که قاعدهٔ مولد ندارد (۸)؛ `with considerable care` به‌جای قید، به‌عنوان ارتقای ثبت (۹)؛ `whose` برای بی‌جان (۱۰)؛ `read/treat X as Y` بدون `like` و بدون `as being` (۱۳)؛ `elsewhere` در برابر `in another place` (۱۴)؛ `tell` در برابر `say` که بعد از دو دوره برگشت (۱۸)؛ `onto` در برابر `on` برای ورود (۱۹)؛ `no amount of` + ناشمار با ضمیر مفردِ `its own` در کنار `questions`ِ جمع (۲۱)؛ فعل سبکِ `carry/bear/meet the cost` در برابر `give` (۲۲)؛ `in exchange for` که `of` نمی‌گیرد (۲۳)؛ `neither` که مفرد است و نفی را حمل می‌کند، کنار `any easier` که فقط در بافت منفی می‌آید (۲۴)؛ و `long before` که تنها تشدیدکنندهٔ مجازش `long` است (۲۵).
- **جفت ۳ و ۲۵ بی‌سروصدا قاعدهٔ جانشین را دو تکه کرد:** `than people are` چون فعل `be` است (۳) و `before the roads do` چون فعل `change` است (۲۵) — یک جایگاه، دو جانشین، و واحد ۲۵ صریح به واحد ۳ ارجاع می‌دهد.
- **در این متن صفر ماندند:** سببی (۲۵)، وارونگی (۳۰)، `-ever` / `no matter` (۲۹)، `do` تأکیدی (۲۴)، جملهٔ شکافته و پیش‌آوری (۱۲)، شرطی (۵۲)، اعداد و درصد (۶۱)، مالکیت (۷۱)، افعال عبارتی (۱۲۶).
- **برای متن ۱۰ (جمع‌بندی: جنگل، کاغذ و بازیافت):** آخرین متن دوره است و طبق نقشه باید **چهار ردیف صفرماندهٔ دوره** را یک‌جا بگیرد: سببی (۲۵)، وارونگی (۳۰)، `-ever` / `no matter` (۲۹) و `do` تأکیدی (۲۴). هر چهار تا در کل دورهٔ ۴ فقط در متن ۵ (وارونگی و `do` تأکیدی) و متن ۶ (سببی، یک واحد) چیزی گرفته‌اند، پس هر کدام دست‌کم ۲ تا ۳ واحد لازم دارند و همه در «متغیر» یا «تله»، نه در `alt`. چون متن مرور است، درس نه‌تنهایی بلکه در **تقابل** بهتر می‌نشیند: `do` تأکیدی کنار `do`ی جانشین، وارونگی کنار پیش‌آوریِ بی‌وارونگی، سببی کنار مجهولِ معمولی، و `-ever` کنار بند موصولی ساده. شرطی (۵۲) و مالکیت (۷۱) و اعداد (۶۱) از هدف جلوترند؛ فقط اگر خودشان آمدند. **یادت باشد این متن آخرِ دوره است:** بعد از تولیدش باید بخش «وضعیت متن‌ها — دورهٔ ۴» به «تمام شد» تغییر کند و جلسهٔ بعد باید قبل از متن اولِ دورهٔ ۵، نقشهٔ تازه بچیند و تأیید بگیرد.
